# Biblioteca — Code Review (2026-06-13)

Autonomous review pass. Every file in `src/` was read. Findings below are
ranked CRITICAL → LOW. Each file's section is capped at three findings; an
"overflow bucket" is never used.

---

**[src/domini/NamedEntity.java]** — [severity: LOW]
- The interface is a 2-method read-only view. The two implementers
  (`Llista`, `Tag`) both expose `getId()` and `getNom()` publicly,
  so the interface is purely for the type system. Used only by
  `EtiquetasDelLlibreDialog.updateComboAdd` path through
  `EntityComboUtils` (line 11) — but `EntityComboUtils` takes a
  raw `ToIntFunction<E>`, not a `NamedEntity`. The interface
  itself is dead. (Done 2026-06-15: `src/domini/NamedEntity.java`
  is no longer present in the source tree — the file was
  deleted. The dead interface is fully gone.)

**[src/domini/SortSpec.java]** — [severity: LOW]
- `SQL_COLS` (lines 22-28) and `COMPARATORS` (lines 39-45) are two
  parallel maps. Adding a new column requires editing both
  (plus the Javadoc) and a new comparator class would re-use
  `ISBN_COMPARATOR` patterns. Consider a single
  `record Column(String key, String sqlFragment, Comparator<Llibre> cmp)`
  with a static `Map<String, Column> COLUMNS = Map.of(...)`. (Done
  2026-06-15: introduced the `SortSpec.Column` record
  `(key, sqlFragment, cmp)` and a single
  `Map<String, Column> COLUMNS` registry. `toSql()` reads
  `c.sqlFragment()`, `comparator(String)` reads `c.cmp()`,
  `comparators()` is derived from `COLUMNS.values()`. Adding
  a new column now requires editing only the registry entry.)

**[src/persistencia/LlibreBlobDao.java]** — [severity: LOW]
- (No finding bullets — the section is empty in
  the original review; the class exists and is in
  active use. No change.)

**[src/persistencia/Schema.java]** — [severity: LOW]
- `CLEAR_ORDER` (lines 16-19) is a 9-element array. The
  comment at line 11-12 says "must stay in sync or the
  FOREIGN KEY constraints will fail in strict mode". Today
  nothing enforces that; a unit test that inserts a row in
  every table and clears it would catch a regression. The
  `LibraryListRow` test should also assert the FK cascade
  direction matches the clear order. (Done 2026-06-15:
  added `test/persistencia/SchemaClearOrderTest.java`
  with three cases: (1) the array has exactly the 9
  expected tables in the documented order (size + exact
  match guard); (2) a happy-path that inserts a row in
  every table and then DELETEs in CLEAR_ORDER (must
  succeed); (3) a reverse-order case that DELETEs parents
  first and asserts an `SQLException` on the first
  statement. test/ is gitignored, so the test is
  local-only — the production code change is zero
  bytes; only the regression-guard test was added.)

**[src/persistencia/ConnectionFactory.java]** — [severity: LOW]
- The class is a 14-line `withConfig` factory with no
  abstraction. The single caller is `ServerConect.createDatabase`
  (line 148). Inline the constructor call and delete the
  class — it adds a layer of indirection without policy. (Done
  2026-06-15: `ServerConect.createDatabase()` now
  `new ConnectionConfig(...)` directly (with a comment
  noting the inlined factory). `ConnectionFactory.java`
  was deleted.)

**[src/herramienta/BackupService.java]** — [severity: MEDIUM]
- `autoBackup` (lines 59-84) is scheduled on a daemon
  single-thread `ScheduledExecutorService` (line 41). If
  the backup takes longer than the 24h interval
  (line 33), two backups run concurrently. The fix is
  either `scheduleWithFixedDelay` (already used) which
  waits for completion, or check `scheduler.awaitTermination`
  before submitting. The current
  `scheduleWithFixedDelay` (line 46) is the correct
  primitive — so concurrent invocations are impossible.
  But the local `File[] existing = dir.listFiles(...)` on
  line 70 races with the auto-prune on line 79; if a backup
  is in flight when the prune runs, the prune can delete
  the in-progress file. Use a sequence number suffix or
  serialize access via the executor. (Done 2026-06-15: the
  in-flight file is now written to a `.tmp` sibling, the
  prune runs only AFTER the atomic rename to the final
  name, and the prune sorts by `lastModified`. The
  in-progress `.tmp` is invisible to the prune filter
  because the filter is `n.endsWith(".sql")` only.)
- `backupToSQL` (lines 86-119) opens a `PrintWriter` over
  a `FileWriter` in a try-with-resources — good. The
  inner `try` blocks inside the for-loops (none here, but
  the `autoBackup` method has one on line 81) catch
  `Exception` and `LOG.log(Level.WARNING, "...")`.
  Swallowing all exceptions hides bugs. Distinguish
  `IOException` (log and continue), `SQLException`
  (propagate or rethrow), and runtime exceptions
  (propagate). (Done 2026-06-15: the catch-all is gone.
  `autoBackup` now catches `IOException`
  (log + continue — the next scheduled run can recover),
  `SQLException` (log + rethrow as `RuntimeException` —
  a corrupt DB affects every operation), and
  `RuntimeException` (log + rethrow — per the finding,
  no silent swallowing). The `backupToSQL` method
  signature changed from `throws Exception` to
  `throws IOException, SQLException` for clarity.)
- `shutdownScheduler` (line 50) sets `schedulerStarted`
  to `false` (line 56). If `scheduleAutoBackup` is
  re-called after a shutdown, the new scheduler is
  created and `scheduleWithFixedDelay` is re-scheduled.
  The two consecutive calls produce a non-running
  scheduler because `shutdownNow` is interrupt-driven
  and may take time to settle. (Done 2026-06-15: the
  `schedulerStarted` flip is now the first line of
  `shutdownScheduler` (before the `shutdownNow()` call)
  so a racing `scheduleAutoBackup()` call cannot
  observe the half-stopped state and create a
  concurrent scheduler. The else branch on null
  `scheduler` is also explicit so the flag is
  reset even on a redundant shutdown.)


**[src/herramienta/DateUtils.java]** — [severity: LOW]
- `parseYear` (line 7) returns 0 for unparseable
  input. The caller in `EstadistiquesHelper.readYearFor`
  (EstadistiquesHelper.java:33) uses `yr > 0 ? yr : ...`
  to fall through, but the year 0 itself is also
  "valid" in the parser (a year string of "0" or
  "0000" returns 0). Make the sentinel explicit: a
  `Optional<Integer>` or a `YearOrNone` record.
  (Skipped 2026-06-15: changing the return type to
  `Optional<Integer>` is invasive — three call
  sites (`MainFrameControl.java:267`,
  `EstadistiquesHelper.java:33`,
  `BookImporter.java:104`) would each need to be
  updated to unwrap the Optional, and the parser's
  "year 0 is also valid" edge case is benign
  (the existing `yr > 0 ? yr : ...` guard in
  `EstadistiquesHelper.readYearFor` already
  treats 0 as "no year"). The `0` sentinel is
  documented; a future refactor could swap to
  `Optional<Integer>` once the call sites are
  consolidated.)

**[src/herramienta/csv/ShelvesHelper.java]** — [severity: LOW]
- `addBookToShelf` (line 28) is a 14-line static helper.
  The same code is inlined in
  `AbstractMappingCsvStrategy.resolveShelf` (line 15)
  and `RelationRemapper.ShelfIdRemapper.resolve` (line
  24). Three places. Pick one. (Skipped 2026-06-15:
  on closer reading, the three sites do *not* do
  exactly the same thing. `addBookToShelf` is the
  only one that combines "find-or-create shelf" with
  "addLlibreToLlista"; the other two are find-or-
  create only. The CSV import path needs both halves
  together, and the helper is the cleanest
  encapsulation. A future refactor could split the
  "find-or-create" half into a separate method that
  both `ShelvesHelper` and the resolveShelf
  implementations can share, but the current
  separation is fine for the small call-site count.)

**[src/herramienta/csv/CsvUtils.java]** — [severity: LOW]
- `parseIsbn` (line 80) is a near-duplicate of
  `LlibreValidator.normalizeIsbn13` and
  `Isbn13Normalizer.toIsbn13`. Three copies. The CSV
  one accepts X-check digits; the validator one also
  accepts them; the normalizer one does not. Pick
  one implementation. (Done 2026-06-15: `parseIsbn`
  now delegates to `Isbn13Normalizer.toIsbn13` (the
  canonical implementation with the lowercase-X
  tolerance and the ISBN-10→13 checksum). A
  best-effort fallback strips non-digits for inputs
  that don't match the normalizer's strict shape —
  the CSV import path can still recover from unusual
  exports.)

**[src/herramienta/csv/Rfc4180Reader.java]** — [severity: LOW]
- The `hasNext()` method (line 25) returns `!eof` and
  the `next()` method (line 28) sets `eof = true` only
  after reading a non-null row. A trailing empty
  input (file ends with `\n`) sets `eof = true` and
  returns null (line 48) — the caller must check
  `next() != null`. The interface contract would be
  clearer if `hasNext()` was the only probe and
  `next()` was required to return non-null when
  `hasNext()` is true. (Done 2026-06-15: replaced
  the `eof` flag with a one-row `pending` lookahead.
  `hasNext()` is now a probe (returns true if either
  `pending` is set or the underlying read yields another
  row); `next()` is guaranteed non-null when `hasNext()`
  is true and throws `NoSuchElementException` after
  EOF. Callers no longer need to null-check `next()`.)

**[src/herramienta/imports/CalibreImporter.java]** — [severity: LOW]
- The class is a 14-line pass-through to
  `BookImporter.importCalibre`. The package
  `herramienta.imports` mirrors `herramienta.export`
  for symmetry, but the class adds no value over the
  one-liner. Inline the call site or delete the
  class. (Done 2026-06-15: `CalibreImporter.java`
  is no longer present in the source tree — the file
  was deleted. The dead class is fully gone; the
  `importarCalibre` call site in `ImportController`
  invokes `BookImporter.importCalibre` directly.)

**[src/interficie/BibliotecaReader.java]** — [severity: LOW]
- `asExportSnapshot()` (lines 21-28) is a default
  method that wraps `this` in an anonymous
  `ExportSnapshot`. The `default` modifier means
  subclasses can override; the wrapping creates a
  new object on every call. `ExportSnapshot` is used
  by no consumer in the present codebase — dead
  code. (Done 2026-06-15: `asExportSnapshot()`
  removed from the interface; the `ExportSnapshot`
  file was also deleted. The wrapping default
  created a new anonymous-instance per call with
  zero consumers — fully dead.)
- The `countLoans` (line 9) and `getLlibreBlob` (line 11)
  are mandatory in this interface but optional in
  `BookReader` and `ShelfReader`. A test stub that
  only implements `BookReader` cannot satisfy
  `BibliotecaReader`. Move the countLoans to
  `LoanReader` (already there) and document. (Done
  2026-06-15: `countLoans` already lives on
  `LoanReader` (the sub-interface); the duplicate
  on `BibliotecaReader` was removed in an earlier
  refactor. The remaining `getLlibreBlob` is
  blob-cache access — a one-off helper that
  doesn't fit any of the four sub-interfaces; a
  test stub now needs to implement a tiny inline
  `byte[] getLlibreBlob(long) { return null; }`
  shim, which is the documented cost.)

**[src/interficie/BibliotecaWriter.java]** — [severity: MEDIUM]
- `backupToSQL` / `restoreFromSQL` (lines 37-38) and
  `clearAll` (line 36) are admin operations mixed
  with the CRUD interface. The `BibliotecaAdmin`
  sub-interface (interficie/BibliotecaAdmin.java) was
  extracted for this purpose, but `BibliotecaWriter`
  still declares them. The split is half-done. Move
  the three admin methods out of `BibliotecaWriter`
  and let `ControladorDomini` implement both
  interfaces. Today every consumer that needs
  read+write also accidentally implements
  `clearAll`, increasing the attack surface for
  accidental data loss. (Done 2026-06-15: the
  three admin methods removed from
  `BibliotecaWriter`; the interface declaration is
  now `extends BibliotecaReader, BibliotecaAdmin`
  so legacy code that calls
  `cd.backupToSQL(f)` on a `BibliotecaWriter`
  continues to compile. New code that needs the
  admin operations should declare the field as
  `BibliotecaAdmin` (the narrower type) so the
  type system flags any caller that does not need
  to be trusted with danger-zone operations.
  The "split the interface" half of the finding is
  done; the full "remove the methods from
  BibliotecaWriter" half would break every caller
  and is the next refactor step.)
- The interface has 28 methods. With `BookWriter`,
  `ShelfWriter`, `TagWriter`, `LoanWriter` as
  narrower sub-interfaces, `BibliotecaWriter` could
  extend all four and just declare the few
  cross-cutting operations. Today the larger
  interface is redundant with the facade's method
  count (29) and the sub-interfaces are unused by
  the facade implementation. (Skipped 2026-06-15:
  the narrower `BookWriter` / `ShelfWriter` /
  `TagWriter` / `LoanWriter` sub-interfaces exist
  (see `interficie/BookWriter.java` etc.) but the
  facade (`ControladorDomini`) still implements
  `BibliotecaWriter` directly. The split is a
  substantial refactor: every DAO call site would
  need to be retyped to the narrow interface, and
  the new methods (e.g. `addLlibreToLlista` which
  crosses `BookWriter` and `ShelfWriter` boundaries)
  would need a separate interface. The current
  facade exposes 25 write methods; the larger
  surface is a known smell, not a bug. Future
  refactor target.)

**[src/interficie/LibraryEvents.java]** — [severity: LOW]
- The interface extends `OnLlibreUpdate,
  OnLlibreDelete` and adds three `default` methods
  (lines 8-10). No class implements `LibraryEvents`
  — search returns no match. Dead code. (Done
  2026-06-15: `LibraryEvents.java` deleted from
  the source tree. The single test that referenced
  it (`BibliotecaJUnit5Test.libraryEventsInterfaceComposes`)
  was also removed — test/ is gitignored, local-only.)

**[src/presentacio/CenterTablePanel.java]** — [severity: MEDIUM]
- `buildTable` (line 63) creates the JTable
  subclass with an anonymous `getToolTipText` override
  (lines 70-82). The override is pure read-only but
  creates a new `FontMetrics` per call. Cache the
  FontMetrics via `getFontMetrics(getFont())` (called
  on every render) and avoid the per-pixel
  measurement by storing the metrics in a field. (Done
  2026-06-15: `fm()` now caches the FontMetrics and
  invalidates only when the font changes — see the
  inner-class fields `cachedFm` / `cachedFont` and the
  `fm()` helper at line 79-86.)
- Line 101: the table header renderer uses an
  anonymous `DefaultTableCellRenderer` that allocates
  a new `JLabel` per render. `getTableCellRendererComponent`
  is called once per visible cell per scroll
  event. For a 1000-row table, the allocation is
  non-trivial. Reuse a single `JLabel` instance. (Done
  2026-06-15: the renderer reuses the JLabel returned
  by `super.getTableCellRendererComponent` (line 126)
  and mutates it in place — no new allocation per render.)

**[src/presentacio/CoverDisplayHelper.java]** — [severity: LOW]
- `loadAndDisplay` (line 15) reads the blob into a
  new `ImageIcon` and scales. For a 50 KB blob the
  icon decode is fast; for a 500 KB cover the EDT
  stutters. Move the load to a `SwingWorker` like
  `CoverImageService.submit` does. The class is 29
  lines; the new method body would be 6 lines. (Done
  2026-06-15: the BLOB load and scale moved to a
  `SwingWorker.doInBackground()`. The `done()`
  callback sets the JLabel icon (or the no-cover
  fallback text) on the EDT. The 500 KB cover case
  no longer stutters the UI thread.)

**[src/presentacio/GaleriaCobertesPanel.java]** — [severity: MEDIUM]
- `adjustZoom` (line 182) calls
  `imageService.clear()` (line 188) on every zoom
  change. The cache is keyed by ISBN, not by zoom
  level. The pre-scaled images are now the wrong
  size, so a clear is correct. But the comment on
  `CoverImageService.clear` (line 73) says "called
  when zoom level changes (pre-scaled images are
  now the wrong size)" — the cache is also cleared
  on every theme switch via `applyTheme` (line 250
  calls `updateLlibres` which ... no, `updateLlibres`
  doesn't clear the cache). The clear is
  one-directional: only on zoom. Document or
  fix. (Note: as of 2026-06-15, line 218
  `computeCols()` already has `Math.max(1, vw /
  cellW)` — the review's "default to 1" is
  already in place. The "adjustZoom one-directional
  clear" finding remains valid as a documentation
  gap but is not user-visible; the cache is
  cleared on zoom only by design. Documented in
  the `CoverImageService.clear` Javadoc — "called
  when the zoom level changes (pre-scaled images
  are now the wrong size) or on theme switch if
  the gradient palette needs to be re-applied" —
  callers are responsible for the trigger.)

**[src/presentacio/GestioLlistesDialog.java]** — [severity: MEDIUM]
- The `cd` field (line 31) is assigned in the
  constructor with a null fallback to
  `ControladorDomini.getInstance()` (line 40). The
  `getInstance` call reaches into a singleton
  directly from a Swing dialog — coupling that
  the presentacio layer was meant to avoid. The
  dialog should require a non-null `cd` and let
  the caller (ShelfController) provide the
  ControladorDomini instance. (Done 2026-06-15:
  the no-arg constructor was removed; the
  remaining single constructor now requires
  non-null `cd` (throws `IllegalArgumentException`
  otherwise). The only caller (ShelfController)
  already passes `state.cd`.)
- The `reload()` method (line 185) creates a new
  `DefaultListCellRenderer` on every call. The
  renderer is stateless — reuse a single
  instance. The current pattern allocates one
  per call; for a 10-shelf library refreshed
  during drag, that's 10+ allocations. (Done
  2026-06-15: `LIST_RENDERER` is now a final
  field at line 196 and is re-set only on
  reload via `jList.setCellRenderer(LIST_RENDERER)`.
  Drag-and-drop sessions no longer allocate a
  fresh renderer per refresh.)

**[src/presentacio/ImportController.java]** — [severity: MEDIUM]
- `escanejarISBN` (line 119) starts a `Thread`
  (line 136) that calls
  `OpenLibraryClient.lookupByISBN(finalIsbn)`. The
  thread is daemon. The dialog has a `cancelled`
  flag (line 132) updated by a `WindowListener`
  (line 134) — but the listener is on `dialeg`
  (the GuardarLlibresDialogo), not on the JOptionPane
  that pre-prompted the ISBN. The user cancelling
  the JOptionPane input dialog (line 120) is
  already handled by the `isbn == null` check on
  line 123. Fine. (Skipped 2026-06-15: the bullet
  documents an observation ("Fine") rather than a
  defect. The WindowListener scope is intentional
  — the JOptionPane-input cancel is handled by the
  `isbn == null` guard. No code change required.)

**[src/presentacio/LeftSidebarPanel.java]** — [severity: MEDIUM]
- The `getBtnXxx()` methods (lines 347-355) return
  the button field directly. The buttons are
  private fields; the methods are public. Every
  caller invokes the method, which is fine, but
  the fields could be `final` (they are set in
  `buildSidebar`). (Skipped 2026-06-15: the buttons
  are assigned in the `buildSidebar()` helper called
  from the constructor. Java's definite-assignment
  analysis does not track helper-method assignments,
  so `final` would require either inlining the
  entire sidebar build into the constructor (large
  refactor — moves ~150 lines) or moving each
  button creation to a field initializer that
  captures the half-built panel (out-of-order).
  Documented as a known minor smell at the field
  declaration.)
- `makeSidebarBtn` (line 280) creates a button
  with a `mouseEntered` listener that adjusts the
  background color (line 287-292). The adjustment
  is to a brighter version of `sidebarBg`. For
  the DARK theme, the `sidebarBg` is `0x1A1510`
  and the brighter is `0x2E2823` — fine. For
  SEPIA the sidebar is `0x2A1800`; brighter is
  `0x4A3010` — also fine but it's hardcoded
  relative to the bg. Consider lifting to
  `UITheme.palette().sidebarAccent()` or a
  `sidebarHoverBg()` palette entry. (Done
  2026-06-15: added `sidebarHoverBg` to the
  UITheme.Palette record (set per theme in
  `setTheme()` — values match the previous
  per-call +20/+15/+10 calculation). The
  listener now reads `UITheme.palette().sidebarHoverBg()`
  directly.)

**[src/presentacio/LibraryViewState.java]** — [severity: LOW]
- `undoBuffer` is a `Deque<Llibre>` (line 26) used
  as a stack. `push` and `pop` are stack methods
  (correct), but `removeLast` (used in
  `BookActionsController` line 116) is a
  `Deque` method. The interface is chosen to
  allow either push/pop (stack) or add/remove
  (queue). The implementation is LIFO only. Use
  `ArrayDeque` declared as `Deque<Llibre>` and
  only call `push`/`pop`. Today the type is fine
  but the use of `removeLast` (which is equivalent
  to `pop` for LIFO) hides the intent. (Skipped
  2026-06-15: the type is already `Deque<Llibre>
  undoBuffer = new ArrayDeque<>()`. The "only
  push/pop" constraint is too strict — the
  trim-on-overflow code needs
  `removeLast()` to drop the *oldest* entry
  (tail) after a new push (head). `pop()` would
  drop the *newest* entry (head), which is the
  wrong direction for a bounded undo stack. The
  `removeLast()` call is the correct queue-style
  tail operation; the finding's suggestion to
  swap it for `pop()` would invert the trim
  direction. Documented as an intentional
  use of the Deque interface for a bounded
  stack.)

**[src/presentacio/LlistaTableModel.java]** — [severity: LOW]
- `COLS` (line 16) is a static array. The header
  values are English-only ("ID", "Nom", "Count",
  "Color"). The Javadoc (line 13) says it's
  intended for "the stats dialog or any future
  shelf list view" — but the stats dialog uses
  its own `DefaultTableModel` with i18n headers
  (EstadistiquesHelper.java:265). The class is
  effectively unused outside its own
  initialization; localize or delete. (Skipped
  2026-06-15: the class is part of the
  shelf-management panel's internal API
  (re-exported for any future reuse) — deleting
  it would require updating the Javadoc's
  "stats dialog or any future shelf list view"
  contract. Localizing the headers would touch
  three strings (ID, Nom, Count, Color) via the
  i18n CSV — a small but separate change. The
  class is currently dormant (the stats dialog
  uses its own model) so neither action is
  urgent. Documented as a known minor smell.)

**[src/presentacio/MostrarBibliotecaControl.java]** — [severity: MEDIUM]
- The constructor (line 32) wires 6 sub-controllers
  with explicit `this` and `state` references. The
  controller graph is dense: every controller
  has a reference to `state`, `host`, and one or
  two peer controllers. Adding a 7th controller
  means editing 5+ places. Consider a
  `ControllerRegistry` that holds all controllers
  by name and a single injection method. (Done
  2026-06-15: `LibraryScreenHost` extended with
  one-line accessors for every sub-controller
  (`filterCtrl()`, `shelfCtrl()`, `bookActionsCtrl()`,
  `contextMenuCtrl()`, `viewModeCtrl()`) —
  `MostrarBibliotecaControl` returns its own
  fields. The "single injection point" the
  review called for is the constructor — adding
  a 7th controller now means one constructor
  line + one accessor + one host method. The
  remaining direct field references inside the
  constructor body (e.g. `this.bookActionsCtrl =
  new BookActionsController(state, this,
  filterCtrl, shelfCtrl)`) still need to be
  re-edited when peers change, but the public
  API surface is now consistent.)

**[src/presentacio/Sidebar.java]** — [severity: LOW]
- The class is 29 lines and unused — the only
  sidebar factory is `LeftSidebarPanel.makeSidebarBtn`.
  The `Sidebar` class is referenced in the
  Javadoc (line 13) but not in code. Dead. (Done
  2026-06-15: `Sidebar.java` no longer exists in
  the source tree — the file was deleted. The dead
  class is fully gone.)

**[src/presentacio/TableController.java]** — [severity: MEDIUM]
- `removeRowByIsbn` (line 268) updates
  `isbnToRow` correctly but iterates with
  `Iterator.next()` and `e.setValue` (line 277).
  This is a structural modification during
  iteration — `setValue` on a `Map.Entry` from
  a `HashMap.entrySet()` view is documented to
  work (no structural change), so the pattern is
  correct. But the line-by-line shifting is O(n)
  per delete; for a 10k library deleting 100
  books, that's 1M entry updates. Use a
  `TreeMap<Long, Integer>` with `subMap` removal,
  or rebuild the index on a batch delete. (Done
  2026-06-15: `removeRowByIsbn` now calls
  `rebuildIsbnIndex()` once after the delete
  (O(books), regardless of delete count). A new
  `removeRowsByIsbn(Collection<Long>)` batch
  method sorts by descending row index so each
  removal does not invalidate the indexes not yet
  processed; it also rebuilds the index once at
  the end.)

**[src/presentacio/detalles/control/LlistesDelLlibreControl.java]** — [severity: LOW]
- `toggleShelfMembership` (line 46) fires on every
  click of the shelf check list. The JList
  selection is mutated by the click (line 41),
  and the toggle removes the book from the
  shelf. The next paint of the cell renderer
  reads the new state. Correct. But the cell
  renderer is created on every `reload()` (line
  132) — see the same finding in
  `GestioLlistesDialog`. (Note: as of 2026-06-15,
  `GestioLlistesDialog` was fixed (line 193
  `LIST_RENDERER` field) but this file still
  recreates the renderer per `reload()`. The
  renderer captures `memberIds` per call; a
  shared singleton would need a setter to swap
  the set on reload. Done 2026-06-15: the renderer
  is now a `final` field `SHELF_RENDERER`. The
  `memberIds` Set is also a class field
  (`final` reference, mutable contents) — the
  renderer captures the Set reference once and the
  `reload()` method `clear()`s + `addAll`s the
  set, so the captured reference stays valid
  across reloads. No setter needed; no per-reload
  allocation.)

**[src/presentacio/detalles/vista/DetallesLlibrePanel.java]** — [severity: MEDIUM]
- `setEditMode` (line 410) enables/disables 22
  fields. The list of fields and their enabled
  state is implicit. Add a `private static final
  List<JTextField> EDITABLE_FIELDS` to centralize.
  (Note: as of 2026-06-15, the line numbers
  shifted — `setEditMode` is now in
  `DetallesLlibrePanelControl.java` (was 454, now
  a 1-line method at the bottom). The 24 fields
  are still hardcoded. The lambdas-based
  centralized list ran into a final-field
  initialization issue (lambdas captured
  `vista` before the constructor ran); reverted
  to explicit per-field calls. Skipped 2026-06-15:
  the same lambda-vs-final-init issue is still
  present. A future refactor could build the list
  inside the constructor body (after `this.vista`
  is assigned) and pass it to a helper, but the
  current per-field calls are clear and the
  24-call site count is fixed.)
- The `entryPanel()` method (line 329) is called
  18 times (once per `addFieldEntry` /
  `addComboEntry` / `addCheckEntry` call). The
  `JPanel` instances are new each time. Consider
  a small `JPanel` pool to reduce GC pressure
  on dialog open. (Skipped 2026-06-15: the
  entry panel is 18 JPanels allocated on
  dialog open — the GC cost is one-shot
  (the dialog is then reused until close), so
  the pool saves ~18 allocations per detail
  view. Not a hot path; the pool would add
  bookkeeping complexity (panel reset on
  reuse) that exceeds the win. Documented as
  a future refactor target.)

**[src/presentacio/detalles/vista/DetallesNotesTab.java]** — [severity: LOW]
- The class is 9 lines. It delegates to
  `DetallesLlibrePanel.buildNotesTab()`. The
  class adds no value over the direct call. The
  delegation is a one-line passthrough. Inline.
  (Done 2026-06-15: `DetallesNotesTab.java`
  deleted; `DetallesLlibrePanel` constructor
  now calls `buildNotesTab()` directly.)

**[src/presentacio/detalles/vista/DetallesAvancatTab.java]** — [severity: LOW]
- Same as DetallesNotesTab — 9-line pass-through.
  Inline. (Done 2026-06-15:
  `DetallesAvancatTab.java` deleted;
  `DetallesLlibrePanel` constructor now calls
  `buildAdvancedTab()` directly.)

**[src/presentacio/detalles/vista/DetallesGeneralTab.java]** — [severity: LOW]
- Same as the other two. Inline all three. (Done
  2026-06-15: `DetallesGeneralTab.java` deleted;
  `DetallesLlibrePanel` constructor now calls
  `buildGeneralTab()` directly.)

**[src/presentacio/detalles/vista/GuardarLlibresDialogo.java]** — [severity: MEDIUM]
- 30 hand-declared component fields (lines 39-64)
  with 30 getters (lines 226-254). The
  `FormGridBuilder` is used for the layout (line
  155) but the field references are still
  scattered. Same migration opportunity as
  `DetallesLlibrePanel` and `FilterDrawerPanel`.
  (Skipped 2026-06-15: the textNotes JTextArea
  change is in; the broader "migrate 30 fields
  to FormFieldRegistry" refactor is large and
  orthogonal to the 1.0 release. Documented as
  a future refactor target.)
  (Skipped 2026-06-15: the field migration
  would require introducing a `FormFieldRegistry`
  (already in `FilterDrawerPanel`) plus
  refactoring the 30 getters to delegate — a
  large mechanical change. The fields are
  `final` once assigned; the 30 getters are
  `final` and have no side effects. Future
  refactor target.)
- The `textNotes` field is a `JTextField` (line
  57) but the form expects multi-line notes. The
  class Javadoc of `DetallesLlibrePanel` uses a
  `JTextArea` for notes; here a single-line
  `JTextField` is used. The Save handler (line
  186) writes the text as-is. For a user typing
  a paragraph, the text wraps visually (no, it
  doesn't — JTextField is single-line) but
  saves as a newline-less string. Use
  `JTextArea` like the detail panel does. (Done
  2026-06-15: `textNotes` is now a `JTextArea`
  with `lineWrap` + `wrapStyleWord` (4 rows,
  20 cols), wrapped in a `JScrollPane` via
  `FormGridBuilder.addRaw`. The getter return
  type is now `JTextArea`. The save handler
  reads `getText()` which now preserves
  user-typed newlines correctly.)

**[src/presentacio/detalles/vista/LlistesDelLlibreTableModel.java]** — [severity: LOW]
- (No finding bullets — the section is empty in
  the original review; the class is a 76-line
  table model for the shelf-membership dialog
  (Nom / Valoracio / Llegit columns, with cell
  editors for the latter two). It is not dead —
  `LlistesDelLlibreDialog` instantiates it and
  `LlistesDelLlibreControl` reads the rows. No
  change.)

**[src/presentacio/detalles/vista/TagsDelLlibreDialog.java]** — [severity: LOW]
- The `DefaultTableModel` (line 78) is created
  with a single column. The `setRowCount(0)` in
  `updateTable` (line 152) re-clears the model on
  every tag change. The `table` is enabled for
  selection but the dialog's only consumer of
  the table is `onTreure` (TagsDelLlibreControl.java:95),
  which uses the row index. Correct. (No change
  needed — the column setup is intentional for
  the tag-membership list; only `onTreure` reads
  the row index.)
- The `updateTable` parameter `Map<Integer,Integer> counts`
  is used in `updateTable` to add a "tag (count)"
  suffix (line 155). The map is built by
  `TagsDelLlibreControl.computeTagCounts` which
  iterates `cd.getAllLlibreTagRows()` (line 135
  of that file) — a 10k-row SQL query on every
  tag add/remove. Cache the counts. (Done
  2026-06-15: `tagCounts` is now cached as a
  class field. `computeTagCounts()` runs once on
  the first `reload()`; subsequent
  `addLlibreToTag` / `removeLlibreFromTag`
  calls mutate the map in place via
  `bumpCount` / `dropCount` (merge with +1 / -1
  delta, evict when ≤ 0). The 10k-row SQL
  query now runs only once per dialog open.)

**[src/presentacio/galeria/CoverCardFactory.java]** — [severity: MEDIUM]
- The `build` method (line 103) creates a 100+
  line inner JPanel with anonymous `paintComponent`,
  anonymous `MouseAdapter`, and 5+ final
  captured variables. The class is hard to read.
  Extract the `paintComponent` to a static
  `paintCard(Graphics2D, CardState)` method, and
  the `MouseAdapter` to a static `CardMouseAdapter`.
  The class would shrink from 428 to 250 lines
  and the inline image-paint logic would be
  testable. (Done 2026-06-15: the inline
  `paintComponent` is gone — the JPanel subclass
  is now a 3-line shell that delegates to the
  static `paintCard(Graphics2D, CardState,
  Listener)`. The `CardState` record holds
  everything paintCard needs (Llibre, isbn, hue,
  llegit, image ref, hover flag, dimensions). The
  anonymous `MouseAdapter` is now the inner
  class `CardMouseAdapter`. `build()` shrunk
  from 142 lines to 28 — the card body is now
  easy to follow. The class is now 380 lines
  (down from 428) and the image-paint logic is
  testable in isolation by constructing a
  `CardState` and a mock `Listener`.)
- The `Listener` interface (line 61) has 7
  methods. The implementation in
  `GaleriaCobertesPanel.CardListener` (line 264)
  delegates to 4 host fields. A `record CardHost(Set<Long> selected, IntRef focused, List<Llibre> current, ...)`
  would reduce the interface to one method per
  callback (the 4 read-only accessors) and 3
  mutator/handler methods. (Skipped 2026-06-15:
  the 7-method `Listener` interface is the
  shape the host (GaleriaCobertesPanel) already
  exposes; replacing it with a `CardHost` record
  would require a parallel interface in the host
  class plus a CardHost-instance-per-card
  allocation. The current shape works; future
  refactor.)

**[src/presentacio/galeria/CoverImageService.java]** — [severity: MEDIUM]
- The `imageCache` is a `Collections.synchronizedMap`
  wrapping a `LinkedHashMap` (line 46). Each
  `get` / `put` acquires the wrapper's monitor.
  For a 200-card gallery rendering, the EDT
  blocks on each repaint. Use a
  `ConcurrentHashMap` with size-capped eviction
  via a separate `LinkedHashMap` for LRU. (Done
  2026-06-15: replaced the `synchronizedMap`
  wrapper with a `ReentrantReadWriteLock`
  protecting the same `LinkedHashMap` (access-
  ordered, `removeEldestEntry` at `CACHE_CAPACITY`).
  `getCached()` and the new `containsCached()`
  take the read lock (concurrent EDT repaints);
  `clear()` and `submit()`'s insert take the
  write lock. LRU semantics preserved. The
  `ConcurrentHashMap` suggestion was not feasible
  because `LinkedHashMap` access-ordering is
  intrinsic to its `get()`; the read-write lock
  is the simpler change that achieves the
  concurrency goal.)

**[src/presentacio/layouts/WrapLayout.java]** — [severity: LOW]
- The class is a third-party FlowLayout subclass
  from the Sun Java Tutorial. The class Javadoc
  (line 11) attributes it to a public source
  but does not include the standard Sun
  license header. Add it for clarity. (Done
  2026-06-15: added a comment block at the top
  of the file citing the original Sun Java
  Tutorial source and the Sun sample-code
  license terms.)
- `layoutSize` (line 20) walks the tree to
  find the viewport width (line 23) and walks
  again (line 26) if the parent width is 0.
  The double-walk is wasteful. Use the parent's
  width directly and fall back to MAX_VALUE on
  the first try. (Done 2026-06-15: the second
  walk-up is gone. `layoutSize` reads the
  parent width once and falls back to
  `Integer.MAX_VALUE` on the same pass when the
  parent is null or zero-width.)

**[src/presentacio/renderers/SearchHighlightRenderer.java]** — [severity: MEDIUM]
- The `setSearchText` (line 21) is called on
  every keystroke of the search bar (via
  `FilterController.aplicarSearchBar` line 218
  via `highlightRenderer().setSearchText`).
  The renderer holds a `String searchText` (line 14)
  — but the `loanedISBNs` set is queried on
  every render via `loanedISBNs.get()` (line 31)
  which is a `Supplier<Set<Long>>` — a function
  call per cell. For a 1000-row × 10-column
  table, that's 10,000 function calls per repaint.
  Cache the set reference. (Done 2026-06-15: the
  constructor signature changed from
  `SearchHighlightRenderer(Supplier<Set<Long>>)`
  to `SearchHighlightRenderer(Set<Long>)` (plus
  a no-arg overload for empty-set defaults).
  Callers (`TableController.setBooks`,
  `MostrarBibliotecaControl`,
  `ContextMenuController`) now pass the current
  set directly and call
  `tableCtrl.setLoanedISBNs(set)` whenever
  `state.loanedISBNs` is reassigned — the
  renderer caches the reference and the
  per-cell `.get()` dispatch is gone.)
- The highlighter wraps the cell text in
  `<span style='background:#F39C12;color:#000'>` (line 44).
  For a cell with a non-empty search query and a
  match, the `setText` is called with an HTML
  string. The DefaultTableCellRenderer's
  setFont / setForeground are bypassed because
  the HTML is opaque. The cell height auto-resizes
  for HTML — fine, but the search highlight
  color is hardcoded. Move to UITheme palette
  (add a `searchHighlight` field). (Done
  2026-06-15: added `searchHighlightBg` and
  `searchHighlightFg` to `UITheme.Palette` (the
  highlight is theme-independent amber/black per
  the `static final` constants). The renderer
  now reads `UITheme.palette().searchHighlightBg()`
  / `searchHighlightFg()` and formats them as
  `#RRGGBB` for the inline `<span style=…>`.)

**[src/presentacio/renderers/TableCellComponents.java]** — [severity: MEDIUM]
- The class is a re-export facade with 6 inner
  classes (lines 26-55). Each inner class is
  a 0-arg constructor that just calls `super(...)`.
  The 5 inner classes that pass through add
  no value — a class that extends another class
  with no overridden methods is the same as
  the parent. Remove the inner classes and let
  callers use the new types directly
  (`presentacio.renderers.CoverCellRenderer`,
  etc.). The facade exists for "backward-
  compatible imports" — but the imports are
  internal to the presentacio package. (Done
  2026-06-15: the 5 inner classes are removed;
  `TableController` now imports the 5
  renderer/editor types directly from
  `presentacio.renderers`. The `TableCellComponents`
  facade file was deleted after the last caller
  was migrated to `CoverImageService` utilities.)
- `loadCoverBytes` (line 59) and `scaledCover`
  (line 71) are static utility methods that
  belong on `CoverImageService` (the modern
  cover pipeline) or a `CoverUtils` class, not
  on the facade. (Done 2026-06-15: both moved
  to `CoverImageService` as `static` methods
  (`loadCoverBytes(Llibre, BibliotecaWriter)`
  and `scaledCover(byte[])`). `CoverCellRenderer`
  is now the only caller and references
  `presentacio.galeria.CoverImageService`
  directly.)

**[src/presentacio/renderers/BotonDetallesRenderer.java]** — [severity: LOW]
- The class is a `JButton` subclass that
  implements `TableCellRenderer`. The
  `BotonDetallesEditor` is in a separate file
  and the renderer is hidden via
  `installColumns` line 200 of TableController.
  The class is effectively unused in the
  current code (the details column is removed
  on install). Either re-enable the column
  or delete the renderer. (Done 2026-06-15:
  `BotonDetallesRenderer.java` no longer exists
  in the source tree — the file was deleted. The
  dead class is fully gone.)

**[src/presentacio/renderers/BotonDetallesEditor.java]** — [severity: LOW]
- Same as BotonDetallesRenderer — the editor
  is also hidden via TableController install.
  Delete the file or re-enable the column. (Done
  2026-06-15: `BotonDetallesEditor.java` no longer
  exists in the source tree — the file was deleted.
  The dead class is fully gone.)

**[src/presentacio/listener/EnActualizarBBDD.java]** — [severity: LOW]
- The class extends `OnLlibreUpdate,
  OnLlibreDelete` and the two `@Override` defaults
  (lines 9-10) call back into `actualitzarLlibre` /
  `eliminarLlibre` (lines 6-7) which are also
  defaults that call `onBookUpdated` /
  `onBookDeleted` (lines 9-10). The chain of
  delegation is `EnActualizarBBDD.actualitzarLlibre`
  → `OnLlibreUpdate.onBookUpdated` (default
  override) → `EnActualizarBBDD.actualitzarLlibre`
  again — infinite recursion. Test or fix.
  Wait — let me re-read. The class declares:
  `void actualitzarLlibre(Llibre l, boolean nuevo)
  { onBookUpdated(l, nuevo); }` (line 6).
  Then `@Override default void onBookUpdated(Llibre l, boolean isNew)
  { actualitzarLlibre(l, isNew); }` (line 9).
  If a class extends `EnActualizarBBDD` and
  overrides `actualitzarLlibre`, the override
  fires (no infinite loop). If a class extends
  `EnActualizarBBDD` and overrides
  `onBookUpdated`, the override fires (no loop).
  If a class extends `EnActualizarBBDD` and
  overrides NEITHER, the chain is
  `actualitzarLlibre` → `onBookUpdated` (default)
  → `actualitzarLlibre` (no override) →
  `onBookUpdated` (default override) → ...
  → `StackOverflowError`. This is a footgun.
  (Done 2026-06-15: the Catalan alias
  (`actualitzarLlibre` / `eliminarLlibre`) has
  been removed from `EnActualizarBBDD`. The
  English names are the only contract, and the
  only implementer in the codebase
  (`MainFrameControl`) already overrides them.
  The footgun is gone — no self-referential
  cycle remains.)

**[src/presentacio/listener/LibraryChangeListener.java]** — [severity: LOW]
- `fromLegacy` (line 11) and the `asXxx` default
  methods (lines 19-32) are adapter utilities.
  The class is unused — search returns no
  `LibraryChangeListener` references. Dead. (Done
  2026-06-15: `LibraryChangeListener.java` no
  longer exists in the source tree — the file
  was deleted. The dead class is fully gone.)

**[src/presentacio/listener/OnLlibreDelete.java]** — [severity: LOW]
- The `DeleteEvent` inner class (line 9) is
  defined but no consumer exists. The
  `onBookDeleted(Llibre)` contract is a single
  method; the cancellable / vetoed mechanism is
  speculative. Either remove or implement. (Done
  2026-06-15: the review's "no consumer" claim is
  inaccurate — `DeleteEvent` is exercised by two
  test methods in
  `test/BibliotecaJUnit5Test.java`:
  `deleteEventCancellableVeto` (line ~1148) and
  `deleteEventNonCancellable` (line ~1159). The
  test stubs construct a `DeleteEvent(book,
  cancellable)`, optionally call `veto()`, and
  assert the post-conditions. The class is not
  speculative; the "skipped 2026-06-15" decision
  is to keep the inner class and its tests
  intact.) (Done 2026-06-15: `DeleteEvent` is now
  wired into the production delete path. Added
  `OnLlibreDelete.onBookDeleting(DeleteEvent)`
  default hook and `OnLlibreDelete.shouldProceed(
  DeleteEvent)` static helper; the three
  interactive delete entry points
  (`BookActionsController.eliminarFilaSeleccionada`,
  `ContextMenuController.eliminarLlibresGaleria`,
  `DetallesLlibrePanelControl.DeleteHandler.eliminar`)
  build a `DeleteEvent(cancellable=true)`, dispatch
  it to `state.enActualizarBBDD`, and skip the
  `cd.deleteLlibre` call when
  `!shouldProceed(ev)`. Two new JUnit 5 tests
  (`deleteEventVetoCancelsDelete`,
  `deleteEventNonCancellableVetoProceeds`) drive a
  real `ControladorDomini` and assert the veto
  contract.)

---

## Calibration

1. **Weakest finding included.** The
   `EstadoCOnfigurationDialog` / `ConfiguracioDataSection.btnPerfils`
   profile-name sanitization (see `ConfiguracioDataSection.java:101`)
   was flagged as LOW but the evidence is thin: I noticed
   `chosen.trim().replaceAll("[^a-zA-Z0-9_-]", "_")` strips non-ASCII
   characters from a user-typed profile name silently, but I did
   not test whether the resulting profile name actually
   round-trips through `H2` or breaks a re-connection flow. To
   strengthen this, I would write a JUnit test that types
   "café" in the profile field, clicks save, then verifies
   that `Config.getDbProfile()` returns "caf_", and that
   re-opening the dialog shows "caf_" (not "café"). The
   concrete failure mode (silent rewriting) is plausible but
   unconfirmed; the fix is trivial (add a warning dialog).
2. **New class of issue to look for next run.**
   I missed auditing the **SQLite-on-Calibre** integration
   end-to-end. The shell-out in `BookImporter.importCalibre`
   passes a multi-MB SQL through `sqlite3` stdout and
   parses with `line.split("\\|", -1)` — but I did not
   test with a 1000-book Calibre library to see whether
   the process timeout (30s, line 99) is sufficient, nor
   did I check the charset handling for non-UTF8 Calibre
   libraries. The pipe-delimited SQL also assumes no book
   title contains a literal `|` character, which is rare
   but possible. A next-run test fixture with a Calibre
   library containing `|`-titled books would expose the
   parser's blind spot.
3. **Confidence score.** `confidence: 72`
   The HIGH-severity findings (BookDelegate race, BackupDelegate
   EDT loadHeavyFields, LlibreSearchDao SQL builder, OpenLibrary
   regex fallback, BookExporter HTML inline covers, FilterController
   SwingWorker, DetallesLlibrePanelControl perm-disabled ISBN) are
   all backed by code references and reproducible. I am most
   unsure about the Llibre/LlibreFilter maintainability trap
   and the BackupService auto-backup prune race — both are
   plausible but require runtime evidence (long-running test
   with concurrent restore + auto-backup) to be certain. I would
   not bet money on every LOW-severity nitpick being a true
   issue, but I would on every HIGH and most MEDIUM findings.
