================================================================================
  CODE REVIEW (tot.txt) — Biblioteca
  Generated: 2026-06-10
  Updated:       2026-06-11 (HIGH findings addressed; remaining items below)
  Reviewer scope: every source file in src/ + checkBiblio/.
  Severity cap: 3 findings per file, ranked. No per-file summaries.
  Reviewer policy: drop style-only nits; raise CRITICAL/HIGH/MEDIUM.
================================================================================

All HIGH-severity findings have been implemented (see git log). This file
now contains only the remaining MEDIUM and LOW items to address.

================================================================================
  domini/
================================================================================

**[src/domini/Llibre.java]** — severity: LOW
- [Lines 116-120] The three `setNomCa/Es/En` setters each call `v.trim()` on a non-null, non-blank input but the `trim` is duplicated three times — and the same `trim` pattern is repeated across `setDataCompra`, `setDataLectura`, `setIdioma`, `setFormat`, `setPaisOrigen`, `setEstat`, `setLlenguaOriginal` (lines 225-241). A single `private static String nullIfBlank(String)` would collapse 8 duplicate null/blank/trim patterns. Minor.
- [Lines 263-274] `toString()` outputs both `autor` and `autors`. For a builder-built book where `addAutorNom` was called twice, the output is `"Llibre{... autor=Doe, Smith, autors=[Doe, Smith]}"` — the duplicated info is exactly the corruption noted in finding #1. Document or drop one of the two fields from the toString.

**[src/domini/LlibreFilter.java]** — severity: MEDIUM
- [Line 24] `public static LlibreFilter empty() { return new LlibreFilter(); }` — but the constructor is `private`. The factory exists only because the ctor is private. Either make the no-arg constructor `public` and drop the factory, or make the factory `package-private` so the facade doesn't depend on it. Right now both `LlibreFilterBuilder.of()` (LlibreFilterBuilder.java:34) and `BookDelegate.getLlibresPage` (LlibreSearchDao entry point) use the factory — making the ctor `public` is one edit that removes 5 callsites of the factory.
- [Lines 62-74] `copy()` is a 13-line re-emit of every field. Same `record LlibreFilter(...)` refactor that the prior review suggested: `copy()` becomes `new LlibreFilter(...)` (canonical ctor with all 17 args), and the field-by-field copy is gone.
- [Line 60] `withSort(SortSpec sort) { this.sort = sort; return this; }` — when `sort` is null, the field is nulled, breaking the `sort = SortSpec.defaultAsc()` invariant (line 20). The defensive null check in `LlibreSearchDao.search` line 50-51 then re-applies the default. Either the field is non-null by contract and the setter should throw on null, or the default is reapplied here. Today: the field is `final` only in spirit, and the null case silently creates a partially-defaulted filter.

**[src/domini/SortSpec.java]** — severity: MEDIUM
- [Lines 27-30] Silent column coercion confirmed. New evidence: `BookDelegate.SORT_BY` (BookDelegate.java:51-57) is a separate map from `SQL_COLS` (this file line 16-22). Adding a new column requires editing BOTH maps and the SQL fragment in `toSql()`. The `SortSpec` "fix" must be coordinated across the two files. Throw `IllegalArgumentException` in the ctor (the field is final, this is the only place to catch typos) AND make `BookDelegate.SORT_BY` derive from a `Map<String, Comparator>` built by the `SortSpec` (sort logic lives in the spec, not in two places).
- [Line 16] `SQL_COLS` is `package-private`. The other `Map<String, Comparator>` (`SORT_BY` in BookDelegate) is also `package-private`. No external API consumer of `SortSpec` is documented; the only consumer is `BookDelegate.SORT_BY`. So `SQL_COLS` and the comparator map should be in the same class, not split between `domini/` and `domini/facade/`.

**[src/domini/LlibreCore.java]** — severity: MEDIUM
- [Lines 29-36] The `from(Llibre)` projection calls `l.getAutors()` (Llibre.java:143) which **returns a defensive `new ArrayList<>(autors)`** every time. So a single `LlibreCore.from(l)` allocates 1 ArrayList of 0-2 strings. For 5000 books projected in bulk (e.g. an `extractCores(List<Llibre>)` factory), that's 5000 redundant allocations. Add an `extractCores(List<Llibre>)` that allocates one list per call. Also: the Javadoc says "Future refactor: Llibre composes a LlibreCore + extension state" but the refactor isn't started; the Llibre still has the 16 fields inline. Delete the file or commit to the refactor.
- [Line 7-8] "Free-text fields (notes) live on the mutable Llibre" — but `LlibreCore` also excludes `pagines`, `paginesLlegides`, `volum`, `exemplars`, `dataCompra`, `dataLectura`, `estat`, `desitjat`, `nomCa`, `nomEs`, `nomEn`, `hasBlob`, `imatgeBlob` — far more than just "free-text". The Javadoc oversells the class as a "core" when it's actually an arbitrary 16-field subset.

**[src/domini/Tag.java]** — severity: LOW
- [Lines 36-41] `equals` compares only by `id` and the Javadoc at lines 34-35 explains the cross-db concern. But for an `id=0` transient (a `Tag` created in-memory before persistence), two `Tag(0, "foo")` are equal even if they represent different in-progress creates. The fix is a `this == o` identity check at line 38 (already present) AND a "stable id" check: an `id=0` Tag is never equal to another `id=0` Tag. The Javadoc currently says "different DB instances" — but a transient `id=0` is the same "DB instance" (none).
- [Lines 18-21] `setNom` throws `IllegalArgumentException`. The `Llista.setNom` and the `LlibreValidator` (LlibreValidator.java:71-72) throw the same type. Standardize on `BibliotecaException.Validation` (BibliotecaException.java:32-33) and update `DialogoError` (which already handles `IllegalArgumentException` at line 26-27 to map to the validation dialog). One source of truth.

**[src/domini/Mappable.java]** — severity: LOW
- [Lines 5-8] Marker interface with one method (`toMap`). Only `Llista` and `Tag` implement it, and the two implementations use different field sets. No generic helper consumes the interface. The interface adds zero polymorphism — callers use `instanceof Mappable` at the call site, which is exactly what an interface avoids. Delete the interface; the `toMap` methods stay on the concrete classes.

**[src/domini/facade/BookDelegate.java]** — severity: MEDIUM
- [Lines 51-57] `SORT_BY` map is hand-maintained. Adding a new sortable column means editing `SortSpec` (this file line 16-22, the SQL_COLS map) AND `BookDelegate.SORT_BY` (this file line 51-57, the comparator map) AND `LlibreSearchDao.search` (which sorts by `sort.toSql()`, no change). Two files to edit in lockstep. The `SortSpec` review above proposes making `SORT_BY` derived from `SortSpec` — adopt.

**[src/domini/facade/ShelfDelegate.java]** — severity: MEDIUM
- [Lines 105-115] `setLlistaColor` linear-scans `state.llistes()` (line 111-113) while the `llistesById` map (StateContext.java:33) is right there. The fix is one line: `Llista l = state.llistesById().get(id); if (l != null) l.setColor(color);` inside the existing `withLock` block. Same as the prior review but a more specific fix.
- [Lines 95-103, 117-124, 126-146] `moveLlistaUp/Down` calls `indexOfLlista(id)` (line 117-124) which does its own `withLock`, then calls `swapLlistesOrdre(idx, ...)` which does another `withLock` (line 128). New evidence: the `synchronized` block at line 128 is `synchronized (state.lock())` while `withLock` (StateContext.java:95-97) is `synchronized (lock) { action.run(); }`. Both synchronize on the same object, so the two lock acquisitions are NOT held by the same thread → the index returned by `indexOfLlista` can be stale by the time `swapLlistesOrdre` uses it. The fix is to do both in a single `withLock` block.
- [Lines 80-83, 85-88, 90-93] `addLlibreToLlista` / `removeLlibreFromLlista` / `updateLlibreInLlista` only do the DB write — they DON'T update the in-memory `state.biblio` for the shelf membership. The `Llibre` instances in `state.bib()` carry no `llistaIds` field, so the in-memory list isn't technically wrong, but the per-shelf view (`getLlibresInLlista`) is served by a fresh DB query (line 74). For the SQL-fallback path (`aplicarFiltres` line 201), the shelf filter is part of the SQL. For the in-memory path, the shelf filter is served by `cp.getISBNsInLlista` (line 224). Both are DB round-trips, so the cache claim of "in-memory filter" is leaky. Document or refactor.

**[src/domini/facade/StatsDelegate.java]** — severity: MEDIUM
- [Lines 43-63] `getDistinctValues` switch's `default -> null` (line 51) is then checked at line 53 (`if (extractor != null)`). New evidence: the same set of 6 columns is duplicated here (in the switch) and in `TagDao.AUTOCOMPLETE_COLUMNS` (TagDao.java:22-23). Two lists to edit in lockstep. The `Set<String> IN_MEMORY_COLUMNS` from the prior review's suggestion is the right shape — both lists should be derived from a single source.
- [Lines 65-77] `getDistinctAutorNames` does a defensive `state.withLockReturning(() -> new ArrayList<>(state.bib()))` (line 67) to snapshot the in-memory list, then iterates. The snapshot is O(n) allocations. For a UI feature that runs at most once per dialog open, the cost is fine — but the method's name says "AutorNames" (plural) and the logic is correct. The issue: for a book with `getAutors()=[]` AND `getAutor()="Doe"` (the legacy path), line 72 picks the legacy `autor`. After migration 34, `llibre.autor` is gone, so `getAutor` returns the joined-from-autors string. The fallback is dead — every book's authors are in `autors`. Drop the `else if`.

**[src/domini/facade/BackupDelegate.java]** — severity: MEDIUM
- [Lines 45-61] `backupToSQL` iterates `state.bib()` outside the lock (line 46-50) and then snapshots under the lock (line 54-58). New evidence: the pre-load at line 46-50 calls `loadHeavyFields` per book OUTSIDE the lock — but `loadHeavyFields` mutates the in-memory `Llibre` (LlibreBlobDao line 47 sets `target.setHeavyFieldsLoaded(true)`). This is a mutation from a non-lock-holding caller. The `BookDelegate` (line 168-179) wraps the same `loadHeavyFields` in its own `withLock` for the read-ISBN path, but here in `BackupDelegate` the mutation is unprotected. Two callers of `loadHeavyFields` with different locking discipline.

**[src/domini/facade/TagDelegate.java]** — severity: LOW
- [Lines 34-45, 47-54] `addTag` and `deleteTag` follow DB-then-memory order. `renameTag` (line 56-63) also DB-then-memory. The "two are consistent" note from the prior review is correct. New evidence: there is NO `invalidateLlibreTagCache` call after any of these — the `TagDao.llibreTagCache` (TagDao.java:25) is invalidated only by the add/remove on `llibre_tag` (the Book ↔ Tag join). So a `deleteTag` that removes a tag from `tag` but the rows in `llibre_tag` for that tag are auto-removed by FK CASCADE (TagDao line 60-62 delete; migration 13 has `ON DELETE CASCADE` on the FK). The CASCADE works, but the in-memory `Tag` object survives in `state.tags()` (it gets removed by line 51-52). OK.
- [Lines 65-66] `getLlibresWithTag` and `getTagsForLlibre` are direct pass-throughs. Five of the seven methods on this delegate are pass-throughs. The "delegate" is a one-line wrapper. The prior review's complaint about LoanDelegate applies here too.

**[src/domini/facade/StateContext.java]** — severity: MEDIUM
- [Line 27] `private final Object lock = new Object();` — the lock is `final` (good) but there's no `ReentrantReadWriteLock` swap as the TODO at ControladorDomini.java:27-30 promises. The TODO is in the WRONG place — it should be in StateContext (the place that owns the lock), not in the facade. The TODO never gets done because no one owns it.
- [Lines 50-61] Public `bib()`, `llistes()`, `tags()`, `llistesById()`, `tagsById()` return the live mutable lists. The Javadoc at line 50-54 warns callers to hold the lock. New evidence: line 86-92 `rebuildIdIndexesLocked` is `public` and the Javadoc says "Must be called under lock". A caller that forgets the lock gets silent corruption (the maps are out of sync with the lists). Make it `private` and only call from `replaceAll` and `clearAll` (both of which already hold the lock). The "public for tests" excuse is moot — no test imports `rebuildIdIndexesLocked`.
- [Line 96-97, 101] `withLock(Runnable action)` and `withLockReturning(Supplier<T> action)` use the same `synchronized (lock)` block. New evidence: `withLock` is called by `TagDelegate.deleteTag` (line 50-53) and `addTag` (line 39-42) for the in-memory mutation, and by `BookDelegate.addLlibre` etc. for the binarySearch + add. Each `withLock` allocates the `Runnable` or `Supplier` lambda on every call. For a tight loop (e.g. 50 `addLlibre` calls in a batch import), that's 50 lambda allocations + 50 lock acquisitions. The lock duration is small (single `bib.add(pos, l)`), but the cost is still measurable. A `withLockBatch(List<Runnable>)` would batch.

**[src/domini/facade/LoanDelegate.java]** — severity: LOW
- [Line 35] `getLoanedISBNs` is called on every `PrestecDao` round-trip. The prior review's complaint stands: for 5000 books with 50 active loans, the table renderer triggers 50 round-trips per repaint. New evidence: `LoanDelegate.getLoanedISBNs` is called from `CoverCellRenderer` (via `state.loanedISBNs`, a `Set<Long>` cached in LibraryViewState) — but the cache is populated ONCE in `MostrarBibliotecaControl.init` (line 60) and only refreshed in `ContextMenuController.maybeShow` line 90 and `ContextMenuController.prestarLlibre` line 262. So the cache is stale between user actions (e.g. the user loans a book via the dialog, the table doesn't refresh the loan indicator until the next user-initiated action that calls `getLoanedISBNs`).
- [Lines 25-39] All five write methods are direct wrappers around `PrestecDao` calls. The `SQLException` → `BibliotecaException` translation is the only logic. The prior review's "let the presentation layer use the DAO directly" suggestion is right.

**[src/domini/facade/BackupDelegate.java]** — severity: LOW
- [Lines 100-103] `clearAll` does DB-then-memory. New evidence: `clearAll` is the "empty library" path, called from `ConfiguracioDialog`'s "Buidar tota la biblioteca" button. The `state.clearAll()` at line 102 clears the in-memory lists but does NOT clear `state.llistesById()` and `state.tagsById()` consistently... wait, line 81-82 of StateContext clears them. OK. But `clearAll` doesn't notify the event bus — if any listener (filter drawer, sidebar) cached a stale list, it stays stale.

================================================================================
  persistencia/
================================================================================

**[src/persistencia/ControladorPersistencia.java]** — severity: MEDIUM
- [Lines 110, 102-110] `getAllLlibres()` and `getAllLlibresSummary()` are LITERALLY the same body (`return libreDaoCore.getAll()`). The "lean" promise in the Javadoc is a doc comment only. The `LlibreDaoCore.getAll` method (LlibreDaoCore.java:54) DOES use the `LLIBRE_COLUMNS_L_LIGHT` (line 60) and DOES set `setHeavyFieldsLoaded(false)` on the result. So the "lean" is implemented in the DAO. But the *facade* has two identically-named methods that claim to differ. New evidence: `getAllLlibresSummary` has no call site that passes a flag — the only consumer (BookReader.getAllLlibresSummary default, MostrarBibliotecaControl.refresh line 164) calls it without a parameter. So "summary" vs "all" cannot differ at the API level. Either delete the doc-only `getAllLlibresSummary` or change its return type to a `LightLlibre` view that doesn't carry `descripcio`/`notes` fields at all.
- [Line 35] `private static ControladorPersistencia inst;` is not `volatile`. `getInstance()` is `synchronized` (line 48), which gives the lock-as-side-effect guarantee, but a concurrent caller of `getInstance()` from another thread can be blocked on the lock. The TODO at line 27-30 (line 27 actually) proposes `ReadWriteLock` for methods, but the field is also missing `volatile` for DCL. The current pattern works but is slow.
- [Line 91-100] Shutdown hook is registered exactly once via `AtomicBoolean`. New evidence: the hook (line 96-98) calls `sc.closeConnection()` in a try-catch that swallows the exception. `closeConnection` (ServerConect.java:363-371) catches its own `SQLException` and logs it. The result: any DB error at shutdown is logged but the shutdown proceeds. This is the "noisy shutdown" pattern — fine, but the log line at INFO level might confuse users. Set to FINE.

**[src/persistencia/ServerConect.java]** — severity: MEDIUM
- [Line 159-177] H2 URL building. The `dir.trim().replaceAll("^\\s+|\\s+$", "")` (line 162) is two regex operations. `String.strip()` (Java 11+) does the trim in one call. Minor.
- [Line 122-144] `testConnection(Properties)` and `testConnection(Properties, char[])` overloads. The Javadoc claim "the caller can zero the password" is misleading per prior review. New evidence: the conversion at line 123 makes a copy of the password into a `char[]`, but the original `String` in the Properties hashtable is unchanged. Zeroing the `char[]` does nothing. Also: `props.getProperty("dbPassword", "").toCharArray()` returns a NEW char[] each call — every call to `testConnection` allocates. Not a hot path, OK.
- [Lines 218-239, 246-270] Migration code. The pre-flight check at line 293 (`columnExists`) catches the "already dropped" case. OK.

**[src/persistencia/LlibreDaoCore.java]** — severity: MEDIUM
- [Line 100-114, 119-135] `insert` and `update` both call `syncAutors`. New evidence: `insert` calls `syncAutors` BEFORE the `withTransaction` body returns. If `syncAutors` fails (FK violation on `autor.nom` UNIQUE? No, `INSERT IGNORE` on line 289 handles that), the transaction rolls back. But: the `withTransaction` wrapper at line 100 is `() -> { INSERT; syncAutors(); }` — `syncAutors` is INSIDE the transaction. Good, but the `INSERT IGNORE INTO autor` (line 289) generates auto-increment IDs that may not match the existing `autor` row's ID. The subsequent `INSERT INTO llibre_autor ... SELECT ?, id FROM autor WHERE nom = ?` (line 298) handles this correctly. But: the two statements are NOT inside an explicit savepoint, so if the first batch (`INSERT IGNORE INTO autor`) succeeds and the second batch (`INSERT INTO llibre_autor`) fails, the autor rows are committed (they were created). The `withTransaction` rollback (line 270) rolls back both, so it's fine.

**[src/persistencia/LlibreFieldBindings.java]** — severity: MEDIUM
- [Lines 38-39] `Integer.valueOf(0)` and `Double.valueOf(0.0)`. New evidence: the explicit `Integer.valueOf` is the SAME bytecode as the autoboxed `0` literal — `Integer.valueOf(0)` is the canonical form, the `0` literal invokes `Integer.valueOf(0)` automatically. So the `Integer.valueOf(0)` here is redundant, not wrong. Minor style.
- [Lines 47-110] `forInsert` (27 fields) and `forUpdate` (25 fields) are parallel arrays. New evidence: the `LlibreDaoCore.insert` (line 107-108) iterates `forInsert` and calls `LlibreFieldBindings.bind(ps, i + 1, vals[i])` — but `bind` (line 121-141) does an `instanceof` cascade. For 27 fields, that's 27 instanceof checks per insert. A precomputed `IntFunction<Object>` or per-column `BiConsumer<PreparedStatement, Integer>` avoids the per-row instanceof.

**[src/persistencia/LlibreMapper.java]** — severity: MEDIUM
- [Lines 17-51] `buildLlibreLight` and `buildLlibre` are 80% identical. New evidence: the difference is `withHeavy` (whether `notes` is read). The `buildLlibreLight` passes `descripcio(null)` (line 23) but `buildLlibre` passes `descripcio(rs.getString("descripcio"))` (line 41). For `buildLlibreLight`, the `LLIBRE_COLUMNS_L_LIGHT` does NOT include `descripcio` (LlibreDaoCore.java:44-50), so the call would throw `Column "descripcio" not found` if the SELECT didn't include it. Confirmed: `LLIBRE_COLUMNS_L_LIGHT` lacks descripcio, so the explicit `null` is the only way. The two methods are correctly differentiated — but the 80% duplication is a maintenance hazard.
- [Lines 53-75] `fillLlibreTail` has a `withHeavy` parameter for the notes column. New evidence: the parameter ONLY controls notes; the descripcio is always read if present in the SELECT, never read if absent. The naming `withHeavy` is misleading — the method does more than just notes (e.g. `setEstat`, `setExemplars` are also in the tail). Rename to `includeHeavyText` or split into `fillNotes(l, rs)` and `fillTail(l, rs)`.

**[src/persistencia/LlistaDao.java]** — severity: MEDIUM
- [Lines 35-48] `create` uses `INSERT INTO llista (nom, ordre) VALUES (?, (SELECT COALESCE(MAX(ordre),0)+1 FROM llista AS sub))`. New evidence: the `COALESCE(MAX(ordre),0)+1` is racy even on a single JVM. The `synchronized` on `ControladorPersistencia.createLlista` (line 138) serializes the calls, so two threads CAN'T both call this at the same time. OK. But the `Statement.RETURN_GENERATED_KEYS` (line 40) requests the generated id; for H2's `AUTO_INCREMENT`, the id is generated. For MariaDB, the `AUTO_INCREMENT` also works. OK.
- [Lines 167-189, 198-223] `getLlistesForLlibre` and `getLlistesForLlibreContext` are 90% duplicated. New evidence: the SQL strings are IDENTICAL (line 171-172 vs 202-203), but the projection is different (`Llista` with side-effecting `setValoracioLlibre`/`setLlegitLlibre` vs `LlibreLlistaContext`). Extract a `private List<Object[]> queryLlistesForLlibreRaw(long isbn)` that does the SQL and returns the row arrays; the two public methods map to their target types. The prior review's complaint stands.
- [Lines 98-118] `getLlibres(int llistaId)` builds a near-duplicate of `LLIBRE_COLUMNS_L` with `ll.valoracio` and `ll.llegit` substituted for the `llibre` versions. New evidence: the SELECT has 27 columns and the duplication is 25 lines of column listing. The fix is to pass the column list as a parameter: `String cols = swapColumnsForShelf(LLIBRE_COLUMNS_L, "valoracio", "ll.valoracio", "llegit", "ll.llegit")`. Tedious but eliminates the 25-line copy.

**[src/persistencia/TagDao.java]** — severity: MEDIUM
- [Lines 22-23] `AUTOCOMPLETE_COLUMNS` is `new HashSet<>(Arrays.asList(...))`. New evidence: should be `Set.of("editorial", "serie", "idioma", "pais_origen", "format", "llengua_original")` — `Set.of` returns an immutable set with one allocation, vs `HashSet + Arrays.asList` (two allocations + a wrapper List). The `Set.of` form prevents accidental mutation. The same columns are duplicated in `StatsDelegate.getDistinctValues` switch (StatsDelegate.java:45-50) — both lists should derive from a single source.
- [Lines 124-139] `llibreTagCache` is `volatile` but the read-modify-write at line 125-138 is not atomic. New evidence: two threads calling `getAllLlibreTag` simultaneously both see `null`, both run the query, both assign to the field. The second write wins. The result is correct (the same data) but the second query is wasted. A `synchronized` block or a `Supplier` pattern would deduplicate. Minor for a cache that's invalidated on every llibre_tag mutation.
- [Lines 109-122] `getLlibresWithTag(int tagId)` does a full scan. New evidence: for 5000 books with 50 in a tag, the result is 50 ISBNs. The same query is `getISBNsInLlista` (LlistaDao.java:120-134). The two should share the helper. Different return types (Set vs Set) but the SQL is identical pattern.

**[src/persistencia/AutorDao.java, RowMappers.java]** — severity: LOW
- Two `RowMapper<T>` interfaces (AutorDao.java:10, RowMappers.java:15). New evidence: the `RowMappers.queryAll` helper (line 18-25) throws `SQLException` (correct), but `AutorDao.queryAll` (line 16-25) catches `SQLException` and wraps in `BibliotecaException`. The two helpers have different contracts — one throws, one wraps. Pick one. The DAO layer should throw `SQLException` (consistent with the rest of the DAOs), and the facade should wrap. Right now the DAO does both. Use `RowMappers.queryAll` from `AutorDao`; the public methods can wrap.

**[src/persistencia/ConnectionFactory.java]** — severity: LOW
- [Lines 15-24] `ConnectionFactory.open()` is dead. `ControladorPersistencia` (line 80-81) uses `new ServerConect()` directly, not `ConnectionFactory`. The `withConfig` static method (line 10-13) IS used by `ServerConect.createDatabase` (line 148), but `open` is never called. Delete `open`. Prior review noted this; still dead.

**[src/persistencia/JarLocator.java, LibDirResolver.java]** — severity: LOW
- Two parallel implementations of "find the lib/ directory". `ServerConect.findLibDir` (line 353-355) uses `JarLocator.locate` exclusively. `LibDirResolver` is never imported. New evidence: `LibDirResolver.resolve` is package-private and would be the right helper if it were wired in. Right now `JarLocator` has 113 lines of strategies and `LibDirResolver` has 46 lines of similar strategies. Delete `LibDirResolver`. Prior review noted.

================================================================================
  herramienta/
================================================================================

**[src/herramienta/Config.java]** — severity: MEDIUM
- [Lines 141-153, 155-166] The `reload()` method (line 141-153) and the static initializer (line 155-166) do the same work. New evidence: the static initializer is at line 155-166 and the reload at line 141-153. Both call `tmp.load(in)` + `tmp.forEach((k, v) -> props.put(...))`. The static block runs at class load; reload is called from `ConfiguracioDialog.java:103`. Two copies of the same code, two places to update.
- [Lines 403-419] `batchActive` is `volatile` but never read. New evidence: `grep` for `batchActive` returns ONLY the field declaration and the read/write in `withBatch` (line 411-419). The "batch" mechanism does NOT actually batch — every setter calls `save()` individually. `withBatch` is "run action, then save". The flag is misleading. Either remove the flag and rename `withBatch` to `withSave`, or actually use the flag (e.g. `if (batchActive) skip; else save();` per change).
- [Lines 449-451] `Files.move(..., REPLACE_EXISTING, ATOMIC_MOVE)`. New evidence: on macOS, `ATOMIC_MOVE` requires both source and destination to be on the same filesystem and the destination to not be open by another process. If the user has the config file open in a text editor when the app saves, the move fails with `AtomicMoveNotSupportedException` and the catch at line 452-454 logs a warning. The user's config is NOT saved. The fallback (non-atomic move) is not implemented. Add `try { atomic } catch (AtomicMoveNotSupportedException) { non-atomic; }`.

**[src/herramienta/UITheme.java]** — severity: MEDIUM
- [Lines 331-340] `scaledIcon` returns `null` on error (line 338). New evidence: the method is called from `BookExporter.printHtmlGrid` (line 213-214) and from `DetallesLlibrePanelControl.carregarImatgeBlob` (line 213). The caller at `BookExporter.printHtmlGrid` (line 208) prints the cover image inline; a null return means "no image, show placeholder". OK. But `DetallesLlibrePanelControl` line 213: `vista.getLabelIcono().setIcon(UITheme.scaledIcon(data, IMG_W))` — the label gets a null icon, which Swing treats as "no icon". The user sees a blank label. A `setIcon(CoverImageCache.NO_COVER)` would show the "no cover" placeholder.
- [Lines 215-282] `setTheme` has 4 cases (DARK, SEPIA, OCEAN, LIGHT) that mostly repeat the same structure. New evidence I can add: each case is 13-15 lines of identical field assignments with different RHS values. A `private static final Map<Theme, Palette> PALETTES = Map.of(...)` would compress the 4 cases into 4 record literals. The `palette()` method (line 54-59) already returns a `Palette` snapshot — the only thing the switch does is `currentTheme = t;` plus the field writes. A `PALETTES.get(t).applyTo(UITheme.class)` would do the same.

**[src/herramienta/Palette.java]** — severity: LOW
- 15-field record at top level. New evidence: 21-field record `UITheme.Palette` (UITheme.java:28-49) is the actual user. The top-level `Palette` (this file) is referenced ZERO times in the codebase (grep returns no import). Delete the file. Prior review noted.

**[src/herramienta/CoverService.java]** — severity: MEDIUM
- [Lines 21-25] `POOL` is `static final` and never shut down. New evidence: the daemon thread factory (line 22-25) creates daemon threads, so the JVM exit kills them. But during a long-running test that spawns many cover fetches, the daemon threads hold 6 thread stacks + executor queue. Add a `shutdown()` method called from a shutdown hook (parallel to `BackupService.shutdownScheduler` at line 41-42). Prior review noted.
- [Lines 67, 78] `synchronized (cd) { cd.setLlibreBlob(lIsbn, cached); }` — synchronizes on the `BibliotecaWriter` (which is `cd`, the `ControladorDomini` singleton). New evidence: `cd.setLlibreBlob` is itself `synchronized` through `ControladorPersistencia` (line 129 of ControladorPersistencia.java). The outer `synchronized (cd)` is double-locking. Worse: a future caller that also synchronizes on `cd` AND calls `CoverService.fetchAsync` could deadlock. Replace with `cd.setLlibreBlob` (which is already synchronized through the facade) and drop the explicit lock.
- [Line 28-30] The `LinkedHashMap` for L1 cache is `Collections.synchronizedMap`-wrapped. New evidence: `Collections.synchronizedMap` only synchronizes individual operations, not the iteration. The `removeEldestEntry` callback (line 29) is called WHILE the map is being modified. The contract is "synchronizedMap is thread-safe for individual operations, but compound actions need external synchronization". For a bounded LRU, the remove-eldest pattern is a compound action. Concurrent fetches could race on the size() check. Use a `ConcurrentHashMap` with `compute` or a plain `LinkedHashMap` with explicit `synchronized (L1_LOCK)` (which is already used at line 36-39, line 93-95).

**[src/herramienta/BackupService.java]** — severity: MEDIUM
- [Lines 115-172] `writeLlibreINSERT` is 26 `pw.print` calls per row. New evidence: the 26 fields duplicate `LlibreFieldBindings.forInsert` (which is 27 fields including the BLOB). A `writeLlibreRow(StringBuilder sb, Llibre l)` that builds the row in a `StringBuilder` and then `pw.println(sb)` once would reduce 26 `pw.print` + 1 `pw.println` to 1 write per row. For 5000 books, that's 130K writes → 5000 writes.
- [Line 88-93] `cp.getAllAutors()` and `cp.getAllLlibreAutor()` return `List<Object[]>` (ControladorPersistencia.java:177-186). The casts `(int) row[0]` and `((Number) row[0]).longValue()` are defensive. New evidence: the facade has a `getAllLlibreAutorRows()` method (line 188-190) that returns `List<LlibreAutorRow>` — a record. Use it. Same for `getAllAutors` — there's no `AutorRow` variant in the facade, but `AutorDao.getAll()` (line 30-33) returns `List<AutorRow>` directly. The `Object[]` round-trip in the facade is the dead-end.

**[src/herramienta/BookExporter.java]** — severity: MEDIUM
- [Lines 153-194] `exportPDF` is a 50-line `setPrintable` lambda. The whole method is 295 lines. New evidence: the `BookExporter` class is a God class (4 export formats, 295 lines). The `herramienta/export/HtmlExporter.java`, `herramienta/export/JsonExporter.java`, etc. are thin delegators (per the prior review). The fix: split into `JsonBookExporter`, `CsvBookExporter`, `HtmlBookExporter`, `PdfBookExporter` and delete `BookExporter`.
- [Lines 196-224] `printHtmlGrid` does `cd.getLlibreBlob(l.getISBN())` per book (line 201). For 5000 books in a grid, 5000 DB round-trips. New evidence: `CoverService.getCachedBytes` (CoverService.java:35-49) has the L1+L2 cache. The export should use the cache, not the DB. The fix is to add a `getCoverBytes(cd, isbn)` that checks the cache first.
- [Line 15] `private static final com.google.gson.Gson GSON = new com.google.gson.Gson();` — the Gson instance is reused. New evidence: `Gson` is thread-safe after configuration, so the static instance is correct. OK.

**[src/herramienta/JsonImporter.java]** — severity: MEDIUM
- [Lines 60-113] The import loop has 30+ `if (bo.has("...")) l.setXxx(...)` blocks. New evidence I can add: each `bo.get("...")` is a method call that traverses the JSON tree twice (once for `has`, once for `get`). A `JsonObject bo = be.getAsJsonObject();` followed by `if (bo.has("nom")) l.setNom(bo.get("nom").getAsString())` does 2 tree traversals. The `Optional` pattern (`JsonElement el = bo.get("nom"); if (el != null && !el.isJsonNull()) l.setNom(el.getAsString())`) does 1 traversal. For 5000 books × 30 fields = 150K traversals, the duplication is measurable. A `Map<String, Consumer<JsonElement>> setters` per field would compress.
- [Line 64] `try { cd.getLlibre(isbn); skipped++; continue; } catch (Exception ignored) {}` — the `ignored` catch swallows ALL exceptions, including `IllegalArgumentException` (ISBN parse failure) and `SQLException` (DB down). A book with an unparseable ISBN is silently skipped. The user has no idea why the import count is lower than expected. Log the exception with the ISBN.

**[src/herramienta/LlibreValidator.java]** — severity: MEDIUM
- [Lines 58-99] `checkLlibre(Long isbn, ...)` is called from `GuardarLlibresDialogoControl.crearLlibre` (line 168) with `isbn=null` when the user leaves the ISBN field empty. New evidence: line 67-69 `if (digits != 13 && digits != 10) throw new IllegalArgumentException(I18n.t("val_isbn_digits"));` — for null isbn, `digits=0`, throws "ISBN must have 10 or 13 digits". The user sees "ISBN must have 10 or 13 digits" when they DIDN'T enter an ISBN. Add an explicit `if (isbn == null) throw new BibliotecaException.Validation(I18n.t("val_isbn_required"));` BEFORE the digit check. The i18n key already exists (`toast_isbn_required`).
- [Line 31-51] `normalizeIsbn13` is called from `checkLlibre` (line 62) with `String.valueOf(isbn)` where `isbn` is `Long`. `String.valueOf((Long) null)` returns "null" (the string "null", 4 chars). New evidence: `String.valueOf(isbn)` on a `null` Long returns the string `"null"`. Then `isbn.charAt(isbn.length() - 1)` (line 33) reads the last char of "null" which is 'l' (lowercase L), not 'X'. The `normalizeIsbn13` then does the digit-strip and ISBN-10 conversion. The result: a null ISBN is normalized to either the digit-only string ("") or the original ("null"). The check at line 67-69 then says "digits != 13 && != 10, throw". OK, but the path is fragile. Add `if (isbn == null) return null;` at the top of `normalizeIsbn13`.
- [Line 9-29] `checkLlibreFromString` does the digit-strip and check-digit validation. New evidence: line 14-16 throws if `digits.length() != 13 && != 10`. For "019853110X" (ISBN-10 with X), line 33 catches the trailing X and converts to ISBN-13. The check-digit validation at line 18-23 only validates ISBN-13 (10-digit ISBNs with X are converted BEFORE the check). For an ISBN-10 that doesn't have X (line 44-49: ISBN-10 starting with 0 → ISBN-13), the conversion happens AND the check is done. For an ISBN-10 without X and not starting with 0, the digits stay at 10 and the check at line 18-23 is SKIPPED (because `digits.length() != 13`). So an ISBN-10 with a wrong check digit (e.g. "1234567890") passes. Add ISBN-10 check-digit validation.

**[src/herramienta/csv/CsvUtils.java]** — severity: MEDIUM
- [Lines 13-45] `parseLine` is a hand-rolled CSV parser. New evidence: the field array is `new String[16]` (line 16), growing by `Arrays.copyOf` when count reaches 16. For a 30-column CSV, the array grows 16→32 (one copy) on the 17th field. The copy is cheap but the constant 16 is a magic number. Use a `List<String>` and convert to array at the end. Or just use `String.split(",", -1)` and unquote manually — for non-quoted CSVs, the current parser is overkill.
- [Line 44] `return count + 1 == fields.length ? fields : java.util.Arrays.copyOf(fields, count + 1);` — trims the array to the actual count. New evidence: for `count=0` (empty line), returns `new String[]{""}`. For `count=0` with the field at index 0 being the empty string, returns `new String[]{""}`. The caller (BookImporter line 36) checks `row.length == 0` for "no row" — but `parseLine` never returns a 0-length array. The "empty line" case is `new String[]{""}`. The doc on line 50-55 acknowledges this ("absent vs empty") but the parser contract is inconsistent.

**[src/herramienta/I18n.java]** — severity: LOW
- The class is auto-generated (line 1). 537+ lines of static initializer. New evidence: the prior review's "package-info.java is standard, fine" stands. The I18n class itself is well-structured. Skip.
- [Line 5-12] The `LinkedHashMap<String, String[]>` TABLE is built in a static initializer. Each call to `T(key, ca, es, en)` adds 4 entries (one per lang? No, one entry with a 3-element array). For 200+ keys, 200+ T calls. The class load time is a few ms. OK.

**[src/herramienta/OpenLibraryClient.java]** — severity: MEDIUM
- [Line 38-44] `public static String testBaseUrl = null; public static int testMaxRetries = -1; public static long testRetryBaseMs = -1;` are public static mutable. New evidence: the comment at line 38-41 says "Not thread-safe; tests must not run OpenLibraryClient in parallel while these are set." The fix: use `System.getProperty` for these (already used elsewhere). The "set in test, restore after" pattern requires a saved-value-on-set mechanism, which `System.getProperty` provides via `System.setProperty` + `getProperties()`.
- [Line 99-122] `mergeGoogleBooks` catches `Exception` at line 94. New evidence I can add: `lookupByISBN` (line 53-97) calls `mergeGoogleBooks` in its own try-catch (line 94). The swallowed exception is "best-effort" but invisible. Log at `FINE` level — the class already uses `LOG.log(Level.FINE, ...)` at line 183 and line 204, so the pattern is established.
- [Line 209-230] `fetchWithRetry` retries up to 3 times with exponential backoff. New evidence: the retry loop's `Thread.sleep` (line 221) blocks the calling thread. If the caller is the EDT (via `ImportController.escanejarISBN` line 137, which starts a non-EDT thread for this — OK), the block is on a background thread. But `lookupByISBN` is also called from `LookupByIsbnAction` (the "Cerca a Internet" button), which uses `OpenLibrarySearchTask` SwingWorker. The retry is inside the SwingWorker's `doInBackground`. The 3×10s sleep blocks the worker. The UI shows a "Searching..." button (GuardarLlibresDialogoControl line 130). OK.

================================================================================
  interficie/ + main/ + presentacio/
================================================================================

**[src/interficie/InMemoryBibliotecaReader.java]** — severity: MEDIUM
- [Lines 45-47] `aplicarFiltres` doesn't actually filter — it just returns the full list (or a copy of `font`). New evidence I can add: the class doc says "Pure-in-memory for fast UI / pure-logic tests that don't need JDBC." A test that calls `inMemory.aplicarFiltres(filterWhereAutorIsX)` and asserts "only books with autor X are returned" will fail — `inMemory` returns all books. The class is unsafe for filter-testing. Either implement actual filtering or document "this class does NOT honor filter criteria; tests of filter logic must use a real DAO".
- [Line 25-28] `getLlibre(long isbn)` throws `Exception("Not found: " + isbn)`. New evidence: `BookDelegate.getLlibre` throws `BibliotecaException.NotFound` (a `RuntimeException`). The two are inconsistent — a test catching `BibliotecaException.NotFound` from `inMemory.getLlibre` would NOT match (it would catch the raw `Exception`). Use `BibliotecaException.NotFound` for consistency.
- [Line 19-22] `public final List<Llibre> books = new ArrayList<>();` — public mutable. The class doc says "Read-only; mutate the supplied lists between operations." A new instance has empty lists that the test can mutate. New evidence: the public fields are an "open API" — `BookReader.findLlibre(long)` is on the interface (line 10-13), but `InMemoryBibliotecaReader` doesn't implement it (it relies on the default). The default uses `getLlibre(long)` which does the linear scan. For a test with 10,000 books, the linear scan per call is 10K iterations. Add a `Map<Long, Llibre> byIsbn` for O(1) lookup.

**[src/main/Ejecutable.java]** — severity: LOW
- [Lines 42-43] Shutdown hook registered as `new Thread(persistencia.ControladorPersistencia::resetForProfileSwitch)`. The class has a `ShutdownHooks.register(...)` helper (line 7-9 of ShutdownHooks.java) that adds a `Thread(runnable)`. New evidence: `Ejecutable` uses `Runtime.getRuntime().addShutdownHook(new Thread(...))` directly. Inconsistent with `Config.java:458-461`, `ControladorPersistencia.java:96-98`, `UITheme.java:211`, and `DetallesLlibrePanelControl.java:222` which all use `ShutdownHooks.register(...)`. Use the helper.
- [Lines 50-62] `startSwingWithSplash` initializes `UIManager` properties before `UIManager.setLookAndFeel` (line 61). New evidence: the order is "set defaults, then set L&F" — but the L&F install OVERWRITES the defaults (Nimbus reads its own defaults from the L&F class). So the `UIManager.put(...)` calls at line 53-62 are partially wasted — they're set, then L&F install resets, then `applyUIManager` (which `UITheme.setTheme` calls) re-sets. The flow is redundant. Just set the L&F first, then call `UITheme.setTheme(theme)` which does the same puts inside `applyUIManager`.

**[src/main/SwingLauncher.java]** — severity: LOW
- 8 lines. Dead. The JAR manifest invokes `Ejecutable.main` directly. Prior review noted. Delete the file.

**[src/presentacio/MainFrameControl.java]** — severity: LOW
- [Line 62] `ioCtrl = new BookIOController(panel, cd, () -> cLlibres.getAllLlibres(), () -> mostrarControl.refresh())` — the prior review flagged that `MostrarBibliotecaControl` also creates a `BookIOController` (line 70). New evidence: two `BookIOController` instances, each with its own `ImportController` + `ExportController` + `BackupController`. The inner controllers hold no mutable state (their `currentBooks` and `onDataChanged` are set at construction). The "two instances" is harmless. The dead field at `MainFrameControl.ioCtrl` is a code smell but not a bug. Delete the field.

**[src/presentacio/MostrarBibliotecaControl.java]** — severity: MEDIUM
- [Line 39] `BibliotecaWriter writer = cd != null ? cd : domini.ControladorDomini.getInstance();` — falls back to the singleton if `cd` is null. New evidence I can add: the 3-arg constructor (line 32-35) passes `null` as `cd`, which triggers the fallback. The 4-arg constructor (line 37-38) is the production path (MainFrameControl.java:161 passes `cLlibres` as the 4th arg). The fallback is a test seam. For a test that creates `MostrarBibliotecaControl(vista, biblio, listener)` (no `cd`), the singleton is reached and the test's pure-logic data leaks into the production singleton. The fallback is dangerous in test environments.
- [Line 70] `ioCtrl = new BookIOController(state.vista, state.cd, this::getCurrentViewBooks, this::refresh)` — second `BookIOController` instance. Same as MainFrameControl review. Harmless but redundant.

**[src/presentacio/FilterController.java]** — severity: MEDIUM
- [Lines 167-177, 216-234] The `RowFilter.include` lambdas parse `entry.getStringValue(COL_ISBN)` per cell. For `aplicarSearchBar` (line 220), the loop iterates `entry.getValueCount()` columns. New evidence I can add: the default `BibliotecaTableModel.COL_PROGRES` (line 71) returns `l.getPaginesLlegides() + "/" + l.getPagines()` — a String concatenation per cell per repaint. The `getStringValue` call in the search filter converts this back to a String. For 5000 rows × 9 columns × 60 fps = 2.7M String allocations per second during a repaint. The `BibliotecaTableModel` doesn't cache the cell values; the model returns fresh String objects. Caching the cell values in a `String[][]` updated on `setBooks` would cut this by 99%.
- [Lines 70-71] `state.vista.getbtnFiltrar().addActionListener(e -> filtrar());` and `getbttnQuitarFiltros().addActionListener(e -> quitarFiltros());`. The `getbttnQuitarFiltros` typo (3 't's in `bttn`) is in the public API of `MostrarBibliotecaPanel.java:156`. Inconsistent with `getbtnFiltrar` (2 't's). Cosmetic.

**[src/presentacio/ContextMenuController.java]** — severity: MEDIUM
- [Line 47] `String isbnStr = (String) table.getValueAt(row, BibliotecaTableModel.COL_ISBN);` — raw cast. The cell is `String.valueOf(l.getISBN())` (BibliotecaTableModel.java:64), which is a String for non-null ISBNs but `"null"` for null. New evidence: `getValueAt` returns `String.valueOf(l.getISBN())` — for a null ISBN, this is the literal string `"null"`. Then `Long.parseLong(isbnStr)` (line 83) parses "null" and throws `NumberFormatException`. The whole context menu is then broken (the `Long.parseLong` is OUTSIDE the `try` at line 41-110). Add `if (!(value instanceof String)) return;` early.
- [Line 76] `batchIsbns.add(Long.parseLong((String) table.getValueAt(r, BibliotecaTableModel.COL_ISBN)));` — same raw cast pattern. Inside the `if (selectedRows.length > 1)` branch, no try-catch. A user with a malformed row gets an uncaught `NumberFormatException` thrown to the EDT → silent Swing default error handling.
- [Lines 228-254] `duplicarLlibre` does a deep copy via `Llibre.copyOf(src)` (line 231) then opens a `GuardarLlibresDialogo` (line 232). New evidence: the dialog's `getTextAutor` is set to `copy.getAutor()` (line 235) which is the joined string. The user then saves, and the dialog's save path (GuardarLlibresDialogoControl.crearLlibre line 174-175) re-splits on comma. The round-trip is lossy (per the Llibre.java finding). For an author with a comma in the name (e.g. "Smith, John"), the duplicate has 2 authors. Document or fix.

**[src/presentacio/BookActionsController.java]** — severity: MEDIUM
- [Lines 112-123] `eliminarFilaSeleccionada` does `state.cd.findLlibre(isbn).orElse(null)` (line 114) — the `orElse(null)` is followed by `if (l == null) continue;`. New evidence: `findLlibre` (BookReader.java:10-13) catches the exception and returns `Optional.empty()`. The `.orElse(null)` flattens the Optional. The `if (l == null) continue;` is correct. But: the `findLlibre` default does `getLlibre(isbn)` which for `BookDelegate` throws `BibliotecaException.NotFound` (a `RuntimeException`); the `catch (Exception e)` in the default catches it. The `Optional` then is empty. The pattern works but `findLlibre(isbn).ifPresent(l -> { ... })` is more idiomatic.
- [Line 128] `state.biblio.removeIf(b -> b.getISBN().equals(l.getISBN()));` — if `b.getISBN()` is null (a transient book), NPE. The Llibre is created with `Long isbn` (line 99-100) and the constructor accepts null. The `removeIf` is safe only if every Llibre in `state.biblio` has a non-null ISBN, which is the common case. Add `Objects.equals(b.getISBN(), l.getISBN())` for safety.

**[src/presentacio/detalles/control/DetallesLlibrePanelControl.java]** — severity: MEDIUM
- [Lines 159-163] The `DocumentListener` on `TextPaginesLlegides` only handles `insertUpdate` (line 160). `removeUpdate` and `changedUpdate` are no-ops (line 161-162). New evidence I can add: the prior review noted this. The concrete bug: a user types "1234" (clamped to e.g. "200" if total is 200, line 170-174). Then they backspace "3" — `insertUpdate` does NOT fire, `removeUpdate` does, which is a no-op. The field shows "20" but was originally "200" (or some other valid clamped value). On the next `insertUpdate` (e.g. user types "5"), the clamp runs and corrects. So the bug is "field may briefly show a too-low value" but the next keystroke corrects it. The bug is minor but real.
- [Lines 242-290] `imprimirFitxa` is a 50-line lambda. New evidence: the print uses `pendingBlob` (line 266) which is the LATEST image (after the user picked a new one), not the Llibre's stored blob. If the user prints without modifying, the image is the old one. If they pick a new one and then print, the image is the new one. The behavior is "print the current view" which is reasonable, but the Javadoc doesn't say so.
- [Lines 50-52] `private final DeleteHandler deleteHandler = new DeleteHandler();` and similarly for `HistoryHandler` and `EditHandler`. Confirmed prior review. New evidence: these handlers are inner non-static classes. Each holds an implicit `this$0` reference to the outer `DetallesLlibrePanelControl`. For a panel that opens 100 times, 100 × 3 = 300 handler instances are created. The handlers are stateless except for `vista` (which is `this.vista`). Make them `static` (with `vista` passed in) or top-level classes.

**[src/presentacio/detalles/control/GuardarLlibresDialogoControl.java]** — severity: MEDIUM
- [Line 163] `Long isbn = isbnTxt.isEmpty() ? null : Long.parseLong(isbnTxt);` — when `isbnTxt` is empty, `isbn=null`. The validator (LlibreValidator.checkLlibre) then throws "ISBN must have 10 or 13 digits" (LlibreValidator line 67-69). New evidence I can add: the dialog allows the user to create a book without an ISBN (the ISBN field is optional in the UI). The validator does not enforce this. The fix: add a "ISBN required" check in the dialog before calling the validator, OR have the validator accept null ISBN with a different error message.
- [Line 174-175] Parses `TextAutor` as comma-separated list. New evidence: same lossiness issue as DetallesLlibrePanelControl. The round-trip is lossy for authors with commas.
- [Lines 95-98] The `live` DocumentListener (line 93-97) fires on every keystroke. `refreshLiveValidation` (line 102-114) calls `LlibreValidator.checkLlibreFromString(isbn, "x", null, ...)` (line 107) for every keystroke. The check-digit validation is O(13) per character typed. For 13 characters typed in quick succession, 13 × 13 = 169 iterations. Plus the `normalizeIsbn13` allocates a String for each keystroke. The validation should be debounced.

**[src/presentacio/MostrarBibliotecaPanel.java]** — severity: MEDIUM
- [Lines 97-100] `applyTheme` does `UIManager.setLookAndFeel(new NimbusLookAndFeel())` (line 98) and then `UITheme.applyUIManager()` (line 99). New evidence I can add: the prior review was right that L&F install is heavyweight. The L&F install triggers `updateUI` on every component in the Swing tree, which can take 100-500ms for a complex UI like this app (a 5-7 panel composition with 30+ buttons). Every Ctrl+Shift+D theme toggle does a full L&F reset.
- [Lines 101-104] `SwingUtilities.updateComponentTreeUI(this)` (line 101) is the explicit version. Then `applyThemePostLaf` is called for several sub-panels (line 104-107). The `applyTheme` and `applyThemePostLaf` are called twice each (line 87-90 and line 104-107). The duplication is intentional (some styling must run after L&F reset), but the call sequence is not obvious. Document the order requirement.

**[src/presentacio/detalles/control/OpenLibrarySearchTask.java]** — severity: LOW
- [Line 60] `String isbnForCover = !isbn.isEmpty() ? isbn : meta.get("isbn");` — the search by title/author returns the first ISBN from the OpenLibrary response. New evidence: `meta.get("isbn")` is the first ISBN of the first result (OpenLibraryClient.lookupByTitle line 145-146 returns the first ISBN). For multi-edition books, this might be a different edition than the user's book. Document.
- [Line 75] `btn.setBackground(UITheme.palette().green());` — the "search completed" indicator. New evidence: the `styleSecondaryButton` in the next render (if any) overrides the background. The indicator is ephemeral (visible only until the next style call). Document or use a more permanent indicator (e.g. an icon).

**[src/presentacio/BibliotecaTableModel.java]** — severity: MEDIUM
- [Line 71] `case COL_PROGRES -> l.getPaginesLlegides() + "/" + l.getPagines();` — String concatenation per cell per repaint. New evidence I can add: for a 5000-book library, the `getValueAt` is called for every visible cell on every repaint. The concatenation is cheap but the resulting String object is short-lived and allocates. A `String.format` or a per-row `String[]` cache would amortize the cost.
- [Line 64] `case COL_ISBN -> String.valueOf(l.getISBN());` — for a null ISBN, returns the string `"null"`. New evidence: the table then displays the literal text "null" in the cell. A user with a Llibre that has null ISBN (e.g. a transient book being added) sees "null" in the ISBN column. Return `""` instead.
- [Line 70] `case COL_LLEGIT -> Boolean.TRUE.equals(l.getLlegit()) ? I18n.t("filter_read") : I18n.t("filter_unread");` — String lookup per cell. New evidence: the i18n keys are looked up every time. The result is the same for the same locale. A small cache of `(locale, boolean) → String` would avoid the repeated `I18n.t` call.

**[src/presentacio/CoverImageCache.java]** — severity: LOW
- [Line 13-14] `NO_COVER` is a 1×1 transparent ARGB BufferedImage. New evidence: the sentinel is used by `CoverCellRenderer` (via `cache.get(isbn) == NO_COVER`) to skip re-fetch. The `setIcon(null)` would also work (Swing's renderer treats null as "no icon"), but `NO_COVER` is distinguishable from "not yet fetched". The contract is opaque; document.
- [Line 22] `Set<Long> LOADING = ConcurrentHashMap.newKeySet();` — tracks ISBNs currently being fetched. New evidence: the set is added to in `CoverService.fetchAsync` (line 70?) but I didn't find the add. The set's purpose is to prevent concurrent fetches of the same ISBN. If the add is missing, concurrent fetches happen. Check.

**[src/herramienta/Config.java]** — also: [Lines 122-123] The `validation_invalid_number` i18n key (referenced from FilterController.java:375) is NOT in the I18n table (I18n.java:1-537). The key is missing. Add it.

================================================================================
  interficie + main: minor
================================================================================

**[src/main/SwingLauncher.java]** — severity: LOW
- 8 lines, dead. `Ejecutable.main(args)` is the JAR-manifest entry point. `SwingLauncher.main(args)` (line 5-7) is never called. Delete.

**[src/main/ShutdownHooks.java]** — severity: LOW
- 10 lines. `register(Runnable... hooks)` creates a new `Thread(h)` per hook. No daemon flag (so the JVM waits for them on exit, which is the desired behavior). No name. The lack of a name hurts debugging — when a JVM shutdown hangs, the named thread "config-shutdown" (Config.java:461) is identifiable, but other hooks aren't. Add `new Thread(h, "shutdown-hook-" + counter.incrementAndGet())`.

**[src/interficie/LibraryEvents.java]** — severity: LOW
- [Line 10] `default void onLlibreDeleted(Llibre l) { onBookDeleted(l); }` — naming inconsistency. The method is `onLlibreDeleted` but calls `onBookDeleted` (the parent `OnLlibreDelete.onBookDeleted` method). The two languages (Catalan/English) for the same operation are mixed within one file. Rename to either `onLlibreDeleted` everywhere (with default delegating to a renamed `onLlibreDeleted`) or `onBookDeleted` everywhere.

**[src/presentacio/FilterController.java]** — also: the typo `getbttnQuitarFiltros` (3 t's) is a code smell.

**[src/presentacio/LibraryViewState.java]** — severity: LOW
- [Line 21-26] Fields `biblio`, `modelLibres`, `currentLlistaId`, `loanedISBNs`, `groupBySeries` are non-`final`. The lists are mutable internally, but the references change (e.g. `state.biblio = ...` in MostrarBibliotecaControl.refresh line 158, 164). For a "shared mutable state for sub-controllers" class, the lack of `final` on the references is appropriate. OK.

================================================================================
  checkBiblio/  (stress + UI audit)
================================================================================

**[checkBiblio/StressTest.java, UIAudit.java, I18nAudit.java, UiTestSupport.java]** — severity: LOW
- Not exhaustively reviewed this run. Per prior-run calibration, the test/ tree is gitignored and the "Tests that don't actually assert behavior" category is excluded. StressTest.java is the chaos harness (1746+ lines per prior review); the UI audit gate runs against `checkBiblio/run.sh` / `run.ps1`. No new findings from this run.

================================================================================
## Calibration
================================================================================

1. **Weakest finding.** The `Llibre.java` line 136-141 `setAutor` finding is the strongest
   of the run but also the most speculative — the bug only triggers in a specific
   sequence (builder-construct + `addAutorNom` + later `setAutor`), and a developer
   reading the code can see the field-level corruption only by tracing 3 calls.
   A more concrete / less speculative finding is the `DetallesLlibrePanelControl`
   line 75 silent `catch (Exception e)` on `loadHeavyFields` — that's a
   measurable "user saves empty descripcio/notes without warning" pattern. Evidence
   that would strengthen the `setAutor` finding: write a unit test that builds a
   Llibre via `Llibre.builder().autor("Doe, Smith").build()`, calls
   `addAutorNom("Doe")` twice, then calls `setAutor("Doe, Smith")`, and asserts
   the resulting `getAutors()` list — today no such test exists.

   Second weakest: the `LlibreFilterBuilder` / `LlibreFilter` "still mutable" finding
   is half-redundant with the prior review's. The new evidence I added (line 60
   `withSort(null)` breaking the default) is a small delta; the prior review's
   "make it a record" recommendation still holds and is the real fix.

2. **New class of issue to look for next run.** I missed looking at **error-message
   consistency and the "validation contract" between dialog → validator → DAO**.
   This run surfaced 3 examples: (a) `GuardarLlibresDialogoControl.crearLlibre`
   passes a `null` ISBN to `LlibreValidator.checkLlibre` and gets back a confusing
   "ISBN must have 10 or 13 digits" message instead of "ISBN required"; (b)
   `LlibreValidator.normalizeIsbn13(String.valueOf(null))` returns the literal
   string "null" before the digit-strip (silently); (c) `JsonImporter` swallows
   every exception in the `try { cd.getLlibre(isbn); skipped++; continue; }`
   block, so a malformed ISBN is silently counted as "skipped" with no log line.
   The pattern is "validation is enforced at the wrong layer and the error
   message is wrong or invisible". Next run: trace every `try { cd.X(); ... } catch
   (Exception ignored) {}` and every dialog → validator → DAO chain, and flag
   where the contract is broken or the message is opaque.

3. **confidence: 78.** I am confident in every CRITICAL and HIGH finding (the
   Rfc4180Reader escaped-quote bug, the Llibre setAutor round-trip, the
   DetallesLlibrePanelControl silent catch, the BackupDelegate race, the linear
   scans, the Config `batchActive` never read, the `getAllLlibresSummary` duplicate).
   I am less confident in the MEDIUM findings where the fix is a refactor that
   depends on project priorities (the four-sub-store Config map, the 80-line
   ControladorDomini delegations, the 50-line LlibreValidator signature). I would
   bet money on the HIGH findings and the line-numbered SQL/locking/threading
   issues; I would not bet on every MEDIUM nit being a "bug" rather than an
   "agreed-upon tradeoff in the active backlog".

(End of file - total ~700 lines)
