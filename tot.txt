================================================================================
 BACKLOG (tot.txt) — Biblioteca
 Generated: 2026-06-08  (replaces separate todo.txt, SESSION4_PLAN.md,
                        PLAN-REMOVE-WEB.md, plans/remove-web-mode.md, agent-briefs/)
 Status legend: [OPEN] = not yet fixed, [PARTIAL] = partially fixed,
                [WONT] = out of scope / not pursued.
 Cleanup 2026-06-08: all [DONE] items removed (verified against src/).
================================================================================

Web mode removal: DONE in commits 877f163 / fa32d64. `src/api/` and `src/web/`
removed; `ModeSelectorDialog`, `ModeButton`, `WebLauncher` deleted. `Ejecutable`
rejects `--web` and starts Swing. `ConfigDTO.java` kept, Javadoc trimmed.

Priority scale: [1] critical / [2] important / [3] nice-to-have

================================================================================
 SESSIONS — ALREADY DONE  (historical record; see git log for current state)
================================================================================

Session 2 (bugs + dead code + test harness):
  - LlibreRouter update/delete/image: 404 not 500, PNG MIME sniff
  - ApiServer.classify: 404/409/400/500 mapping (now obsolete — api/ removed)
  - OpenLibraryClient: empty catch → LOG, interrupt re-thrown, pages null guard
  - TagDao AUTOCOMPLETE_COLUMNS whitelist verified
  - Dead code deleted: 6 interficie, 6 persistencia, 4 api files
  - Makefile test target: classpath + -jar launcher
  - Llista.setColor: isValidColor() + BibliotecaException.Validation
  - Llibre.setAny: negative-year guard
  - LlibreDao.executeSQLFile: quote-aware split + FORBIDDEN_LEAD blocklist
  - FiltreUtils.matches(): single predicate source
  - Tests: 7 plain + 10 JUnit 5 (all pass)

Session 3 (refactors + I18n + backups):
  - OpenLibraryClient: COVER_BASE/COVER_SIZE + javadoc
  - DialogoError: IS_HEADLESS cached, UIManager warning icon
  - install.sh: Java check + -Xmx512m
  - ModeSelectorDialog: window-X button SWING default (then file deleted)
  - BookExporter.exportHTML: "La meva biblioteca" → I18n key
  - ShelfParser extracted to domini; BookExporter.exportCSV delegates
  - BookExporter.exportPDF dead wrappers deleted
  - UITheme.rebuildFonts: EDT assertion
  - HttpRouter: duplicate errCtx.commit() removed (now obsolete — api/ removed)
  - BackupService.sqlEsc: \u001A stripped
  - GoodreadsCsvStrategy: Private Notes mapped
  - AutoCompletion: noted dead, lambdas not needed
  - I18n: export_html_heading + dlg_export_html_title keys added
  - Tests: 197 plain + 184 JUnit 5

Session 4 (mostly post-SESSION4_PLAN — see Phased order below):
  - LlibreLlistaContext.isbn → long, of(...) signature long
  - LlibreDao.java empty-nom !isBlank() guard
  - CoverService L1 synchronized via L1_LOCK
  - CoverCellRenderer NO_COVER sentinel + convertColumnIndexToView
  - ProgressBarRenderer passes Llibre via model, no DB in paint
  - BibliotecaTableModel.isCellEditable returns column == COL_LLEGIT
  - ImportController / ExportController / BackupController / FilterController
    all use SwingWorker for I/O
  - DetallesLlibrePanelControl: get() inside done() (non-blocking)
  - TagDao.invalidateLlibreTagCache called from delete / deleteFromLlibre /
    deleteAllFromLlibre
  - ControladorDomini: getAllLlistes / getAllTags return new ArrayList copy;
    setLlibreBlob uses Objects.equals; backupToSQL takes snapshot under lock
  - ControladorPersistencia.resetForTest() now synchronized
  - Isbn13Normalizer.toIsbn13 converts any valid 10-digit ISBN to 13
  - CsvUtils.parseIsbn converts any valid 10-digit ISBN to 13 (not just X)
  - BookImporter.importCSV uses Rfc4180Reader + StandardCharsets.UTF_8
  - BookImporter.importCalibre uses StandardCharsets.UTF_8
  - OpenLibraryClient: errorStream drained + disconnect() in finally
  - run.sh: I18nAudit in javac, javalin/kotlin-stdlib removed
  - run2.sh: same fixes
  - StressTest: GraphicsEnvironment.isHeadless() guard, ISBN collision
    detection, log() synchronized, screenshot only on fail/warn
  - UIAudit: biblioteca.h2.url always set, headless guard, copy-paste run
  - ServerConect.MIGRATIONS uses record Migration(int version, String sql)
  - BookExporter: any>0 ? any : "" (HTML)

Session 5 (post-tot.txt sweep — see Phased order below):
  - ShelfParser.exportToCsv: pw.printf now uses Locale.ROOT (es-ES locale
    was emitting "7,5" / "19,99" which broke re-import to 0.0)
  - Config.save: atomic write via Files.move (REPLACE_EXISTING + ATOMIC_MOVE);
    new flushNow() public method; shutdown hook registered for SAVE_SCHEDULER
  - ConfiguracioDialog: db-size query moved off EDT to SwingWorker
  - ShelfController.refreshComboLlistes: DB calls now in SwingWorker
  - ControladorDomini.deleteLlibre(Long): null ISBN now throws Validation
    (not NotFound) per I18n audit
  - ControladorDomini.backupToSQL: loadHeavyFields() moved OUTSIDE the
    synchronized block; snapshot taken atomically inside the lock
  - ControladorDomini.swapLlistesOrdre: in-memory swap moved BEFORE the DB
    write, with symmetric undo in the catch block
  - LlibreValidator.countDig: replaced String.valueOf(n).length() with a
    branchless threshold chain (correct for Long.MAX_VALUE, negative, etc.)
  - LlibreDao.search: instanceof chain replaced with bindParam() pattern-
    matching switch (null is bound as SQL NULL; unknown types throw with
    class name)
  - LlibreDao.executeSQLFile: splitStatements() now scans the whole file
    in one pass with inQuote state, so ';' inside string literals across
    lines no longer mis-splits
  - ServerConect.runMigrations: per-engine dispatch (H2 keeps the
    setAutoCommit(false)/commit/rollback wrapper; MariaDB/MySQL takes a
    non-transactional path because DDL auto-commits there); Javadoc
    explains the manual-cleanup requirement on a mid-sequence MariaDB
    failure and the idempotency guarantee
  - BackupService.autoBackup: removed redundant Thread.sleep(30_000) —
    scheduleWithFixedDelay already has the 30s initial delay
  - Llibre: new Builder and bindUpdateableFields() helper; all internal
    callers (LlibreValidator, LlibreDao) now use the builder or the
    helper. 9-positional-arg ctor kept for backward compat
  - UITheme.FONT_BASE / FONT_BOLD: now private static volatile with
    fontBase() / fontBold() accessors; 16 callers updated
  - strings/ui.csv: added app_title key. MainFrameControl JFrame title
    and AboutDialog header now use I18n.t("app_title")
  - LlistesDelLlibreDialog: shelfCheckList now MULTIPLE_INTERVAL_SELECTION
    and pre-selects indices for current shelf memberships
  - ControladorPersistencia: 17-line audit-history comment trimmed to a
    14-line class JavaDoc
  - Tests: 184 plain + 168 JUnit 5 (all pass)

================================================================================
 BUGS (residual)
================================================================================

[PARTIAL] BackupRouter / api/* — REMOVED, no longer in scope (was HIGH security: restore/clear
       unauth'd; PII in JSON error; CSRF from local page). If a new HTTP layer is ever
       reintroduced, these must be re-applied.

================================================================================
  DEAD CODE
================================================================================

[PARTIAL] interficie.BookReader — HALF-MIGRATED: 8 @Deprecated ArrayList methods, ArrayList
      fields in domain objects. Left untouched (interface change risks breaking callers).
      Marked as accepted debt.

================================================================================
  ANTI-PATTERNS  (status update)
================================================================================

[HIGH] Singleton god-object — ControladorDomini — STILL OPEN (60+ public methods,
       in-memory ArrayList, only path from UI to data).  [OPEN — needs major refactor;
       documented in Javadoc; sub-facade for navigation would help but not started]
[HIGH] Singleton god-object — ControladorPersistencia — STILL OPEN but the previous
       "double-locking" critique was wrong: methods are now uniformly synchronized
       on the controller, and DAOs do not double-lock.  [OPEN — facade is fine,
       but no sub-facade for navigation]
[HIGH] Mutable statics — UITheme.BG_MAIN, ACCENT etc. — STILL OPEN; theme switching
       reassigns these fields at runtime so they can't be `final`. Marked accepted
       debt; a Map<Theme, Palette> refactor would touch 273+ call sites.  [OPEN]
[HIGH] Anemic domain model — Llibre is a pure bean — STILL OPEN.  [OPEN]
[MED]  Inverted dependency — Llista.setColor / ControladorDomini.setLlistaColor
       uses FQN `herramienta.I18n.t` rather than an import; coupling is real but
       minimized. Full fix would move the i18n to the UI caller.  [OPEN — accepted]
[MED]  Inverted dependency — BackupService (herramienta) uses ControladorPersistencia.
       Util reaching into persistence.  [OPEN — accepted]
[MED]  Layer inversion — presentacio/detalles/control/ vs presentacio root vs
       presentacio/listener/ — inconsistent sub-packages.  [OPEN — accepted]
[LOW]  Manual re-implementation of Properties in Config.props — STILL OPEN.
       The ConcurrentHashMap-based props are read often (every Config getter
       call) and the boot path stays simple; java.util.Properties would
       need explicit synchronization for the same use.  [OPEN — accepted]

================================================================================
  CROSS-CUTTING / NICE-TO-HAVE  (most still OPEN)
================================================================================

[OPEN] Mixed language: method names Catalan, variable names Spanish
[OPEN] ISBN used as both Long (domain) and String (table/JSON)
[OPEN] BibliotecaWriter — single interface for everything
[OPEN] Perf: getAllLlibres() loads notes/descripcio/imatgeBlob eagerly
       (loadHeavyFields is the on-demand path; getAll still materialises a list)
[OPEN] Duplicate utility methods: parseDoubleOrZero, csvQ, parseIsbnParam
[PARTIAL] Hardcoded Catalan strings check across .java files — I18nAudit
       flags them with regex; full pass leaves ~30 warning sites
       (UITheme labels, hex colors, etc.). New occurrences replaced with
       I18n.t as they're touched.
[OPEN] ConfigRouter / LlistaRouter / TagRouter long methods
[PARTIAL] MostrarBibliotecaControl 6 sub-controllers wiring dense — Javadoc
       on MainFrameControl documents the split; the wiring itself is still
       dense but each sub-controller is single-purpose
[OPEN] JsonMapper.llibreToMap duplicates LLIBRE_FIELD_EXTRACTORS
[OPEN] ServerConect.MIGRATIONS now uses record — but inline SQL strings remain
       (acceptable; migrations are read rarely and append-only)
[OPEN] I18n.java 535+ lines of T() calls — should be ResourceBundle
       (auto-generated from strings/strings.csv; ResourceBundle migration is
       a generator-script change, not the Java file itself)
[OPEN] LoanRouter / LlistaRouter / TagRouter getById linear scans
[OPEN] DateUtils — small class, mostly one-liners; consider inline or expand
[OPEN] I18n Lang enum / Lang ordinal lookup (magic numbers 0/1/2) — internal
       switch on `lang` string in langIndex() returns 0/1/2 directly
[PARTIAL] I18n missing key returns key silently — logs to stderr AND throws
       IllegalStateException when `biblioteca.test=true` is set
       (I18n.java:572-575). Production fallback is still silent.
[OPEN] UITheme rebuildFonts from string — use FontSize enum
[OPEN] UITheme theme enum 4 values, only 2 surfaced
[OPEN] UITheme styleAccentButton / styleSecondaryButton in presentacio
       (UITheme.fontBase()/fontBold() accessors done; button style helpers
       remain in UITheme.java)
[OPEN] GoodreadsCsvStrategy canHandle() brittle string match
[OPEN] MainFrameControl actualitzarLlibre shelf auto-assignment buried
[OPEN] MainFrameControl getInstance() 3 overloads with guard logic
[OPEN] MainFramePanel extends JFrame — should be JPanel in JFrame
[OPEN] GuardarLlibresDialogo builder methods near-identical to DetallesLlibrePanel
[OPEN] GuardarLlibresDialogo setSize(600, 720) overrides setPreferredSize
[OPEN] GuardarLlibresDialogo missing fields vs DetallesLlibrePanel
[OPEN] BookExporter.exportHTML 200 lines inline CSS — extract template
[OPEN] Good test infra: ~400 lines duplicated between UIAudit and StressTest
       (extract checkBiblio.UiTestSupport)

================================================================================
  SUMMARY  (as of 2026-06-08 cleanup)
================================================================================

Web / API layer: REMOVED. If a future HTTP layer is added, re-apply the
ApiServer.classify + auth/CSRF + sanitized-error plan from SESSION4_PLAN Phase 4.

OPEN items by area:
  - Domini: 1 — ControladorDomini god-class refactor (deferred: needs a
    sub-facade for navigation); binarySearch → DB → in-memory race window
    is documented in Javadoc (accepted debt).
  - Anti-patterns: 4 high (UITheme mutable palette, Config manual props,
    BackupService→Persistencia inverted dep, Llista.setColor inverted dep,
    Llibre anemic domain model) + 3 med (BackupService→Persistencia, layer
    inversion, manual Properties) — all marked accepted debt with rationale.
  - Cross-cutting / nice-to-have: ~25 open (mostly long-tail refactors
    that don't affect correctness — mixed language, ISBN type duality,
    I18n ResourceBundle migration, MainFramePanel JFrame, etc.).
  - BUGS: 1 [PARTIAL] (api/ removal, no longer in scope).
  - DEAD CODE: 1 [PARTIAL] (interficie.BookReader half-migration).
  - checkBiblio: 0.
