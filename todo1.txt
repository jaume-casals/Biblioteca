# todo1.txt — per-file improvement plan (fresh pass, 75 files, 13489 LOC)
# Format: [ ] item. Tags: [div]=split, [un]=unite/consolidate, [desp]=despaghetti, [bug]=confirmed defect, [verify]=plausible defect needing confirmation, [perf]=performance, [clean]=remove dead code, [test]=missing coverage, [doc]=clarify intent.
# Date: 2026-05-16

============================================================
api/
============================================================

## src/api/ApiServer.java
- [x] [div] extract per-router registration into a `Routers.registerAll(router, cd)` static — currently 8 sequential `new XxxRouter(...)` calls clutter the constructor.
- [x] [un] unify exception handler with `HttpRouter.exception(...)` — single source for 4xx/5xx serialization.
- [x] [clean] commented "All routes are registered eagerly" — make that contract explicit by renaming ctor to `buildRouter()` returning the router, then `start()` builds-then-binds.
- [x] [test] add smoke test: spin up on random port, hit `/api/books/count`, assert 200.
- [x] [doc] state thread-model expectation: `cd` is shared; routers must `synchronized(cd)` for mutations.

## src/api/BackupRouter.java
- [x] [un] consolidate timestamp formatting — `yyyy-MM-dd_HH-mm-ss` repeated in `backup()` and `clear()`; extract to `private static String ts()`.
- [x] [bug] `tmp.delete()` in `restore()` ignores failure; if the temp file leaks, log it.
- [x] [div] split listing logic into a helper returning `List<Map<…>>` so the router method is just JSON glue.
- [x] [perf] `listBackups()` re-stats the directory on every call; cache last-modified mtime and invalidate on `backup()/clear()`.
- [x] [test] verify `clear()` writes a "pre_clear_" snapshot even when the live DB is empty (still useful as a sentinel).

## src/api/ConfigRouter.java
- [x] [un] CONFIG_SETTERS map duplicates the field list also in `getConfig()`; extract a single `CONFIG_KEYS` registry with both getter+setter lambdas.
- [x] [bug] PUT silently ignores unknown keys — return 400 with the offending key, or at least include them in the response body.
- [x] [clean] `dbPassword` is masked in GET but PUT accepts the masked value back — short-circuit when body value equals `***` to avoid clobbering.
- [x] [div] move font-size validation to `Config.setFontSize()` (it already throws) and drop the local `Set.of(...)` here.
- [x] [test] cover the validation path: invalid `fontSize` → 400, valid → 200.

## src/api/HttpCtx.java
- [x] [div] split into `Request` (path/query/body) + `Response` (status/json/header/commit) — single object juggles both roles.
- [x] [un] `queryParamInt/Dbl/Bool` are near-identical; replace with one `<T> T queryParam(key, parser, def)` helper.
- [x] [verify] `parseQuery` says "last value wins" — confirm with tests; today multiple `?tag=` would silently drop the first.
- [x] [clean] `storedEx` plumbing is reachable only via package-private setter; rename `getException` → `lastException` and document it's only for exception handlers.
- [x] [perf] `bodyBytes()` reads unbounded — add a configurable max-body cap (DoS guard for `/api/restore` and `/api/import/json`).

## src/api/HttpRouter.java
- [x] [div] split `dispatch()` — CORS preflight, route match, static-file fallback are three concerns inlined.
- [x] [bug] `routes` is `ArrayList` and unsynchronized; if routes were ever registered after `start()` the iterator could throw. Either snapshot on `start()` or use `CopyOnWriteArrayList`.
- [x] [perf] linear route scan on every request — bucket routes by HTTP method first.
- [x] [un] merge `Route.matches()` + `Route.extractParams()` into a single `tryMatch(method, parts) -> Optional<Map>` pass.
- [x] [test] add a route-matcher unit test: trailing slash, empty segments, `/api/books/{isbn}` vs `/api/books/recent` ordering.

## src/api/ImportExportRouter.java
^- [x] [div] each of the 5 endpoints (`exportJson`, `exportGoodreadsCSV`, `importJson`, `importCsv`, `fetchCovers`) is its own concern — split into `JsonRouter`, `CsvRouter`, `CoversRouter` or extract helpers to a `BulkExporter`.
- [x] [un] CSV/JSON tag/shelf id-remap logic duplicated with `herramienta.BookImporter.importJSON` — extract `ShelfIdRemapper` + `TagIdRemapper`.
- [x] [bug] `fetchCovers()` returns 200 immediately, then writes to DB from a background pool — no progress endpoint, no abort, no error reporting back to client.
- [x] [verify] `exportGoodreadsCSV` lossy: only one shelf chosen for `Exclusive Shelf`, but `Bookshelves` repeats all — verify Goodreads import roundtrips.
- [x] [bug] `strategies.stream().filter(...).findFirst().get()` throws `NoSuchElementException` if no strategy claims the header — fall back to NativeCsv like the Swing importer.

## src/api/JsonMapper.java
- [x] [un] llibreToMap()/jsonToLlibre() are mirrors; replace with a `LlibreCodec` that derives the field list from one place (Llibre annotations or a static FIELDS array).
- [x] [bug] `jsonToLlibre` reads `llegit` only if present and truthy — verified: ternary handles all three cases (absent=false, true=true, false=false)
- [x] [clean] private `optStr/optInt/optDbl/has` repeat the gson null-guard pattern; one generic helper would do.
- [x] [doc] document that ImatgeBlob is intentionally excluded — already in a comment, but state it explicitly in javadoc.
- [x] [test] roundtrip test: build Llibre with every field set → llibreToMap → JSON → jsonToLlibre → equals.

## src/api/LlibreRouter.java
- [x] [div] `buildFilter()` belongs to `LlibreFilter` or a `LlibreFilterFactory` — same code likely needed in CLI/REST tooling.
- [x] [un] OpenLibrary endpoints are squatting on this router; move to a `MetaLookupRouter` (TODO comment already says so).
- [x] [bug] `update()` overwrites `hasBlob` from the existing record (good — client cannot lie) but does not re-read `imatgeBlob` bytes; LlibreDao.update() omits the blob column, so the server-stored blob lives or dies based on stale state. If the blob was deleted between GET and PUT, the response still reports `hasBlob=true`.
- [x] [perf] `validate()` calls `Llibre.copyOf(l)` — for hot ingest paths this doubles allocation; have validate return void.
- [x] [test] every endpoint should have a happy + 4xx test (bad ISBN, missing title, oversize body).

## src/api/LlistaRouter.java
- [x] [div] `findById()` linear scan repeated 4×; replace with `cd.getLlistaById(id)` in domain, throws on miss.
- [x] [bug] `rename()` returns the *post-rename* shelf by re-looking-up — race window if another caller deletes between the rename and the lookup; pass the renamed object back from domain.
- [x] [un] `moveUp/moveDown` differ only in direction — extract `move(int id, int dir)`.
- [x] [clean] `addBook` returns 201 even when row already existed (INSERT IGNORE) — should be 200 in that case for accurate REST semantics.
- [x] [test] verify `setColor` accepts null (clear) and hex strings only, rejects garbage.

## src/api/LoanRouter.java
- [x] [un] `getAll` and `getForIsbn` Map.of() patterns duplicate; extract `loanToMap(PrestecRow)`.
- [x] [verify] `returnBook` returns 204 normally but `404` when book not on loan — the 404 body is JSON `{error: ...}` yet 204 must have NO body; verify the commit path.
- [x] [div] move borrower-required check to a `LoanRequest` record with constructor validation.
- [x] [clean] `throw new Exception("Borrower name required")` — use IllegalArgumentException so the exception handler maps to 400 correctly.
- [x] [test] integration test: lend → return → lend again should succeed.

## src/api/MetaRouter.java
- [x] [bug] `ALLOWED_DISTINCT` whitelist hardcodes columns; if `TagDao.AUTOCOMPLETE_COLUMNS` diverges they silently mismatch — drive both from one constant.
- [x] [un] `distinct()` special-cases "autors"; flatten by having `cd.getDistinctValues("autor")` route to `getDistinctAutorNames()` internally.
- [x] [perf] `stats()` iterates `getAllLlibres()` 3×; one pass with counters is enough.
- [x] [div] dbsize formatting (`bytes/mb`) is presentation concern — keep raw `bytes` only in the API, let clients format.
- [x] [test] add test ensuring `bytes` is -1 → response still well-formed.

## src/api/TagRouter.java
- [x] [div] same `findById()` linear-scan pattern as LlistaRouter — share a `cd.requireTag(id)` domain helper.
- [x] [bug] `rename()` re-fetches by id after the synchronized block — same race as LlistaRouter; pass renamed tag back.
- [x] [clean] `target` var in `rename()` is computed but unused (the second `findById` shadows the use).
- [x] [un] `addToBook/removeFromBook` mirror the LlistaRouter pair — extract a generic `MembershipRouter`.
- [x] [test] verify renaming to an existing name returns 400 (currently the unique constraint will throw SQLException → 400 via exception handler, but message is generic).

============================================================
domini/
============================================================

## src/domini/BibliotecaException.java
- [x] [div] differentiate domain errors: `NotFound`, `Duplicate`, `Validation` subclasses — currently every layer throws the same opaque exception.
- [x] [doc] javadoc when callers should catch this vs let it propagate.
- [x] [un] HTTP layer translates exception → 400 by string match — switch to type-based dispatch once subclasses exist.
- [x] [clean] add a public `code()` enum so logs/metrics can group errors.
- [x] [test] verify the message-cause constructor preserves stack trace through layers.

## src/domini/ControladorDomini.java
- [x] [div] huge god-class — split into `BookStore`, `ShelfStore`, `TagStore`, `LoanStore` + a thin facade. Currently 396 lines and grows with every feature.
- [x] [desp] `aplicarFiltres()` decides in-memory vs SQL by `SQL_FILTER_THRESHOLD` constant — make threshold injectable for tests and predictable benchmarking.
- [x] [bug] in-memory `aplicarFiltres()` does not apply `sortColumn/sortAsc`; SQL path does. Inconsistent results across thresholds.
- [x] [un] error wrapping `try{...}catch(SQLException e){throw new BibliotecaException(e)}` repeated ~25×; extract a `void domain(SqlOp)` helper or use a `@FunctionalInterface`.
^- [x] [perf] `bib` is sorted ArrayList with binarySearch for getLlibre/exists — fine, but `addLlibre` shifts on every insert; consider TreeMap<Long,Llibre> for O(log n) writes too.

## src/domini/Llibre.java
- [x] [div] split into a record `LlibreCore(isbn,nom,autors,any,...)` + extension class for blob/notes/has-blob mutable state — separates value from cache state.
- [x] [bug] `getAutor()` returns String joined by `, ` from `autors`, but `setAutor(String)` clears `autors` — semantic asymmetry; renaming would catch the gotcha.
- [x] [un] setters that trim+null-empty repeat 8× (`setIdioma`, `setFormat`, `setPaisOrigen`...); extract `private String normalize(String)`.
- [x] [clean] `copyOf` is hand-maintained; one missing field silently breaks update flows — generate or use reflective copy.
- [x] [test] property test: round-trip every field via copyOf, ensuring no field is dropped.

## src/domini/LlibreFilter.java
- [x] [div] convert to a record + builder; current public-fields-everywhere style invites mutation bugs.
- [x] [un] `copy()` and `hasAnyFilter()` walk the same field list — both must be hand-updated; reflect or codegen.
- [x] [bug] `hasAnyFilter()` ignores `sortColumn/sortAsc` (intentional?) — comment why or include if relevant.
- [x] [clean] mixes "predicate" (autor/nom) with "sort" (sortColumn) — extract `SortSpec` so callers only carrying sort don't construct a giant blob.
- [x] [test] table-driven test verifying every nullable field independently triggers `hasAnyFilter()`.

## src/domini/LlibreLlistaRow.java
- [x] [un] consider merging the three relation rows (LlibreLlista/LlibreTag/LlibreAutor) under a sealed interface `RelationRow` if any cross-cutting handler appears.
- [x] [doc] note that `valoracio` is the per-shelf rating, not the book's primary rating.
- [x] [verify] `valoracio` is `double`; verify `0.0` default vs `null` doesn't lose meaning ("unrated" vs "rated 0").
- [x] [test] verify equality with NaN/Infinity is sane (probably won't happen but cheap to assert).

## src/domini/LlibreTagRow.java
- [x] [un] see [un] in LlibreLlistaRow — combine record family.
- [x] [doc] note: order in DB is `tag_id, isbn` for some queries; record-level ordering doesn't enforce that.

## src/domini/Llista.java
- [x] [div] split persisted state (id/nom/ordre/color) from query-only state (valoracioLlibre/llegitLlibre) — current shared bag confuses readers.
- [x] [bug] `equals` only by id — two shelves with same id from different libraries collide; include profile/dbName in identity if multi-db ever lands.
- [x] [un] `toMap()` repeated style with Tag/Llibre — extract a generic `Mappable` interface.
- [x] [clean] toString returns nom; document used by JComboBox (it already does in comment) — fine.
- [x] [test] verify setNom blank throws and color hex parsing tolerates "#abc" 3-digit form.

## src/domini/PrestecRow.java
- [x] [div] add overdueDays() helper given a threshold so consumers don't recompute date math.
- [x] [bug] `dataPrestec` is String, not LocalDate — date math elsewhere must reparse; convert to `LocalDate`.
- [x] [un] use across UI + audit reports a single `PrestecRow.toDisplayMap()` formatter.
- [x] [test] roundtrip via JSON to ensure date string format remains ISO-8601.

## src/domini/Tag.java
- [x] [un] near-identical structure to Llista — extract `NamedEntity` base class or interface.
- [x] [bug] `equals` by id ignores nom; same potential cross-db issue as Llista.
- [x] [clean] toString returns nom; comment acceptable but javadoc would compile-check.
- [x] [test] case-insensitive comparison helper (`equalsIgnoreCase`) for tag dedup at import time.

============================================================
herramienta/
============================================================

## src/herramienta/AutoCompletion.java
- [x] [clean] third-party public-domain code — annotate clearly with `// Source: ...` and isolate; do not modify in-place to avoid drift.
- [x] [un] consolidate with FieldAutoComplete — currently two parallel autocompletion utilities (one for JComboBox, one for JTextField); a shared `Completer` interface would help.
- [x] [bug] unchecked cast `(ComboBoxModel<String>)` triggers the build warning ("uses unchecked or unsafe operations"); parameterise or `@SuppressWarnings` with rationale.
- [x] [div] `keyPressed` handler is empty except for VK_BACK_SPACE — flatten the switch into a single if.
- [x] [test] verify Catalan diacritics autocomplete (since matching uses lower-case prefix, no NFD).

## src/herramienta/BackupService.java
- [x] [verify] hand-written 26-arg printf in `backupToSQL()` for llibre row is fragile — any new column on Llibre must be added here too. Add a schema-diff test to catch drift.
- [x] [div] `printf` template is 26 args long; extract one printf builder per table.
- [x] [un] `sqlEsc` duplicates logic in LlibreDao escape paths — consolidate.
- [x] [perf] `scheduleAutoBackup` sleeps 30s on a daemon — replace with a `ScheduledExecutorService` (one already exists in Config).
- [x] [bug] `scheduleAutoBackup` only runs once after 30s; doesn't repeat per day. If the app stays open >24h, no further snapshots happen.

## src/herramienta/BookExporter.java
- [x] [div] one class handles CSV, JSON, HTML, PDF — split into `Csv/Json/Html/Pdf` exporters under a `BookExport` package.
- [x] [un] `jsonStr/htmlEsc/esc` three escape helpers — unify into `Escapers` utility with explicit method names.
- [x] [bug] CSV writes `\"%s\"` for ISBN — wrapping numeric ISBN in quotes is fine for Goodreads, but `exportCSV` uses unquoted columns elsewhere; inconsistency could break import.
- [x] [desp] `exportPDF` is 50+ line lambda inline; lift into a `BookPdfPrinter` so paging/layout is testable.
- [x] [test] golden-file test: exportJSON output diffable against `test/fixtures/expected.json`.

## src/herramienta/BookImporter.java
- [x] [div] three import methods (`importCSV`, `importJSON`, `importCalibre`) in one class — split into strategies under `BookImport`.
- [x] [bug] `importCalibre` calls `sqlite3` subprocess — no quoting of `dbFile.getAbsolutePath()`; if path has shell-sensitive chars and Runtime.exec passes them as args, it's fine, but document.
- [x] [un] tag/shelf remap loops duplicate the `ImportExportRouter.importJson` logic — extract `RelationRemapper`.
- [x] [perf] `cd.getAllTags().stream().filter(...)` runs per-book in the Calibre loop — load once into a HashMap.
- [x] [test] cover the "ISBN-10 with X check" path (already in CsvUtils) — assert imported as ISBN-13 in the importer integration.

## src/herramienta/Config.java
- [x] [div] split into typed views: `DbConfig`, `UiConfig`, `WindowConfig`, `PresetStore` — currently 50+ get/set pairs in one class.
- [x] [bug] `static { ... load }` happens at first class touch — but tests that override `user.home` AFTER class init still see the old config; provide a `Config.reload()` for tests.
- [x] [un] `parseInt(String,int)` defensively parses many properties; build a `propInt/propBool/propStr/propDouble` family once.
- [x] [perf] every setter triggers `save()` which schedules a 300ms-delayed write — fine, but bulk changes (theme + font + lang in one dialog) cause 3 writes; expose `withBatch(()->{...})`.
- [x] [clean] deletePreset() rewrites all preset keys — extract `PresetStore` with proper list semantics.

## src/herramienta/csv/CsvImportStrategy.java
- [x] [doc] javadoc the return: `true=imported, false=skipped (duplicate)`. Currently a one-line comment.
- [x] [div] add `int columnCountHint()` so callers can pre-size arrays.
- [x] [un] consider sealed interface + permits LibraryThing/Goodreads/Native — at 3 impls today, plus future Notion/Anybooks.
- [x] [test] add a CSV-fixture test ensuring each strategy refuses headers it shouldn't handle.
- [x] [clean] `throws Exception` is too broad — narrow to `IOException | NumberFormatException | BibliotecaException`.

## src/herramienta/csv/CsvUtils.java
- [x] [div] `parseLine` (RFC-4180) is the only non-trivial method; move it to a `Rfc4180Reader` class with streaming API to avoid loading whole files.
- [x] [bug] `parseLine` strips `\r` only when found in `indexOf` — defensive guard, kept; covers files-read-whole-and-split case.
- [x] [un] `parseIsbn` duplicates ISBN-10→13 conversion already in LlibreValidator; pick one canonical impl.
- [x] [perf] `parseLine` rebuilds `List<String>` per row → could reuse `String[]` with known column count (use `buildHeaderMap` result).
- [x] [test] edge cases: trailing comma, embedded `"` inside unquoted, BOM at start of header.

## src/herramienta/csv/GoodreadsCsvStrategy.java
- [x] [bug] only first author handled — Goodreads "Additional Authors" column dropped silently.
- [x] [un] shelfMap construction repeats in LibraryThing strategy — extract `loadShelfMap(cd)` helper.
- [x] [bug] valoracio multiplied by 2.0 (1-5 → 2-10) is fine, but ratings of 0 (unrated) still become 0; explicit check would document intent.
- [x] [clean] `if (CsvUtils.existsInLibrary(cd, isbn)) return false;` happens AFTER parsing — defer parsing of optional columns until known-new.
- [x] [test] fixture from a real Goodreads export.

## src/herramienta/csv/LibraryThingCsvStrategy.java
- [x] [bug] author-name flip `"Lastname, Firstname"` breaks for multi-comma cases ("van der Berg, Jan") — comment already acknowledges this; either implement properly or strip the heuristic.
- [x] [un] same shelfMap/tagMap pattern as GoodreadsCsv — share via base class.
- [x] [div] inline `try{Integer.parseInt}catch{}` for year — use DateUtils.parseYear.
- [x] [test] fixture: LibraryThing CSV with multiple authors, tags, and an X-check ISBN.
- [x] [perf] tagMap re-built per row — hoist to once-per-import.

## src/herramienta/csv/NativeCsvStrategy.java
- [x] [div] the inline shelf-membership parser is its own grammar (`name|val|llegit;name|val|llegit`); extract `ShelfMembershipParser`.
- [x] [bug] `Boolean.parseBoolean(c[7])` returns false for anything non-"true"; explicit "1"/"yes"/"y" acceptance would be friendlier.
^- [x] [un] consolidate with Native export format in BookExporter.exportCSV — keep both in sync via shared schema constant.
- [x] [clean] `canHandle` returns true (fallback) — fine, but consider name `FallbackCsvStrategy` to make role obvious.
^- [x] [test] roundtrip: export → re-import via Native; assert idempotent.

## src/herramienta/DateUtils.java
- [x] [un] both `parseYear` and date-string handling needed in Llibre setters — centralize all date parsing here.
- [x] [bug] `parseYear` regex accepts 1000-2199; relax/explicit boundaries (mainstream pubs are 1500-CURRENT_YEAR+5).
- [x] [div] add `parseIsoDate(String) -> LocalDate?` for callers currently doing `try{LocalDate.parse}catch{}`.
- [x] [test] property test: random YYYY-MM-DD strings reversed parsed correctly.

## src/herramienta/DialogoError.java
^- [x] [div] split validation dialog vs system-error dialog into two classes sharing a Header builder.
- [x] [un] `escHtml` repeated elsewhere (BookExporter.htmlEsc); consolidate.
- [x] [verify] log rotation uses `REPLACE_EXISTING` on `errors.log.1` — only one rotated generation is kept. Intentional but document it; users debugging an intermittent issue may need more history.
- [x] [perf] writeToLog opens FileWriter on every error — for error storms, buffer or batch.
- [x] [test] verify headless/test mode short-circuits before any Swing call.

## src/herramienta/FieldAutoComplete.java
- [x] [un] see [un] in AutoCompletion — both serve the same job.
- [x] [bug] DocumentListener has no removal path; even though `attach()` is called once per field, hot-reload (theme reset rebuilding fields) would leak.
- [x] [perf] popup item lookup iterates all suggestions on every keystroke; switch to a sorted `TreeSet<String>` + tail-set query for O(log n + k).
^- [x] [div] mouse-listener for each menu item could be one shared listener checking source.
^- [x] [test] verify popup hides on field-blur.

## src/herramienta/FiltreUtils.java
- [x] [un] this NFD-strip logic is needed in SQL search too (LlibreDao.search LIKE) — currently SQL is case-insensitive but accent-sensitive. Document gap.
- [x] [perf] cache normalized form of book fields on Llibre once, to avoid normalizing every haystack per filter call.
- [x] [div] extract `Normalizer.normalize(s)` as a public method for shared use.
- [x] [test] verify "Garcia" matches "García" via matchString.
- [x] [clean] private ctor — good.

## src/herramienta/I18n.java
- [x] [div] split `TABLE` into per-domain files (`strings_ui.csv`, `strings_errors.csv`, `strings_csv.csv`) so 600-line generated file becomes 3 smaller ones.
- [x] [bug] missing-key fallback returns the key itself — easy to ship broken UI; in dev mode log loudly.
- [x] [perf] LinkedHashMap with ~400 entries; switch to immutable `Map.copyOf()` after static init.
- [x] [un] add `Tx` for plural forms (count-dependent suffix); currently callers concat `value + (n>1 ? "s" : "")` in many places.
- [x] [test] verify every key has exactly 3 entries (ca/es/en) — automated check on `T()` calls.

## src/herramienta/LlibreValidator.java
- [x] [un] two entry points (`checkLlibre(Long,...)` and `checkLlibreFromString(String,...)`) duplicate ISBN-10→13 logic; extract `Isbn13Normalizer`.
- [x] [div] `validateExtras` vs `validateExtrasAll` — rename to `validateLengths(short)` / `validateLengths(full)` or drop the short version.
- [x] [bug] year range `1000..currentYear+5` excludes future-dated tour editions; widen to `+10` or make configurable.
- [x] [perf] `java.time.Year.now()` parses every call — cache or use `LocalDate.now().getYear()`.
- [x] [test] table-driven test covering each thrown branch with the localized message expected.

## src/herramienta/OpenLibraryClient.java
- [x] [div] split into `OpenLibraryClient` (network) + `OpenLibraryParser` (JSON → Map); current mix makes testing pure parsing hard.
- [x] [bug] `mergeGoogleBooks` only triggered when OpenLibrary returns no title — but if OpenLibrary returns a partial record (no descripcio), Google data is never merged.
- [x] [un] `fetch` and `fetchWithRetry` differ in retry only; collapse to one with `retries=0` default.
- [x] [perf] no HTTP cache; repeated ISBN lookups during covers fetch hit network. Add per-process LRU on responses.
- [x] [clean] `testBaseUrl/testMaxRetries/testRetryBaseMs` public mutable statics — wrap in a single record for thread-safety.

## src/herramienta/UITheme.java
- [x] [div] palette declaration (constants) vs application logic (`setTheme` switch) — move palettes into per-theme enum-bound records so adding a theme means one new enum case + record literal.
- [x] [un] big switch in setTheme assigns 13 fields × 4 cases (52 statements). With record-per-theme this drops to one apply() per theme.
- [x] [bug] `setTheme` warns when off-EDT but proceeds; if used in tests that's noisy. Provide `setThemeQuietly` for headless.
^- [x] [clean] public mutable static colors (BG_MAIN, ACCENT...) — callers cache references; on theme switch their cached value is stale. Replace with getters.
- [x] [test] verify rebuildFonts updates UIManager defaultFont so a re-applied L&F picks up the new size.

============================================================
interficie/
============================================================

## src/interficie/BibliotecaReader.java
^- [x] [bug] mixed `ArrayList<>` vs `List<>` return types — comment acknowledges this is for backward compatibility. Plan a migration: introduce `List<>` overloads, deprecate ArrayList ones, remove next major version.
- [x] [div] split read interface into `BookReader`, `ShelfReader`, `TagReader`, `LoanReader` (matching the eventual ControladorDomini split).
- [x] [un] consider Optional<Llibre> for `getLlibre` instead of throws.
- [x] [doc] document `aplicarFiltres` vs `searchLlibresSQL` — when does each get called? (The threshold logic is private in ControladorDomini.)
- [x] [test] interface mock (`InMemoryBibliotecaReader`) for fast UI tests.

## src/interficie/BibliotecaWriter.java
- [x] [div] same split as Reader.
- [x] [un] `deleteLlibre(Llibre)` vs `deleteLlibre(Long)` — pick one, deprecate the other.
- [x] [doc] note: methods marked `synchronized` in implementor are caller's contract — document.
^- [x] [test] contract tests reused between H2 and MariaDB backends.
- [x] [clean] `clearAll()` lives next to `backupToSQL/restoreFromSQL` — group "danger zone" methods with a sub-interface `BibliotecaAdmin`.

## src/interficie/EnActualizarBBDD.java
- [x] [div] composes OnLlibreUpdate + OnLlibreDelete — fine. Add `OnLlibreBlobChanged` for cover-update notifications too.
- [x] [bug] no method covers shelf membership changes — caller listens on full table refresh; add `onLlistaMembershipChanged(isbn, llistaId, added)`.
- [x] [doc] rename: Catalan/Spanish hybrid "EnActualizarBBDD" → `LibraryEvents` (English public API).
^- [x] [un] consider replacing with EventBus pattern if callback count grows.
- [x] [test] mock listener verifying `nuevo` flag passed correctly on update vs insert.

## src/interficie/OnLlibreDelete.java
- [x] [div] add a `boolean cancellable` mode (e.g., dependency check) — currently delete cannot be vetoed.
- [x] [un] merge into a single `LibraryEvents` interface (see EnActualizarBBDD).
- [x] [doc] who calls this and from which thread? (EDT vs background).
- [x] [test] verify the listener invoked exactly once per delete.

## src/interficie/OnLlibreUpdate.java
- [x] [div] separate `OnAdded` from `OnUpdated` — `boolean nuevo` is a code smell.
- [x] [un] see [un] in OnLlibreDelete.
- [x] [doc] document that Llibre passed is the new state, not the old.
- [x] [test] verify both branches (`nuevo=true` and `nuevo=false`) call the listener.

============================================================
main/
============================================================

## src/main/Ejecutable.java
- [x] [div] split `startWeb` and `startSwingWithSplash` into separate launcher classes; main just dispatches.
- [x] [bug] `Thread.setDefaultUncaughtExceptionHandler` uses `new herramienta.DialogoError(..., (Exception)(e instanceof Exception ? e : new RuntimeException(e)))` — Errors (OOM) wrapped as RuntimeException lose the original cause chain. (now wraps Errors properly)
- [x] [un] both modes set up shutdown hooks; consolidate hook registration in one helper.
- [x] [clean] `resolveMode` returning null vs `Mode.CANCELLED` is awkward — make it return `Optional<Mode>`.
- [x] [test] verify `--web`/`--swing` arg parsing and persistence to Config.lastMode.

============================================================
persistencia/
============================================================

## src/persistencia/AutorDao.java
- [x] [un] three near-identical `try{Statement s=…;ResultSet rs=…executeQuery(SQL)}` blocks; extract `<T> List<T> queryAll(String sql, RowMapper<T>)`.
- [x] [bug] errors logged with `System.err.println` then swallowed — caller gets empty list and can't tell apart "no rows" from "error".
- [x] [div] move SQL strings to top-of-class constants for visibility.
- [x] [perf] missing index on `autor(nom)` would slow distinct queries on large DBs — verify migration adds one (`UNIQUE` on nom should suffice).
- [x] [test] paramterized test with 0/1/many authors.

## src/persistencia/AutorRow.java
- [x] [doc] document that id is db-assigned auto-increment.
- [x] [un] consider `equals` by nom (autors should dedupe by name across migrations).

## src/persistencia/ConnectionConfig.java
- [x] [verify] toString masks password — good. But Java records auto-toString uses fields; override is correct, just verify no library reflection bypasses it (Gson would).
- [x] [doc] `profile` and `testUrl` semantics — when is each set/used?
- [x] [div] separate `H2Config` and `MariaDbConfig` records under a sealed interface — host/user/password meaningless for H2.
- [x] [un] config knobs split between this record + `Config` (the static): unify via a single `Config.connection()` returning ConnectionConfig.
- [x] [test] verify toString never reveals password even via reflection.

## src/persistencia/ControladorPersistencia.java
- [x] [div] this class is a thin delegator — replace with `Persistencia(con).libreDao()/llistaDao()/...` accessor pattern, drop the wrappers.
- [x] [un] every method is `synchronized` and just delegates; if all DAOs are also synchronized, double-locking adds no safety.
- [x] [bug] `resetForTest()` calls `inst.libreDao.clearAllData()` swallowing exceptions — a test that fails to reset may quietly run on dirty state.
- [x] [clean] `getInstance()` + 3 reset methods (`resetForTest`, `resetForProfileSwitch`, the shutdown hook in Ejecutable) — clarify lifecycle.
- [x] [test] verify resetForProfileSwitch closes the previous connection before discarding the reference.

## src/persistencia/LecturaRow.java
- [x] [bug] `dataInici/dataFi` as String — see PrestecRow same issue.
- [x] [doc] javadoc — what is `paginesLlegides` semantically (cumulative or per-session)?
^- [x] [test] roundtrip via JDBC.

## src/persistencia/LlibreAutorRow.java
- [x] [doc] note that `(isbn, autorId)` is PK in the join table.
- [x] [bug] uniqueness not enforced at record level — fine, DB enforces it.
- [x] [un] consider folding into a `LibraryGraph` aggregate that loads all relations.

## src/persistencia/LlibreDao.java
^- [x] [div] split into `LlibreReadDao` (getAll, search, getRecentlyAdded) and `LlibreWriteDao` (insert/update/delete/blob/clear) — currently 393 lines and growing.
^- [x] [un] `LLIBRE_SELECT` vs `LLIBRE_SELECT_L` differ only by table alias — generate from a single template.
- [x] [bug] `insert()` and `update()` set 27 PreparedStatement params each in two near-identical blocks; one slip-up between them is a silent data bug. Extract a `bindLlibre(ps, ll, includeIsbn)` helper.
^- [x] [perf] `getAll()` does GROUP_CONCAT in the SELECT subquery AND joins llibre_autor in the outer; redundant. Pick one strategy.
- [x] [test] verify `search()` parameter binding for every field type (String/Long/Integer/Double/Boolean).

## src/persistencia/LlistaDao.java
^- [x] [div] separate "shelf CRUD" from "membership CRUD" (`addLlibre/removeLlibre/updateLlibre/getISBNsInLlista`) into two DAOs.
^- [x] [un] `getLlibres.*builds a full Llibre SELECT inline — share `LlibreDao.LLIBRE_SELECT_L` instead of duplicating columns.
- [x] [bug] `create()` uses `INSERT INTO llista (nom, ordre) VALUES (?, SELECT COALESCE(MAX(ordre),0)+1 FROM llista AS sub)` — method is already `synchronized`, so single-JVM safe. Cross-process MariaDB writes still race; either wrap in a SERIALIZABLE transaction or compute `ordre` from a sequence.
^- [x] [perf] `getAllCounts()` group-by — useful, but `getCount(llistaId)` for one shelf could be served from the cached `getAllCounts()` result.
- [x] [test] verify color set to null clears and hex roundtrips.

## src/persistencia/PrestecDao.java
- [x] [verify] `getOverdue()` uses `data_prestec < ?` with a `java.sql.Date` cutoff — works on H2 and MariaDB. Confirm timezone semantics (LocalDate.now() uses system zone; loan dates use CURRENT_DATE).
- [x] [un] `getAll`, `getLoanedISBNs`, `getForIsbn` share the column projection pattern — RowMapper helper.
- [x] [div] add `count(isbn)` for showing "loaned N times" in book detail.
- [x] [perf] `getLoanedISBNs()` returns ALL non-returned loans; with thousands could be slow if not indexed on (retornat, isbn).
- [x] [test] verify returnLoan() of an already-returned loan throws (it currently does via 0-row-update guard).

## src/persistencia/ServerConect.java
- [x] [div] separate `ConnectionFactory` (driver loading, URL building) from `SchemaManager` (migrations).
- [x] [bug] migrations 32-34 (autor table migration + drop column) cannot be re-run on a partially-migrated DB without specific data — document the irreversible step.
- [x] [un] `findLibDir()` walks 5 strategies — extract as `LibDirResolver` with one method per strategy and a sequence.
- [x] [perf] CREATE_TABLE + 37 migrations apply sequentially in a single transaction; for large libraries the `ALTER TABLE ADD COLUMN` cascade is slow. Document expected startup cost.
- [x] [test] migration test: start from CREATE_TABLE only, apply all migrations, verify final schema equals a known reference.

## src/persistencia/TagDao.java
- [x] [un] `getDistinctValues(column)` lives here but is about Llibre columns, not Tag — move to LlibreDao.
- [x] [bug] `AUTOCOMPLETE_COLUMNS` whitelist duplicates `MetaRouter.ALLOWED_DISTINCT` — keep a single constant.
- [x] [div] same RowMapper extraction as AutorDao.
- [x] [perf] add index on `tag(nom)` for unique check on insert (already UNIQUE? confirm migration 12).
- [x] [test] rename to existing nom should throw via unique constraint.

============================================================
presentacio/
============================================================

## src/presentacio/AboutDialog.java
- [x] [bug] hard-coded GitHub URL `https://github.com/jaume-casals/Biblioteca` vs `Makefile`/installer.nsi which use `jordic` — make it a constant.
- [x] [div] `addRow` is a method-helper; small as is. No split needed.
- [x] [un] license text duplicated in installer.nsi and here — load from `/LICENSE.txt` resource.
- [x] [clean] `lblSub.setHorizontalAlignment` called after styleLabel — chain order: style then customize.
- [x] [test] verify dialog disposes on Escape (registered, but Swing tests usually skip — at least add headless smoke).

## src/presentacio/BookIOController.java
- [x] [div] split into `ImportController`, `ExportController`, `BackupController` — three responsibilities currently bundled.
- [x] [un] every method opens a JFileChooser with extension filter; extract `pickFile(open|save, ext, desc)` once. (pickFile helper added)
- [x] [verify] `escanejarISBN()` registers a window-listener AFTER `setVisible(true)` — listener race possible (rare but possible).
- [x] [perf] `fetchMissingCovers` spawns a fixed pool of 8 threads — for tiny "missing" lists this overshoots; size pool to `min(8, missing.size())`.
- [x] [test] verify export/import roundtrip preserves shelf memberships.

## src/presentacio/ConfiguracioDialog.java
- [x] [div] absolute positioning (`setBounds(lx, 48, …)`) breaks at non-default font sizes. Migrate to GroupLayout or MigLayout.
- [x] [un] save action builds Config one setter at a time; same field-list appears in ConfigRouter — share via a `ConfigDTO` record.
- [x] [verify] when DB type switches between H2 and MariaDB the dialog says "restart needed" but does not enforce it; saved state might be wrong on next launch.
- [x] [desp] giant linear constructor — split per section (`buildDbSection`, `buildAppearanceSection`, ...).
- [x] [test] verify saving with H2 type doesn't store stale host/user (Config retains old values).

## src/presentacio/detalles/control/DetallesLlibrePanelControl.java
- [x] [div] split `editar()` (form-to-domain) from `eliminar()` (delete flow) from `mostrarHistorialPrestecs()` (history dialog) — three concerns in one class. (extracted inner classes)
- [x] [un] field-binding code (`getText/setText` for each Llibre field) repeats in GuardarLlibresDialogoControl — extract `LlibreFormBinder`.
- [x] [bug] dialog title not refreshed after edit (fixed prior session, but no regression test) — add headless verification.
- [x] [perf] `imatgeWorker` cancellation logic is correct but verbose; the SwingWorker pattern could be simpler with a Future + cancel-on-close.
- [x] [test] cover the "save → updateLlibre → callback" happy path.

## src/presentacio/detalles/control/GuardarLlibresDialogoControl.java
- [x] [div] `cercaInternet()` mixes network, EDT marshalling, and UI mutation; split into `OpenLibrarySearchTask` (SwingWorker) + UI applier.
- [x] [un] form-binding mirrors DetallesLlibrePanelControl — same `LlibreFormBinder` candidate.
- [x] [verify] `crearLlibre()` parses autor list using `split(",")` — but `setAutors(List<String>)` re-joins with `, ` so roundtrip OK; double-check `setAutors` semantic with whitespace.
- [x] [clean] empty WindowListener methods could be replaced with `WindowAdapter`.
- [x] [test] simulate `cercaInternet` returning error key and verify dialog stays open with restored button text.

## src/presentacio/detalles/vista/DetallesLlibrePanel.java
- [x] [div] 27 fields manually declared + 27 getters — generate via reflection-backed `Field<T>` registry.
- [x] [un] entry-builder pattern (`addFieldEntry`, `addComboEntry`, `addCheckEntry`) mirrors GuardarLlibresDialogo — share via `FormGridBuilder`.
- [x] [verify] `getTextAny().setText(l.getAny().toString())` in Control NPEs if any is null — verify (Llibre default = 0, not null, so safe today, but fragile).
- [x] [clean] absolute positioning gone (GridLayout used) — good. But notes scroll has hard-coded height 4 rows.
- [x] [test] verify hide/show of optional fields (nomCa/Es/En) when blank.

## src/presentacio/detalles/vista/GuardarLlibresDialogo.java
- [x] [div] same field-registry refactor as DetallesLlibrePanel.
- [x] [un] entry-builder methods duplicated — extract to a shared `FormGridBuilder`.
- [x] [bug] dialog is modal but search thread keeps running after dispose — Control handles via interrupt; ensure no race when user closes while thread mid-update.
- [x] [perf] `setSize(600, 720)` after `setPreferredSize` and `pack` not called — verify intended.
- [x] [test] ensure each form field has a label association for screen readers.

## src/presentacio/detalles/vista/LlistesDelLlibreDialog.java
- [x] [div] table-model + add-row + remove-row logic could move to `LlistesDelLlibreTableModel`.
- [x] [un] same comboAdd-reload pattern as TagsDelLlibreDialog — share `entitySelectCombo()`.
- [x] [bug] `findLlistaByNom()` collides when two shelves have the same name — use id stored as table column instead.
- [x] [perf] `reload()` re-fetches all shelves via `cd.getAllLlistes()` every time — cache or invalidate selectively.
- [x] [test] verify rating column editor coerces non-numeric input to 0.

## src/presentacio/detalles/vista/TagsDelLlibreDialog.java
- [x] [div] add-row + remove-row + create-new are three buttons that share state plumbing — extract `TagSelectionPanel`. (extracted TagsDelLlibreControl instead)
- [x] [un] mirror of LlistesDelLlibreDialog — see `entitySelectCombo()` proposal.
- [x] [bug] adding a tag that already exists triggers a JOptionPane but `reloadComboAdd()` runs first — order swap would avoid the flash.
- [x] [perf] same reload-per-action overhead as LlistesDelLlibreDialog.
- [x] [test] verify enter-key on the new-tag field triggers create (currently only the button).

## src/presentacio/EstadistiquesHelper.java
- [x] [div] one method per chart with anonymous JPanel subclass — extract `ChartPanel` superclass. (BookStats already exists)
- [x] [un] year-grouping logic duplicated between `buildReadingChart` and `buildReadingPacePanel` — share `booksByReadYear()`.
- [x] [perf] each chart re-iterates `books`; build a `BookStats` aggregate once and feed it to all charts.
- [x] [bug] `String.format("%.1f", 100.0 * llegits / total)` divides by zero when total == 0 — guard.
- [x] [test] golden snapshot of `buildStatsSummary` for a fixed library.

## src/presentacio/GaleriaCobertesPanel.java
- [x] [div] split into GaleriaCobertesPanel (layout) + GaleriaController (selection/keyboard) + GaleriaCard (per-card paint).
- [x] [desp]paintComponent of cards uses anonymous classes with captured maps — extract a `CardModel` record holding all per-card state.
- [x] [perf] image cache `LinkedHashMap` capped at 150 — for 5000-book libraries scrolling causes constant evict-and-reload. Tier the cache (L1 small, L2 disk).
- [x] [verify] `zoomTimer`/`zoomPopup` for hover preview — disposed on `hide()`? Verify no leaked window on rapid hover.
- [x] [test] verify selection survives a zoom change.

## src/presentacio/GestioLlistesDialog.java
- [x] [div] inner ActionListeners for add/remove/rename/color/up/down clutter constructor — bind via method references to private methods.
- [x] [un] color hex formatting `String.format("#%02X%02X%02X", r, g, b)` duplicated elsewhere — `ColorUtils.toHex(Color)`.
- [x] [verify] reorder re-loads list AND calls `mainControl.refreshComboLlistes()` — possible double-refresh; debounce.
- [x] [clean] `colorSwatch` anonymous Icon — extract.
- [x] [test] verify deleting shelf with books shows confirmation and cascades correctly.

## src/presentacio/MainFrameControl.java
- [x] [div] keyboard-shortcut wiring (10+ blocks) → extract `KeyboardShortcuts.install(rootPane, control)`.
- [x] [un] each `im.put(...) + am.put(... new AbstractAction)` repeated 10×; use a `bind(KeyStroke, Runnable)` helper.
- [x] [bug] `getInstance(MainFramePanel,…)` lazy-inits; `getInstance(MainFramePanel)` throws if not init — confusing trio; collapse to one entry point.
- [x] [clean] `mostrarAlertes()` swallows exceptions in two try/catch blocks; log instead of `ignored`.
- [x] [test] verify Ctrl+N triggers the new-book dialog on the EDT.

## src/presentacio/MainFramePanel.java
- [x] [un] hardcoded color `0xFF8080` for sidebar exit button (in MostrarBibliotecaPanel) belongs in UITheme — also relevant if MainFramePanel grows.
- [x] [div] currently small — fine.
- [x] [bug] `setDefaultCloseOperation(DO_NOTHING_ON_CLOSE)` ties exit to MainFrameControl's listener; if the listener throws on close, the user is stuck. Catch defensively.
- [x] [clean] `pack()` after explicit setBounds — pack overrides? Verify intent.
- [x] [test] verify Config-driven window placement is applied on first launch.

## src/presentacio/ModeSelectorDialog.java
- [x] [div] custom-painted JButton subclass inside `buildModeButton` — extract `ModeButton extends JButton`.
- [x] [un] colors `0x7AA2F7`, `0x9ECE6A`, `0x24283A`... duplicate UITheme palette — use UITheme constants.
- [x] [bug] `prompt(String defaultMode)` short-circuits without opening dialog when previous mode is set — user can never get the chooser back. Add Shift-click on app icon or a "--ask" arg.
- [x] [perf] inner panel allocation in paintComponent — fine; static EmptyBorder reuse.
- [x] [test] verify Escape cancels, Enter picks default.

## src/presentacio/MostrarBibliotecaControl.java
- [x] [div] god-class — split into `TableController`, `FilterController`, `ShelfController`, `IOController`, `ContextMenuController`. 1500 lines is unmaintainable.
- [x] [un] cover-cache (`coverCache`, `coverLoading`) duplicated in GaleriaCobertesPanel — single shared CoverService.
- [x] [bug] `currentLlistaId` mutated from multiple action listeners without lock — concurrency on EDT only, but new background-fetch callbacks could race.
- [x] [perf] table model rebuilt from scratch on every filter — DefaultTableModel + setRowCount(0) + addRow per book; for 10k rows this is slow. Use AbstractTableModel with backing list.
- [x] [test] add column-toggle persistence test (hidden cols restored on next launch).

## src/presentacio/MostrarBibliotecaPanel.java
- [x] [div] split into `LeftSidebarPanel`, `TopBarPanel`, `FilterDrawerPanel`, `CenterTablePanel`, `GaleriaContainer` — currently one mega-panel.
- [x] [un] `makeSidebarBtn` + theme-button re-styling + scrollbar-naming work centralizes look — extract a `Sidebar` widget.
- [x] [bug] `applyTheme()` reinstalls Nimbus then re-styles only `sidebarBtns` and shelves; check that filter-drawer custom-painted buttons (presets) also get re-themed.
- [x] [perf] `updateComponentTreeUI(w)` is heavy; restrict to subtree being themed. (changed to use `this`)
- [x] [test] verify column widths/visibility round-trip via Config.

## src/presentacio/renderers/TableCellComponents.java
- [x] [div] file holds 5 inner classes — split into one file per renderer/editor under `presentacio.renderers`.
- [x] [un] `setBackground(selected ? UITheme.ACCENT : UITheme.BG_PANEL)` pattern repeats; extract `applySelectionBg(comp, selected)`.
- [x] [bug] `LlegitCheckBoxEditor` compares display strings (`I18n.t("filter_read")`) — if locale changes mid-session the cell value won't match. Use a boolean column model instead.
- [x] [perf] `CoverCellRenderer` spawns a SwingWorker per missed-cache cell — paginated tables with 100 rows can fire 100 workers simultaneously; bound with a single `ExecutorService`. (static LOADER pool added)
- [x] [test] renderer headless test: render a Llibre row, assert text/icon outputs.

## src/presentacio/SplashScreen.java
- [x] [bug] `show()` records `showTime` but `MIN_DISPLAY_MS=500` is only honored on `hide()`; if app crashes between, no cleanup.
- [x] [un] simple dialog wrapper — could reuse a `LoadingDialog` shared with `BookIOController.fetchMissingCovers`.
- [x] [div] small enough; leave intact.
- [x] [perf] `javax.swing.Timer` for the 500ms minimum — fine, but could be a single shared scheduler.
- [x] [test] verify hide() before show() is a no-op.

## src/presentacio/TablePageController.java
- [x] [div] computed-state (`paginatedMode`, `currentPage`, `useDBPagination`) interleaved with view mutation — split into `PaginationModel` + `PaginationView`.
- [x] [bug] `showPage(int, ArrayList<Llibre>)` accepts null biblio to mean "empty" — surprising; prefer `Collections.emptyList()` default at callers.
- [x] [un] page-info text `I18n.t("page_info_java", page+1, totalPages)` repeated in DB and in-memory branches; unify after computing page+totalPages.
- [x] [perf] DB-pagination path calls `cd.countLlibresDB()` + `cd.getLlibresPage()` on every page click; consider caching count for the duration of one filter. (cachedTotalCount in TablePageController)
- [x] [test] verify `currentPage` clamps to [0, totalPages-1] on filter shrink.

============================================================
Cross-cutting (root-level themes spanning many files)
============================================================
- [x] [un] ARRAYLIST-VS-LIST: 50+ public APIs return ArrayList<>; migrate to List<> over a major version (BibliotecaReader comment already flags this).
- [x] [un] CALLBACK INTERFACES: EnActualizarBBDD + OnLlibreUpdate + OnLlibreDelete → single LibraryEvents.
- [x] [un] FORM-BINDING: two big dialogs (Detalles/Guardar) share ~80% field handling — `LlibreFormBinder`.
- [x] [un] ESCAPE HELPERS: htmlEsc/jsonStr/csvQ/sqlEsc scattered in 4 files → one `Escapers` utility.
- [x] [un] OPENLIBRARY CLIENT: parsing concerns mixed with network — split testable.
- [x] [un] ROW MAPPERS: each DAO redoes try/Statement/ResultSet patterns — `<T> List<T> queryAll(...)` helper.
- [x] [div] DOMAIN GOD-CLASSES: ControladorDomini, ControladorPersistencia, MostrarBibliotecaControl, MostrarBibliotecaPanel — split per concern (Book/Shelf/Tag/Loan). (UI: coordinator + sub-controllers/panels done; domain/persistencia deferred)
- [x] [div] LOCALIZATION: 600-line I18n.java is generated; split CSV into 3 files (ui/errors/csv) to ease review.
- [x] [div] RENDERERS: TableCellComponents.java has 5 inner classes — one file per public renderer.
- [x] [div] EXPORTER FAN-OUT: BookExporter (CSV/JSON/HTML/PDF) → 4 files under `herramienta.export`.
- [x] [desp] FILTER PATH: in-memory vs SQL path divergence in ControladorDomini.aplicarFiltres — unify so sort + filter behave identically.
- [x] [desp]THEME APPLY: MostrarBibliotecaPanel.applyTheme() reinstalls Nimbus then patches subset of components — instead, scope theme changes to a subtree.
- [x] [desp] COVER FETCHING: BookIOController.fetchMissingCovers, ImportExportRouter.fetchCovers, GaleriaCobertesPanel image loading, CoverCellRenderer — four separate fetchers; one `CoverService` with prioritized queue would replace all.
- [x] [test] integration: build a fixture library (50 books, 5 shelves, 10 tags, 3 active loans), exercise CRUD + export/import roundtrip.
- [x] [test] migration: add a regression test that verifies every Migration in ServerConect.MIGRATIONS is idempotent (re-applying schema_version-recorded migrations is a no-op).
- [x] [test] H2 vs MariaDB parity: every DAO method should behave the same under both backends — wire a JUnit5 ParameterizedTest with both URLs.
- [x] [perf] CONFIG.save() debounce already exists; verify all callers use it correctly (no .save() in tight loops).
- [x] [doc] file-by-file javadoc summarizing the role; current state has uneven coverage (a few have //-comments, most have nothing).
- [x] [clean] Hungarian "Catalan/Spanish hybrid" naming — pick one. Public API should be English; internal Catalan/Spanish kept where domain terminology demands (Llibre, Llista, prestatge).
- [x] [bug] every DAO swallows SQLException with System.err.println — propagate or wrap consistently (BibliotecaException with cause) so callers can react.
- [x] [desp]ISBN is used as both Long (domain) and String (table cells, JSON) — pick one canonical representation and convert only at boundaries. (Partially addressed: LlibreLlistaRow uses Long isbn, PrestecRow uses long isbn; JSON layer handles conversion)
- [x] [desp] BibliotecaWriter is the single interface for everything — the API, UI, and backup all depend on it; create narrower read-only snapshots for export/backup to avoid accidental mutations.
- [x] [perf] On startup, getAllLlibres() loads all books including their metadata (notes, descriptions) but the table only shows 6 columns — lazy-load the heavy fields (notes, descripcio, imatgeBlob) on first access.

============================================================
APPENDIX — UNIQUE ITEMS FROM todo.txt (deduped 2026-05-17)
============================================================
# Items below appear in todo.txt's [ ] list AND don't duplicate
# the main body above. ~70 dupes stripped after main-body cross-check.

## src/api/ApiServer.java
- [x] [desp] Move port to a constant or Config property instead of constructor parameter

## src/api/HttpCtx.java
- [x] [desp] CORS headers are set in HttpRouter.dispatch() but response headers like Cache-Control are set in individual routers via responseHeader() — centralize CORS header addition in commit()

## src/api/ConfigRouter.java
- [x] [div]ConfigRouter mixes DB config with UI config; consider splitting into /api/config/ui and /api/config/db or at least document which fields require restart

## src/api/LoanRouter.java
- [x] [clean]   loan() re-reads getLoanedISBNs() to check for duplicates — this is a Set lookup but it queries the DB each time; cache or use cd.existsLlibre() pattern

## src/api/LlistaRouter.java
- [x] [desp] books() returns full libro JSON including all 25 fields; for shelf view the client usually only needs title, author, ISBN — add a ?fields= param or a slim projection

## src/domini/ControladorDomini.java
- [x] [desp] getDistinctValues() has two paths (in-memory stream vs cp.getDistinctValues()); the in-memory path covers "editorial","serie","idioma","format","pais_origen","llengua_original" but the SQL path is used for everything else — document this split or unify

## src/domini/Llibre.java
- [x] [clean]   Primitive int fields (pagines, volum, exemplars) mixed with boxed Integer/Double/Boolean fields — be consistent; use primitives for always-present fields (pagines is always 0, never null)
- [x] [clean]   imatgeBlob byte[] is stored in-memory on the domain object and loaded on demand — for large libraries this wastes heap; mark it as a lazy-loaded field and add a clear comment

## src/domini/LlibreFilter.java
- [x] [clean]   LlibreFilter.empty() is just `new LlibreFilter()` — this factory is only useful if the constructor becomes non-public (which it should be with a builder)

## src/domini/Llista.java
- [x] [desp]ordre is set via setOrdre() but the ordering is maintained by the DB and in-memory list position — the field on the object is redundant for in-memory use; clarify or remove

## src/domini/Tag.java
- [x] [clean]   Tag has no color field but Llista does; if tags should also get colors in the future, plan the field addition now

## src/domini/BibliotecaException.java
- [x] [clean]   Class is package-private in usage but public — decide and document the visibility contract
- [x] [clean]   RuntimeException subclass means callers don't need to declare it — document this is intentional and why (Swing handlers can't throw checked exceptions)

## src/domini/LlibreLlistaRow.java / LlibreTagRow.java / PrestecRow.java
- [x] [desp]These records are in domini but they're pure data transfer objects from persistencia — move to persistencia package alongside AutorRow, LecturaRow, LlibreAutorRow for consistency
- [x] [clean] LlibreLlistaRow uses primitives (long isbn, int llistaId) while PrestecRow uses Long isbn — make primitive usage consistent across all row types

## src/persistencia/ControladorPersistencia.java
- [x] [desp]searchLlibres() is the only method that accepts a LlibreFilter — the rest of the LlibreDao methods could be unified under search() with an empty filter; reduce the surface area

## src/persistencia/LlibreDao.java
- [x] [perf]    syncAutors() does DELETE + N×(INSERT IGNORE + SELECT) for every book save — use a single MERGE/UPSERT or batch the inserts; for books with many authors this is N+2 round trips

## src/persistencia/LlistaDao.java
- [x] [clean]   All methods are synchronized but they're called through ControladorPersistencia which is also synchronized — double locking; remove synchronized from DAO methods and rely solely on CP's lock

## src/persistencia/TagDao.java
- [x] [desp] getDistinctValues() has dynamic column name SQL even though it's guarded by AUTOCOMPLETE_COLUMNS whitelist — document why the dynamic approach is safe or use a Map<String, String> of allowed columns to their queries
- [x] [desp] getAllLlibreTag() and getLlibresWithTag() can both be called in the same request (stats + filter); consider caching the full set in a lazy-loaded Map

## src/persistencia/PrestecDao.java
- [x] [desp] add() doesn't validate that the isbn exists in the llibre table — DB FK constraint handles it but the error message is an SQL constraint message; add an explicit check
- [x] [desp] getAll() is called by BackupService but never by the API or UI for display — consider making it package-private and adding a getActivePrestecs() public method for the UI

## src/persistencia/AutorDao.java
- [x] [desp]No createAutor() / updateAutor() / deleteAutor() — only bulk readers; all mutations go through LlibreDao.syncAutors(); consider adding explicit CRUD here and removing the syncAutors() complexity from LlibreDao
- [x] [clean]   getAllLlibreAutor() is only used by BackupService — make it package-private or move it to BackupService's direct use
- [x] [clean]   Synchronized on every method but ControladorPersistencia also locks — remove double sync (see LlistaDao note)

## src/persistencia/ConnectionConfig.java / AutorRow.java / LecturaRow.java / LlibreAutorRow.java
- [x] [desp] AutorRow and LlibreAutorRow are only used by BackupService; consider co-locating them with BackupService or making package-private
- [x] [desp] LecturaRow is returned by ControladorPersistencia.getAllLectures() which is never called from the UI — either wire it up to a reading history feature or mark as TODO

## src/herramienta/Config.java
- [x] [clean]   getDbPassword() returns empty string if not set — callers can't distinguish "not configured" from "empty password"; return Optional<String> or null

## src/herramienta/BackupService.java
- [x] [desp] backupToSQL() and the clearAll()+restore SQL execution in LlibreDao are two places that know about table ordering (FK constraints); centralize the table order in one constant
- [x] [clean]   BackupService is constructed with ControladorPersistencia but ControladorDomini also uses it — ControladorDomini passes `cp` but cd itself has allLlibres; passing cp separately creates a potential desync; pass cd instead

## src/herramienta/FiltreUtils.java
- [x] [clean]   Both methods take Long/String but the null checks are inconsistent — matchISBN returns false on null inputs (correct) but matchString returns false on null haystack which could be a legitimate "no value" case; document the null contract
- [x] [desp] Add matchStringContainsAll(String query, String haystack) for multi-word search ("tolkien ring" finds "The Lord of the Rings, Tolkien")
- [x] [clean]   This is a utility class with only 2 methods used in 3 files — either expand into a proper SearchUtils or fold the methods into ControladorDomini and remove the class

## src/herramienta/LlibreValidator.java
- [x] [desp] countDig() uses String.valueOf(n).length() — works for positive longs but 0 returns length 1; add a comment or use Math.floor(Math.log10(n))+1 with a special case for 0
- [x] [desp]Validator builds and returns a new Llibre — but the caller must then manually copy the extra fields (notes, pagines, etc.) back from the original; validator should accept a Llibre and validate in-place, or return a validated wrapper

## src/herramienta/DialogoError.java
- [x] [clean]   escHtml() doesn't escape quotes for use inside HTML attribute values — already escapes &quot;
- [x] [desp]showErrorMessage() checks GraphicsEnvironment.isHeadless() — this check should happen once at startup and cached, not on every error
- [x] [clean]   validationDialog uses "⚠" emoji as a label character — on some systems this renders as empty box; use a proper ImageIcon or UIManager.getIcon("OptionPane.warningIcon")

## src/herramienta/DateUtils.java
- [x] [clean]   Both methods are one-liners; this class is too thin — either merge into a fuller DateUtils with more methods or inline the callers and delete the class
- [x] [desp] Add formatDateForDisplay(String isoDate, Locale locale) — currently dates are displayed as raw ISO strings everywhere in the UI
- [x] [clean]   No tests for DateUtils — edge cases like null, empty string, partial dates all need coverage

## src/herramienta/I18n.java
- [x] [clean]   t(key, args...) format uses String.format() — if the key has no %s/%d placeholders and args are passed, they're silently ignored; add a debug assertion in dev mode
- [x] [desp]The language index (0=ca, 1=es, 2=en) is a magic number — use an enum Lang and look up by ordinal; adding a new language requires changing every T() call

## src/herramienta/UITheme.java
- [x] [desp]Theme is an enum with 4 values (LIGHT, DARK, SEPIA, OCEAN) but only 2 are surfaced in the mode selector UI — document which themes are "public" vs internal
- [x] [clean]   rebuildFonts() builds fonts from a "small"/"medium"/"large" string — magic strings; use FontSize enum to avoid typos and enable exhaustive switch
- [x] [desp]styleAccentButton(), styleSecondaryButton(), styleLabel() are theming utilities scattered in UITheme but used exclusively in presentacio — consider a UIComponents factory in presentacio that uses UITheme constants without having UITheme know about Swing components

## src/herramienta/OpenLibraryClient.java
- [x] [desp] HTTP calls are synchronous on whatever thread calls them — lookupByISBN() is called from LlibreRouter in the API server thread; use CompletableFuture or add a timeout
- [x] [desp] fetchCoverByISBN() tries multiple cover sizes — document the fallback order and add a constant for the base URL so it's easy to mock in tests
- [x] [clean]   The OL client has no rate limiting — bulk cover fetches can trigger OL's rate limiter; add a Semaphore or delay between requests

## src/herramienta/BookExporter.java
- [x] [desp] exportHTML() has 200 lines of inline CSS — extract to a template or resource file
- [x] [desp] exportCSV() and the Goodreads CSV export in ImportExportRouter are separate implementations — unify into one GoodreadsExportService

## src/herramienta/AutoCompletion.java
- [x] [clean]   Delete key is consumed and replaced with a beep — verified no delete-key block exists; only backspace-at-offset-0 beeps (standard behavior)
- [x] [clean]   Package-level fields (no access modifier) — should be private; this is a leftover from the public domain source
- [x] [desp]Uses old anonymous class style for ActionListener, PropertyChangeListener, KeyAdapter, FocusAdapter — convert to lambdas for readability

## src/herramienta/FieldAutoComplete.java
- [x] [clean]   MAX_SUGGESTIONS is 8 — promoted to public constant; callers can reference it

## src/herramienta/csv/CsvUtils.java
- [x] [desp] parseLine() builds a new StringBuilder per field — reuse a single StringBuilder (already implemented)
- [x] [clean]   colVal() returns "" for missing or out-of-range columns — callers can't distinguish "column missing in header" from "column exists but empty value"; documented in javadoc.
- [x] [desp] buildHeaderMap() trims header names but parseLine() doesn't trim field values — trimming happens in colVal(); this inconsistency can cause "Author " != "Author" bugs if a CSV has trailing spaces in header but not values

## src/herramienta/csv/GoodreadsCsvStrategy.java
- [x] [desp] canHandle() checks for "Book Id" OR "Exclusive Shelf" — brittle string matching; add a minimum column count check

## src/herramienta/csv/NativeCsvStrategy.java
- [x] [desp] Column offsets are magic numbers (c[0]=isbn, c[1]=nom, etc.) — define constants or use hMap even for native format

## src/herramienta/csv/LibraryThingCsvStrategy.java
- [x] [clean]   ISBN extraction with replaceAll("[^0-9]", "") — LibraryThing wraps ISBNs in brackets like "[9781234567890]"; the regex handles it but this isn't documented

## src/main/Ejecutable.java
- [x] [clean]   "Biblioteca running at http://localhost:" + port is hardcoded English in a multilingual app — use I18n or at least a constant

## src/presentacio/MainFrameControl.java
- [x] [desp]Singleton with static instance — same testability issue as ControladorDomini; at minimum document that it's a singleton and why
- [x] [clean]   actualitzarLlibre() calls addLlibreToLlista when nuevo=true and currentLlistaId != null — this is shelf auto-assignment on new book; the logic is buried in the callback; make it explicit

## src/presentacio/MostrarBibliotecaControl.java
- [x] [div]Statistics dialog (~150 lines in mostrarEstadistiques()) is already delegated to EstadistiquesHelper but the dialog construction is inline — move dialog wiring into EstadistiquesHelper.showDialog(parent, cd)
- [x] [bug]     filtrar() line 1107: `ArrayList<Llibre> base = currentLlistaId != null ? biblio : biblio;` — ternary always returns biblio; the condition is dead code; fix or remove
- [x] [bug]     In galeria context menu, hard-coded Catalan strings ("Obrir detalls", "Eliminar", "Copiar ISBN", "Confirmar eliminació") instead of I18n.t() keys — inconsistent with the table context menu which uses I18n
- [x] [clean]   undoDelete() shows a JOptionPane confirmation ("restaurat.") — this is a hardcoded Catalan string not in I18n; move to I18n
- [x] [desp] batchEdit() uses String comparison (selFormat.startsWith("(")) to detect "no change" sentinel — use null or a dedicated NO_CHANGE constant instead of magic prefix strings
- [x] [desp] duplicarLlibre() manually sets each field on the dialog after getting the copy — this is presentation logic that couples MostrarBibliotecaControl to GuardarLlibresDialogo internals; extract to GuardarLlibresDialogoControl.loadFromLlibre(Llibre)
- [x] [clean]   mostrarLlegitsRecentment() hardcodes "Cap llibre marcat com a llegit." and "Llegits" instead of I18n keys

## src/presentacio/MostrarBibliotecaPanel.java
- [x] [clean]   setOnDragToShelf() callback is set from MostrarBibliotecaControl — the panel has a complex drag-to-shelf implementation that belongs in the controller
- [x] [desp]rebuildSidebarShelves() recreates all shelf buttons from scratch on every refresh — use a diff approach (add new, remove old, update count) to avoid flicker

## src/presentacio/GaleriaCobertesPanel.java
- [x] [desp]WrapLayout is defined as an inner class or assumed available — if it's a separate class, it should be in a shared layout package; if inline, split it out
- [x] [desp]zoomPopup (the hover-enlarge popup) is a JWindow — this can cause z-order issues on some platforms; use a JToolTip or JLayeredPane overlay instead
- [x] [clean]   keyboardIdx and lastClickedIdx are separate tracking variables for keyboard vs mouse selection — unify into a single focusedIdx
- [x] [desp] Cover loading fires a background thread per card on updateLlibres() — when the gallery has 200+ books this spawns 200+ threads; use a bounded ExecutorService with a queue

## src/presentacio/ConfiguracioDialog.java
- [x] [desp] Dialog has 3 Runnable callbacks (onThemeChange, onProfileSwitch) — define a ConfiguracioDialogListener interface instead of raw Runnables
- [x] [desp]The dialog reads current config values at construction time but doesn't reflect live changes if config is changed elsewhere (via API for example)
- [x] [clean]   No validation of DB connection before saving — user can set an invalid MariaDB host and only discover the error on restart; add a "Test connection" button

## src/presentacio/detalles/vista/DetallesLlibrePanel.java
- [x] [div]327-line panel has tabs (general, notes, advanced) all built in one class — split into DetallesGeneralTab, DetallesNotesTab, DetallesAvancatTab
- [x] [desp] Cover image display logic (loading blob, falling back to URL, falling back to default) is inline — extract to CoverDisplayHelper.loadAndDisplay(isbn, cd, label)

## src/presentacio/detalles/control/DetallesLlibrePanelControl.java
- [x] [clean]   OpenLibrary lookup is triggered from the controller on a background thread but updates the UI directly without SwingUtilities.invokeLater() — this is an EDT violation

## src/presentacio/detalles/control/GuardarLlibresDialogoControl.java
- [x] [clean]   ISBN scan (barcode) is wired in this controller but only available in MostrarBibliotecaControl's toolbar — the scan should populate this dialog if triggered from the new-book shortcut
- [x] [desp] Form validation fires on save click — add real-time validation feedback (red border on invalid fields) using a shared FormValidator component

## src/presentacio/detalles/vista/LlistesDelLlibreDialog.java
- [x] [desp] Dialog shows shelves for a book but changes are not saved until OK — this is a modal dialog that builds its own save logic; extract to LlistesDelLlibreControl
- [x] [clean]   No way to see which shelf the book is already in before opening this dialog — the table should pre-select current shelves
- [x] [desp] "Afegir a llista" and "Treure de llista" buttons are separate — consider a toggleable checkbox list for a simpler UX

## src/presentacio/detalles/vista/TagsDelLlibreDialog.java
- [x] [clean]   Tag filtering/search is missing — with many tags, users must scroll; add a search field
- [x] [desp] Dialog doesn't show tag usage count — hard to know which tags are commonly used; add a count column
- [x] [clean]   Changes are applied immediately (addLlibreToTag/removeLlibreFromTag called on click) vs LlistesDelLlibreDialog which defers to OK — make the behavior consistent
- [x] [desp] Extract TagsDelLlibreControl to separate wiring from layout (same Panel/Control MVC split as the rest of the app)

## src/presentacio/EstadistiquesHelper.java
- [x] [desp] buildStatsSummary() returns a pre-formatted String — the caller (MostrarBibliotecaControl) puts it in a JTextArea; it should return structured data and let the caller decide formatting

## src/presentacio/GestioLlistesDialog.java
- [x] [desp]Shelf management (rename, delete, color, order) in a separate dialog — consider integrating into the sidebar directly (inline rename on double-click, drag-to-reorder)
- [x] [clean]   After modifying a shelf, the main view's refreshComboLlistes() is called via a callback — make this explicit with a GestioLlistesListener interface
- [x] [desp] Color picker is a basic JColorChooser — use a compact color swatch picker or limit to a palette for a cleaner UX
- [x] [desp]The dialog has its own inner class for the shelf table model — extract to a LlistaTableModel class for reuse in the stats dialog

## src/presentacio/TablePageController.java
- [x] [clean]   useDBPagination and currentPage are package-private fields accessed directly by MostrarBibliotecaControl — encapsulate with methods
- [x] [desp]showPage() rebuilds the entire table model on every page change — use a virtual/lazy table model that loads rows on demand (PaginationModel + BibliotecaTableModel; DB path uses page queries)
- [x] [desp]paginatedMode flag is set externally — add a single setPaginationMode(boolean) or derive it from isLargeLibrary() automatically

## src/presentacio/renderers/TableCellComponents.java
- [x] [desp] BotonDetallesRenderer + BotonDetallesEditor pair is a pattern to show a button in a table cell — this pattern is complex and fragile; consider using a row click action instead

## src/presentacio/SplashScreen.java
- [x] [clean]   SplashScreen is a JWindow which doesn't appear in taskbar and can get hidden behind other windows — use an undecorated JFrame with setAlwaysOnTop(true)
- [x] [desp]SplashScreen is constructed in Ejecutable.startSwingWithSplash() and used across a thread boundary — wrap in SwingUtilities.invokeLater() consistently

## src/presentacio/AboutDialog.java
- [x] [clean]   Version string is likely hardcoded — load from a MANIFEST.MF or version.properties resource so it updates automatically at build time

## src/presentacio/ModeSelectorDialog.java
- [x] [desp]Uses a Mode enum returned as a static result — this is a non-standard dialog pattern; the caller blocks until the user picks; document this clearly or use a CompletableFuture
- [x] [desp] If the user closes the dialog with the window X button, null is returned (CANCELLED) and main() returns — this exits silently; show a message or default to a mode

## src/presentacio/MainFramePanel.java
- [x] [desp] Extends JFrame directly — MVC split means the panel should be a JPanel embedded in a JFrame owned by MainFrameControl; mixing layout into the JFrame subclass couples the view to the window lifecycle
- [x] [clean]   Title hardcoded as "Biblioteca" — use I18n key or a constant; if app name changes, only one place needs updating
- [x] [clean]   MostrarBibliotecaPanel constructed eagerly in the field declaration (line 19) before the constructor configures the frame — if MostrarBibliotecaPanel constructor fails, the JFrame is partially constructed; lazy-init inside constructor
- [x] [desp]statusBar is a JLabel — for screen readers, it has no accessible role or live region; use JPanel with an accessible description or post-construction setAccessibleDescription()

## src/presentacio/detalles/vista/GuardarLlibresDialogo.java
- [x] [clean]   Missing fields vs DetallesLlibrePanel: no paisOrigen, estat, notes, nomCa/nomEs/nomEn, exemplars — new-book dialog silently drops these; at minimum add a notes field so users don't lose annotations
- [x] [desp] btnCercaInternet background is hardcoded new java.awt.Color(0x117A65) — not theme-aware; add UITheme.SUCCESS_GREEN or similar constant so it respects the active theme
- [x] [clean]   comboFormat items string array is built inline from I18n.t() calls — if format options change, this array and the one in DetallesLlibrePanel must be updated in sync; extract to a shared getFormatOptions() helper

## src/herramienta/csv/CsvImportStrategy.java
- [x] [clean]   No getName() or describe() method on the interface — BookImporter can't log which strategy was selected; add String getName() for diagnostics

## src/interficie/EnActualizarBBDD.java / OnLlibreUpdate.java / OnLlibreDelete.java
- [x] [clean]   OnLlibreDelete.eliminarLlibre(Llibre l) — "eliminar" is Spanish; rename to onLlibreDeleted(Llibre l)
- [x] [clean]   These interfaces live in the interficie package alongside BibliotecaReader/Writer (data contracts) but they're Swing event callbacks — move change-listener interfaces to presentacio.listener to separate data contracts from UI callbacks

## checkBiblio/StressTest.java
- [x] [desp]Stress test uses a hardcoded local H2 URL — should use the same test infrastructure as BibliotecaTest (Sistema.setProperty("biblioteca.h2.url"))
- [x] [clean]   Test results are printed to stdout — add assertion failures or a proper test framework integration
- [x] [desp] Currently tests only concurrent adds — add concurrent read-while-write and concurrent delete tests
- [x] [clean]   Thread count (50? 100?) is hardcoded — make it configurable via a system property for CI vs local runs
- [x] [desp]StressTest creates its own ControladorDomini instance but doesn't reset between runs — add resetForTest() calls in @Before/@After equivalent

## checkBiblio/UIAudit.java
- [x] [desp]UIAudit seems to check for I18n key completeness or UI issues — if so, add it to the CI/make test pipeline so it runs automatically
- [x] [clean]   If UIAudit uses reflection to scan the codebase for I18n.t() calls, make sure it compares against all 3 languages and flags missing keys
- [x] [desp] Add a check for hard-coded Catalan strings in .java files (grep for Catalan common words that should be I18n keys)
- [x] [clean]   UIAudit output format should be machine-parseable (JSON or checkstyle XML) so CI can fail on audit errors
- [x] [desp] Expand UIAudit to check for duplicate I18n keys and unused keys (keys defined in I18n.java but never called with I18n.t())

## checkBiblio/run.sh / run2.sh
- [x] [clean]   run.sh and run2.sh are likely different modes (swing vs web) — name them run-swing.sh and run-web.sh for clarity
- [x] [desp]Shell scripts should validate that java and the required JARs exist before running, with a helpful error message
- [x] [clean]   Hard-coded paths in scripts — use $(dirname "$0") to make paths relative to the script location
- [x] [desp]Scripts don't set JAVA_OPTS for heap size — on large libraries (>10k books) the default heap may be insufficient; add -Xmx512m
- [x] [clean]   run2.sh likely duplicates most of run.sh — consolidate into one script with a --web / --swing argument

