═══════════════════════════════════════════════════════════
  BIBLIOTECA — TODO
═══════════════════════════════════════════════════════════

── BUGS / CORRECTNESS ──────────────────────────────────────
[x] checkLlibre.cheackLlibre logic is OR not AND — any one
    condition (14-digit ISBN, valoracio 0-10, portada prefix)
    makes it pass. Should require ALL to be valid.
[x] cheackLlibre: portada.startsWith("portades/") NPE if
    portada is null (caller can pass null).
[x] actualizarfila() in MostrarBibliotecaControl only updates
    the ISBN cell, does not refresh Nom/Autor/Any/etc. after edit.
[x] matchString() does not handle case-insensitive search —
    searching "cervantes" won't find "Cervantes".
[x] If DB is unreachable at startup, app shows error dialog
    but continues with empty/broken state instead of exiting
    cleanly.

── FEATURES ────────────────────────────────────────────────
[x] Delete book button in DetallesLlibrePanel (with confirm dialog).
[x] Export library to CSV / Excel / PDF.
[x] Import books from CSV or barcode scanner.
[x] Lookup book metadata from ISBN via OpenLibrary API
    (auto-fill nom, autor, any from ISBN scan via escanejarISBN).
[ ] Pagination or lazy loading for large libraries (>1000 books).
[x] Search bar at top of table (real-time filter as user types).
[x] Sort indicator on table headers (currently sortable but no icon).
[x] Statistics panel: total books, % read, average price/rating,
    books per year.
[ ] Multiple libraries / shelves (tags or categories per book).
[x] Backup & restore database button — "Backup BD" exports SQL dump,
    "Restaurar BD" restores from SQL file (both in filter panel).
[x] Settings dialog: DB connection (host/user/password) + default
    images folder. Language skipped. Dark mode already persisted.

── UX / UI ─────────────────────────────────────────────────
[x] Dark mode preference not saved — reverts to light on restart.
    Persist to a config file (e.g. ~/.biblioteca/config.properties).
[x] Llegit/No llegit checkboxes in table column (toggle in-place).
[x] Keyboard shortcut Ctrl+N for Nou Llibre (already has tooltip,
    not yet wired to KeyBinding).
[x] Keyboard shortcut Ctrl+F to focus filter panel.
[x] Keyboard shortcut Delete on selected row to delete book.
[x] DetallesLlibrePanel: image preview when typing/selecting portada
    path (live update, not only on load).
[x] GuardarLlibresDialogo: image preview next to Explorar button.
[ ] Table row double-click already opens details — add single-click
    selection highlight (currently no visual feedback on first click).
[x] Show book count in window title or status bar.
[x] Tooltips on table column headers explaining each field.

── CODE QUALITY ─────────────────────────────────────────────
[x] checkLlibre class name violates Java conventions — renamed to
    LlibreValidator (checkLlibre.java kept as empty stub).
[x] cheackLlibre method name had a typo — renamed to checkLlibre
    in LlibreValidator.
[x] Unused import java.awt.event.KeyListener in GuardarLlibresDialogo.
[x] Unused import java.awt.event.KeyEvent in GuardarLlibresDialogo.
[x] AcercaDeDialogo (About dialog) is mostly unused/empty — either
    fill it with app info or remove it.
[x] acercade package is dead code — not accessible from UI.
[x] ServerConect: single try block — failed connection exits cleanly
    via System.exit(1) before any table creation is attempted.
[ ] ControladorPersistencia: no connection pooling — single shared
    Connection object is not thread-safe.
[ ] Add integration tests for ServerConect / ControladorPersistencia
    (need a test DB or H2 in-memory).
[ ] Add edge case tests: empty library, duplicate ISBN insert,
    delete non-existent book, filter with all params set.

── PERSISTENCE ─────────────────────────────────────────────
[x] Column name mismatch: Llibre.getPortada/setPortada renamed to
    getImatge/setImatge everywhere to match DB column `imatge`.
[ ] No migration system — schema changes require manual resetDatabase().
    Add a versioned migration table.
[x] Connection string configurable — dbHost/dbUser/dbPassword now
    read from ~/.biblioteca/config.properties via Config class.

═══════════════════════════════════════════════════════════
