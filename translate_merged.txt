# translate_merged.txt — non-Catalan text in the Biblioteca codebase
#
# Auto-merged from translate.txt (comment/string inventory) and
# translate1.txt (identifier / string-suggestion plan). Both files
# are kept untouched; this is the unified single source of truth.
#
# TSV columns:
#   KIND      COMMENT_BLOCK | COMMENT_LINE | STRING | IDENTIFIER | LICENSE_HEADER
#   FILE      source path relative to repo root
#   LINE      line number, or a range (e.g. 5-8)
#   LANG      en | mixed | section-header | ca   (blank for IDENTIFIER rows)
#   ORIGINAL  the non-Catalan text as it appears in source
#   SUGGESTED proposed Catalan form, or "-" if none / "keep" if no change
#   NOTES     context / occurrence counts / edge-case flags
#
# Meta sections (SUMMARY / RENAMES / JUDGMENT / FILE INVENTORY) from
# translate1.txt are preserved verbatim in the ## META block at the
# bottom of this file.
#
# Filter examples:
#   rg -F $'\tIDENTIFIER\t' translate_merged.txt
#   rg '^STRING\t' translate_merged.txt

KIND	FILE	LINE	LANG	ORIGINAL	SUGGESTED	NOTES
# --- from translate.txt (263 rows) ---
COMMENT_BLOCK	src/domini/ControladorDomini.java	27	en	/** * Singleton facade over the library's in-memory state + persistence. * * <p>Responsibilities: * <ul> * <li>Singleton lifecycle ({@link #getInstance()}, {@link #resetForTest()}, * {@link #resetForProfileSwitch()}, {@link #create(ControladorPersistencia)}).</li> * <li>Owns the {@link StateContext}: the three backing lists * ({@code bib}, {@code llistes}, {@code tags}), the in-memory lock * ({@code BIB_LOCK}), and the id-index maps.</li> * <li>Delegates every business operation to one of six focused * collaborator classes in {@code domini.facade} — Shelf, Tag, * Loan, Book, Stats, Backup.</li> * </ul> * * <p>The public API of this class is unchanged: every method that existed * before the split still exists, with the same signature, same * exceptions, and same side effects. The only externally observable * change is the file's line count (637 → ~150). */	-	
COMMENT_LINE	src/domini/ControladorDomini.java	100	mixed	── Book / search / blob ──────────────────────────────────────────────────	-	
COMMENT_BLOCK	src/domini/EspecificacioOrdenacio.java	6	en	/** * Pagination-aware sort specification, extracted from LlibreFilter so that * filter criteria stay pure data while sort is a separate pagination concern. * * <p>Single source of truth for both SQL fragment and in-memory comparator * per sortable column. The {@link #COLUMNS} map is the canonical registry — * adding a new column is a one-line edit (per the tot.txt LOW finding on * the parallel {@code SQL_COLS} / {@code COMPARATORS} maps). */	-	
COMMENT_BLOCK	src/domini/EspecificacioOrdenacio.java	22	en	/** Canonical registry: one entry per sortable column. */	-	
COMMENT_BLOCK	src/domini/EspecificacioOrdenacio.java	25	en	/** ISBN-ascending comparator; nulls sort first to match binarySearch behavior. */	-	
COMMENT_BLOCK	src/domini/EspecificacioOrdenacio.java	45	en	/** * Unknown / null column names are tolerated and silently coerced to * {@link #COL_ISBN}. The fallback lives here (not in the call site) * so the policy of "unknown → ISBN" is in one place. toSql() and * {@link #comparator(String)} both honour the fallback. */	-	
STRING	src/domini/EspecificacioOrdenacio.java	48	en	unknown → ISBN	-	
COMMENT_BLOCK	src/domini/EspecificacioOrdenacio.java	61	en	/** SQL ORDER BY expression fragment, e.g. "l.`ISBN` ASC" */	-	
COMMENT_BLOCK	src/domini/EspecificacioOrdenacio.java	67	en	/** * In-memory comparator for the column. Falls back to * {@link #ISBN_COMPARATOR} for unknown columns so callers don't * have to repeat the null-coalesce. Null column is treated as * unknown (same coercion the constructor applies). */	-	
COMMENT_BLOCK	src/domini/EspecificacioOrdenacio.java	79	en	/** Read-only view of all in-memory comparators keyed by SortSpec column name. */	-	
COMMENT_BLOCK	src/domini/EspecificacioOrdenacio.java	86	en	/** Read-only view of the canonical column registry. */	-	
COMMENT_BLOCK	src/domini/Llibre.java	52	en	/** * Copy the canonical "updateable" fields (everything except the * per-language title, cover blob, autors list, notes, pages, etc.) * into {@code target}. The {@code autor} parameter is the joined * string from the validator / form; callers that have the parsed * list should call {@link #setAutors(java.util.List)} afterwards, * which OVERWRITES the joined string. The two-step order * (bind → setAutors) is intentional: bindUpdateableFields first * seeds the joined string so {@link #getAutors()} is consistent * immediately, then the list override lets the caller replace it * with the parsed result without the field-level split logic. */	-	
COMMENT_BLOCK	src/domini/Llibre.java	134	en	/** Returns {@code s.trim()} if {@code s} is non-null and not blank, else {@code null}. */	-	
COMMENT_BLOCK	src/domini/Llibre.java	139	en	/** Returns the title for the given lang code ("ca","es","en"), falling back to nom. */	-	
COMMENT_BLOCK	src/domini/Llibre.java	155	en	/** Used when loading multiple author rows from SQL (see {@link persistencia.LlibreDao#getAll}). */	-	
COMMENT_BLOCK	src/domini/LlibreCore.java	6	en	/** * Immutable value-object view of a book's identifying/business data. * Carries a 16-field subset of {@link Llibre} (identity, authors, title, * year, descripcio, valoracio, preu, llegit, imatge, pagines, editorial, * serie, volum, idioma, format, paisOrigen). The remaining Llibre state * (notes, paginesLlegides, dataCompra, dataLectura, estat, desitjat, * nomCa/Es/En, exemplars, llenguaOriginal, cover blob) is consumed in * place from the mutable Llibre. Future refactor: Llibre composes a * LlibreCore + extension state. */	-	
COMMENT_BLOCK	src/domini/LlibreCore.java	34	mixed	/** Project a {@link Llibre} into its immutable core. */	-	
COMMENT_BLOCK	src/domini/LlibreCore.java	44	en	/** Bulk projection — equivalent to {@code source.stream().map(LlibreCore::from).toList()} * but avoids the stream lambda allocation. */	-	
COMMENT_BLOCK	src/domini/LlibreLlistaContext.java	3	en	/** * Per-book context for a shelf membership: which shelf, which book, and the * shelf-local values (rating, read flag) that only make sense in the context * of that specific (book, shelf) pair. Use this instead of putting the * per-book fields on {@link Llista} directly — the shelf itself has no * rating, only the (book, shelf) join does. */	-	
COMMENT_BLOCK	src/domini/SqlOp.java	5	en	/** * Lambda body that may throw {@link SQLException}. Used by domain-layer try/catch wrappers * to convert SQL errors into {@link BibliotecaException} without boilerplate at each call site. */	-	
COMMENT_BLOCK	src/domini/SqlOp.java	13	en	/** Run {@code op}; rethrow any SQLException as a {@link BibliotecaException} with the cause preserved. */	-	
COMMENT_BLOCK	src/domini/facade/DelegatCopiaSeguretat.java	13	en	/** * Backup, restore, and clear-all. Touches all three state lists; the * snapshot is taken atomically under the state lock; the file I/O runs * outside the lock (the original code did the same — I/O is not held * against concurrent state reads). */	-	
COMMENT_BLOCK	src/domini/facade/DelegatPrestatgeria.java	16	en	/** * Shelf ({@link Llista}) management and book↔shelf relation operations. * * <p>All single-DB mutations follow an atomic contract: the state * lock is held for the entire {@code pre-check → persistence → * in-memory mutate} sequence. This closes the race that existed * in the pre-facade {@code ControladorDomini}, where two threads * renaming the same shelf could both pass the pre-check, race the * DB (last-writer-wins), and then both mutate the in-memory map — * the second mutation was silently lost or the in-memory state * diverged from the DB. * * <p>For the move-up / move-down helpers the lock is also held * across the two single-row updates * ({@link #swapLlistesOrdreLocked}); both writes are brief and the * caller already holds the lock, so a release-and-reacquire dance * would add complexity for negligible throughput gain. */	-	
COMMENT_BLOCK	src/domini/facade/DelegatPrestatgeria.java	134	en	/** Caller MUST hold the state lock. */	-	
COMMENT_BLOCK	src/domini/facade/DelegatPrestatgeria.java	141	en	/** * Caller MUST hold the state lock. Holds it across the two DB * updates as well — both are single-row writes so the lock is * only held for milliseconds. The original code already * followed this pattern; it is preserved here for consistency * with the rest of the delegate and because the caller * ({@link #moveLlistaUp} / {@link #moveLlistaDown}) is itself * already inside a {@code withLock} block, making a * release-and-reacquire dance unnecessarily complex. */	-	
COMMENT_BLOCK	src/domini/facade/DelegatPrestec.java	10	en	/** * Loan ({@link PrestecRow}) management and loan queries. * * <p>All operations delegate to the persistence layer; loan state is * not held in memory (it lives entirely in the DB). Operations translate * {@link SQLException} into {@link BibliotecaException} for callers. */	-	
COMMENT_BLOCK	src/domini/facade/StateContext.java	12	en	/** * Shared in-memory state for the {@link domini.ControladorDomini} facade. * * <p>Package-private to {@code domini.facade}; only the delegates and the * facade itself touch this. Exposes the lock, the three backing lists * ({@code bib}, {@code llistes}, {@code tags}), the id-index maps * ({@code llistesById}, {@code tagsById}), and the persistence reference. * * <p>All reads and writes of the backing lists MUST go through the * {@link #lock} so the lists and the id-index maps stay in sync. The * {@link #withLock} and {@link #withLockReturning} helpers are the * canonical way to do that. */	-	
COMMENT_BLOCK	src/domini/facade/StateContext.java	49	en	/** The monitor used by {@link #withLock} / {@link #withLockReturning}. * Package-private (no public accessor): every delegate in * {@code domini.facade} goes through {@code withLock(...)} so * there is no reason for a caller to acquire the lock directly. * The package-private access is preserved for the facade * package (e.g. tests that need a raw {@code synchronized * (state.lock) { ... }}) but the public surface stays * clean. */	-	
COMMENT_BLOCK	src/domini/facade/StateContext.java	59	en	/** Returns the live backing list. Callers MUST hold {@link #lock()} before * reading or mutating; iterating without the lock risks * {@link java.util.ConcurrentModificationException} and mutating without * the lock can desync the list from the id-index maps. Prefer * {@link #withLock} / {@link #withLockReturning} for read+copy patterns. */	-	
COMMENT_BLOCK	src/domini/facade/StateContext.java	65	mixed	/** See {@link #bib()} for the locking contract. */	-	
COMMENT_BLOCK	src/domini/facade/StateContext.java	67	mixed	/** See {@link #bib()} for the locking contract. */	-	
COMMENT_BLOCK	src/domini/facade/StateContext.java	72	en	/** Replace all three backing lists atomically (used by restoreFromSQL). */	-	
COMMENT_BLOCK	src/domini/facade/StateContext.java	84	en	/** Clear all three backing lists and the id-index maps (used by clearAll). * Mirrors {@link #replaceAll(ArrayList, ArrayList, ArrayList)}'s contract * by going through {@link #rebuildIdIndexesLocked()} for consistency * (today the two paths clear the maps directly, but the centralised * call makes future additions — e.g. a new id-index map — a single * line change rather than a two-line search). */	-	
COMMENT_BLOCK	src/domini/facade/StateContext.java	99	en	/** Rebuild id-index maps from the current backing lists. Must be called under lock. */	-	
COMMENT_BLOCK	src/domini/facade/StateContext.java	107	en	/** Run {@code action} while holding the state lock. */	-	
COMMENT_BLOCK	src/domini/facade/StateContext.java	112	en	/** Run {@code action} while holding the state lock; return its result. */	-	
COMMENT_BLOCK	src/domini/facade/TagDelegate.java	12	en	/** * Tag ({@link Tag}) management and book↔tag relation operations. * * <p>All methods follow the same contract: mutate the persistence layer * first, then update the in-memory tag list + id-index map atomically * under the {@link StateContext#lock() state lock}. */	-	
STRING	src/domini/facade/TagDelegate.java	31	en	Tag not found:	-	
COMMENT_BLOCK	src/herramienta/AnalitzadorOpenLibrary.java	13	en	/** * Pure-JSON parsing for OpenLibrary responses. Extracted from {@link OpenLibraryClient} so the * network path and the parsing logic can be tested independently. */	-	
COMMENT_BLOCK	src/herramienta/AnalitzadorOpenLibrary.java	20	en	/** Parse an {@code /api/books?bibkeys=ISBN:...&format=json&jscmd=data} response body. */	-	
COMMENT_BLOCK	src/herramienta/AutoCompletion.java	18	en	/* This work is hereby released into the Public Domain. * To view a copy of the public domain dedication, visit * http://creativecommons.org/licenses/publicdomain/ */	-	
STRING	src/herramienta/ClientOpenLibrary.java	163	en	User-Agent	-	
STRING	src/herramienta/ClientOpenLibrary.java	235	en	Interrupted during retry backoff	-	
STRING	src/herramienta/ClientOpenLibrary.java	240	en	No retries performed for	-	
COMMENT_BLOCK	src/herramienta/ConfigDTO.java	3	en	/** * DTO for the Swing settings dialog. Mirrors fields exposed in configuration UI; * persistent state still lives in {@link Config}. */	-	
COMMENT_BLOCK	src/herramienta/Configuracio.java	53	en	/** Internal: write a key into a specific domain store. */	-	
COMMENT_BLOCK	src/herramienta/Configuracio.java	63	en	/** Internal: read a key from a specific domain store. */	-	
STRING	src/herramienta/Configuracio.java	450	en	config-save	-	
STRING	src/herramienta/Configuracio.java	562	en	config-shutdown	-	
COMMENT_BLOCK	src/herramienta/ConfiguracioDb.java	3	en	/** Typed read/write view of database-related {@link Config} keys. */	-	
COMMENT_BLOCK	src/herramienta/ConfiguracioFiltre.java	3	en	/** * Typed read/write view of the small "filter / defaults" sub-store of * {@link Config} (currently just {@code defaultImgDir}; the preset * save/delete APIs remain on {@link Config} directly because they span * 17 keys at once). */	-	
COMMENT_BLOCK	src/herramienta/ConfiguracioFinestra.java	5	en	/** * Typed read/write view of window-geometry and table-column {@link Config} keys. * The column-width / column-visible keys live in the WINDOW sub-store * (see {@code Config.WINDOW_KEYS} plus the dynamic {@code colWidth_*} / * {@code colVisible_*} prefix rule). */	-	
COMMENT_BLOCK	src/herramienta/ConfiguracioUi.java	5	en	/** * Typed read/write view of UI-related {@link Config} keys. * Internally backed by the UI sub-store introduced with the B1 god-class * split. Callers should prefer these typed setters over {@link Config}'s * generic ones — they group related configuration and make the * dependency from callers to a specific sub-domain explicit. */	-	
STRING	src/herramienta/DialegError.java	95	mixed	OptionPane.warningIcon	-	
COMMENT_BLOCK	src/herramienta/Escapers.java	3	en	/** Centralized string-escape utilities used by export/serialization paths. */	-	
COMMENT_BLOCK	src/herramienta/Escapers.java	7	mixed	/** HTML-escape: handles & < > " '. */	-	
COMMENT_BLOCK	src/herramienta/Escapers.java	17	en	/** SQL single-quote-string escape (doubles a {@code '} character). The caller adds outer quotes. */	-	
COMMENT_BLOCK	src/herramienta/Escapers.java	22	en	/** JSON string escape (handles backslash, quote, control chars). */	-	
COMMENT_BLOCK	src/herramienta/Escapers.java	45	mixed	/** CSV quoting per RFC-4180 (double internal {@code "}, wrap in {@code "}). */	-	
STRING	src/herramienta/Escapers.java	45	en	}, wrap in {@code	-	
COMMENT_BLOCK	src/herramienta/ExportadorLlibres.java	96	en	/** * Extended HTML export with an explicit cover-inclusion flag and * pre-loaded cover bytes. The simpler overload above omits covers * (renders a 📖 placeholder for every book) and is the safe default * for libraries with more than {@link #LARGE_LIBRARY_COVER_THRESHOLD} * books — base64-encoding a 50 KB cover per book produces ~50 MB * of HTML for 1 000 books, which most browsers handle poorly. * * <p>When {@code coverBlobs} is non-null it must contain one entry * per book in {@code view} (same order). A {@code null} entry means * "no cover available, render the placeholder". Callers that opt in * to covers must populate the array from a background thread (the * underlying {@code CoverService.getCachedBytes} and * {@code cd.getLlibreBlob} do disk + DB I/O). */	-	
COMMENT_BLOCK	src/herramienta/ExportadorLlibres.java	122	en	/** Above this many books, the caller should pre-load covers off the * EDT and pass them in, or skip covers entirely (the default * 3-arg overload does this). */	-	
COMMENT_BLOCK	src/herramienta/ExportadorLlibres.java	328	en	/** Wraps {@link Escapers#json(String)} in double quotes and renders * {@code null} as the JSON literal {@code null}. */	-	
STRING	src/herramienta/FiltreUtils.java	24	mixed	The Lord of the Rings	-	
COMMENT_BLOCK	src/herramienta/FiltreUtils.java	42	en	/** * Single source of truth for the in-memory filter predicate. SQL path in * {@code LlibreDao.search} mirrors this — when adding a new field to * {@link LlibreFilter}, also extend the SQL builder there. * * <p>{@code tagISBNs} and {@code llistaISBNs} are pre-computed ISBN sets * supplied by the caller (they require DB lookups) so this method stays * pure and side-effect-free. */	-	
COMMENT_BLOCK	src/herramienta/FormatOptions.java	3	en	/** * Shared format option lists for book format combos. * Used by New-Book Dialog, Details Panel, and Batch-Edit Dialog. * Single source of truth avoids I18n key drift. */	-	
COMMENT_BLOCK	src/herramienta/FormatOptions.java	11	en	/** Returns format options with a blank first entry (for "no selection"). */	-	
COMMENT_BLOCK	src/herramienta/FormatOptions.java	17	en	/** Returns format options with a "no change" first entry (for batch edit). */	-	
STRING	src/herramienta/FormatOptions.java	17	mixed	no change	-	
COMMENT_LINE	src/herramienta/ImportadorLlibres.java	58	mixed	per a CSV grans).	-	
COMMENT_BLOCK	src/herramienta/ImpressorTaulaCopiaSeguretat.java	3	en	/** * Place-holder for per-table backup-printf builders. {@link BackupService#backupToSQL} * currently inlines a 26-arg printf for the llibre table; future refactor will move one * builder per table here (llibre, llista, llibre_llista, tag, llibre_tag, prestec, lectura, * autor, llibre_autor). * * <p>Today, callers should continue using {@code BackupService.backupToSQL}; this class is the * named seam where future per-table builders will live. */	-	
COMMENT_BLOCK	src/herramienta/Lang.java	3	en	/** Supported UI languages. Ordinal matches the column index used by {@link I18n#TABLE} arrays. */	-	
COMMENT_BLOCK	src/herramienta/ParseHelpers.java	5	en	/** * Shared text-to-number parsers for the presentacio layer. * * Two contracts coexist: * <ul> * <li><b>Best-effort</b> ({@code parseIntOrNull} / {@code parseLongOrNull} / * {@code parseDoubleOrNull}): silent on bad input. Returns a boxed type so * the caller can distinguish "empty" from "zero". Use in sort, display, * or default-aware paths where an error dialog is undesirable.</li> * <li><b>Strict</b> ({@code parseInt} / {@code parseLong} / {@code parseDouble}): * empty input is mapped to {@code defaultValue}; non-numeric input * appends a translated message to {@code errors} and returns * {@code defaultValue}. Use in save paths so a single bad field * aborts the save before the model is mutated.</li> * </ul> * * Replaces the parallel parsers that used to live in * {@code DetallesLlibrePanelControl} and {@code GuardarLlibresDialogoControl}. */	-	
COMMENT_BLOCK	src/herramienta/PresetStore.java	5	mixed	/** Typed view of preset-related {@link Config} keys. */	-	
COMMENT_BLOCK	src/herramienta/RelationRemapper.java	11	en	/** * Maps incoming external IDs (from CSV/JSON exports) to the locally-assigned IDs of shelves * and tags, creating new entries when no name match exists. Replaces duplicated remap loops in * {@code ImportExportRouter.importJson} and {@code BookImporter.importJSON}. */	-	
COMMENT_BLOCK	src/herramienta/SelectorMostraColor.java	13	en	/** Compact shelf-color picker using a fixed palette (no full {@code JColorChooser}). */	-	
COMMENT_BLOCK	src/herramienta/SelectorMostraColor.java	16	en	/** * Default shelf color when none has been assigned. Shared by * {@link #chooseHex} (when the caller passes {@code null}) and * {@code GestioLlistesDialog.onColorLlista} (when the existing * shelf has no color). Lifting it to a single constant makes * the policy "new shelf starts blue" easy to change in one * place (per the tot.txt MEDIUM finding on the shared default). */	-	
COMMENT_BLOCK	src/herramienta/SelectorMostraColor.java	33	en	/** @return chosen color hex (#RRGGBB) or {@code null} if cancelled */	-	
COMMENT_BLOCK	src/herramienta/ServeiCoberta.java	15	en	/** * Unified cover-fetching: bounded pool, L1 memory cache, L2 disk cache under ~/.biblioteca/covers/. */	-	
STRING	src/herramienta/ServeiCoberta.java	28	en	cover-fetch	-	
STRING	src/herramienta/ServeiCoberta.java	36	en	cover-write	-	
COMMENT_BLOCK	src/herramienta/ServeiCoberta.java	83	mixed	/** Fetch cover bytes for {@code isbn}, save to DB blob, invoke callback on success. */	-	
COMMENT_BLOCK	src/herramienta/ServeiCoberta.java	151	en	/** Shutdown the cover-fetch pool. Called from a shutdown hook. */	-	
STRING	src/herramienta/ServeiCopiaSeguretat.java	24	mixed	Cerca a Internet	-	
STRING	src/herramienta/ServeiCopiaSeguretat.java	44	mixed	auto-backup	-	
STRING	src/herramienta/ServeiCopiaSeguretat.java	87	mixed	yyyy-MM-dd_HH-mm-ss	-	
COMMENT_BLOCK	src/herramienta/UITheme.java	22	en	/** Typed read-only view of the runtime theme palette. A fresh snapshot * is returned by {@link #palette()} on every call so the record reflects * the current state of the mutable Color statics (which are still the * single source of truth — they get re-assigned by {@link #setTheme} * and the theme appliers). Migration target: callers do * {@code UITheme.palette().bgMain} instead of {@code UITheme.palette().bgMain()}. */	-	
COMMENT_BLOCK	src/herramienta/UITheme.java	54	en	/** Snapshot the current Color state into an immutable Palette record. * Callers should prefer {@code UITheme.palette().xxx} over the raw * {@code UITheme.XXX} field accesses. */	-	
COMMENT_LINE	src/herramienta/UITheme.java	108	mixed	── Fixed colors (theme-independent) ─────────────────────────────────────	-	
COMMENT_BLOCK	src/herramienta/UITheme.java	112	en	/** Search-highlight palette entries — used by the SearchHighlightRenderer. * Theme-independent: the highlight should pop on any background, so the * amber bg / black fg combo is shared across all themes. */	-	
COMMENT_LINE	src/herramienta/UITheme.java	126	mixed	── Light palette (warm library theme) ───────────────────────────────────	-	
COMMENT_BLOCK	src/herramienta/UITheme.java	195	en	/** * Base font, rebuilt by {@link #rebuildFonts(String)} when the configured * font size changes. Held as {@code private static volatile} so external * code must go through {@link #fontBase()} to read the current value, * preventing races with {@code rebuildFonts} on the EDT. */	-	
COMMENT_BLOCK	src/herramienta/UITheme.java	203	en	/** * Bold base font, rebuilt by {@link #rebuildFonts(String)} when the * configured font size changes. Held as {@code private static volatile} * so external code must go through {@link #fontBold()} to read the * current value, preventing races with {@code rebuildFonts} on the EDT. */	-	
COMMENT_BLOCK	src/herramienta/UITheme.java	216	en	/** Pair with {@link Config}'s shutdown hook to release the cached Font * instances at JVM exit (belt-and-suspenders for the static-state pattern). */	-	
COMMENT_BLOCK	src/herramienta/UITheme.java	301	mixed	/** Compat — maps to LIGHT or DARK. */	-	
COMMENT_BLOCK	src/herramienta/UITheme.java	319	en	/** Backwards-compat overload — accepts the legacy "small"/"medium"/"large" key. */	-	
COMMENT_BLOCK	src/herramienta/UITheme.java	366	en	/** * Release the cached Font instances so the JVM can reclaim them * sooner at shutdown. Called automatically once via a JVM shutdown * hook (paired with {@link Config}'s) and safe to call manually * (idempotent, no-op if already cleared). */	-	
COMMENT_BLOCK	src/herramienta/UtilitatsColor.java	8	en	/** Color helpers (hex format/parse, swatch icon) used by shelf-color dialogs and theme code. */	-	
COMMENT_LINE	src/herramienta/UtilitatsColor.java	21	mixed	Expand #rgb → #rrggbb	-	
COMMENT_BLOCK	src/herramienta/UtilitatsColor.java	32	en	/** Small rounded-rectangle swatch icon for shelf-color display. */	-	
COMMENT_BLOCK	src/herramienta/ValidadorLlibre.java	54	en	/** * Validates and builds a Llibre. * Only isbn and nom are mandatory. All other fields accept null/blank and get * safe defaults (valoracio=0, preu=0, llegit=false, rest empty strings). */	-	
COMMENT_BLOCK	src/herramienta/csv/AbstractMappingCsvStrategy.java	26	en	/** * Generic "find or create by name" lookup. Caches the full table on * first call so subsequent lookups are O(1) hash hits. The cache is * held in the strategy so the same map is reused across rows. */	-	
STRING	src/herramienta/csv/AbstractMappingCsvStrategy.java	27	en	find or create by name	-	
COMMENT_BLOCK	src/herramienta/csv/AnalitzadorPertinençaPrestatgeria.java	6	en	/** * Parses the native CSV "shelves" column: a string like * <pre>NameA|7.5|true;NameB|0|false</pre> * into {@link Entry} records. */	-	
COMMENT_BLOCK	src/herramienta/csv/CsvImportStrategy.java	10	en	/** Returns true if the row was imported, false if skipped (e.g. duplicate). Throws on parse error. */	-	
STRING	src/herramienta/csv/GoodreadsCsvStrategy.java	35	mixed	ISBN buit	-	
STRING	src/herramienta/csv/LibraryThingCsvStrategy.java	43	mixed	Cognom, Nom	-	
STRING	src/herramienta/csv/LibraryThingCsvStrategy.java	43	mixed	Nom Cognom	-	
COMMENT_BLOCK	src/herramienta/csv/Rfc4180Reader.java	8	en	/** * Streaming RFC-4180 CSV reader. Replaces whole-file loading in {@link CsvUtils#parseLine} * for large imports. Returns one row at a time as a {@code String[]}. * * <p>Contract: {@link #hasNext()} is the only probe; {@link #next()} is * guaranteed to return non-null when {@code hasNext()} is true (and throw * {@link NoSuchElementException} otherwise). The previous API returned * {@code null} for a trailing empty input, forcing callers to null-check * {@code next()}; per the tot.txt LOW finding, the contract is now * "hasNext is the only probe". * * <p>Caller-supplied {@link Reader} must be {@code BufferedReader} for line-buffered reads. */	-	
COMMENT_BLOCK	src/herramienta/csv/Rfc4180Reader.java	30	en	/** Returns true if there is another row to read. The next call to * {@link #next()} will return a non-null {@code String[]}. */	-	
COMMENT_BLOCK	src/herramienta/csv/Rfc4180Reader.java	38	en	/** Reads the next logical row, joining continuation lines inside quotes. */	-	
COMMENT_BLOCK	src/herramienta/csv/Rfc4180Reader.java	47	en	/** Reads one logical row (or null at EOF) into a flat string. */	-	
COMMENT_BLOCK	src/herramienta/csv/UtilitatsCsv.java	41	en	/** Returns the trimmed value for the given column name, or "" if the column is absent * or its index exceeds the row length. Note: callers cannot distinguish a missing column * from a column whose value is actually empty — both yield "". For column-presence checks, * use hMap.containsKey(col) first. */	-	
COMMENT_BLOCK	src/herramienta/csv/UtilitatsCsv.java	62	en	/** Returns the trimmed value parsed as a double, or 0.0 on null/blank/parse failure. */	-	
COMMENT_BLOCK	src/herramienta/csv/UtilitatsCsv.java	79	en	/** Parses an ISBN string, converting any valid ISBN-10 to ISBN-13. * Delegates to {@link herramienta.Isbn13Normalizer#toIsbn13(String)} * (the canonical implementation) so the ISBN-10→13 checksum and * the lowercase-X tolerance are in one place — per the tot.txt LOW * finding on the three near-duplicate parseIsbn / normalizeIsbn13 * / toIsbn13 implementations. Falls back to a digit-only pass-through * for inputs that don't match the normalizer's strict shape, so the * CSV import path can still recover from unusual exports (the * normalizer returns null for those, but the CSV path wants a best * effort rather than a failed row). */	-	
COMMENT_BLOCK	src/herramienta/csv/UtilitatsCsv.java	97	en	/** Returns true if a book with the given ISBN already exists in the library (skip on import). */	-	
COMMENT_BLOCK	src/herramienta/export/ExportadorCsv.java	10	mixed	/** Delegating thin entry point; CSV export logic lives in {@link BookExporter#exportCSV}. */	-	
COMMENT_BLOCK	src/herramienta/export/GoodreadsExportService.java	17	en	/** Shared Goodreads-compatible CSV export used by API and Swing. */	-	
STRING	src/herramienta/export/GoodreadsExportService.java	46	mixed	Condition,Condition Description,BCID	-	
COMMENT_BLOCK	src/interficie/BibliotecaAdmin.java	5	en	/** * Danger-zone administrative operations. * Separate sub-interface so consumers that only need reads/writes (UI, API) need not depend * on backup/restore/clear capabilities. */	-	
COMMENT_BLOCK	src/interficie/BibliotecaReader.java	15	en	/** Loads notes/descripcio for a book returned by a light table query. */	-	
COMMENT_BLOCK	src/interficie/BibliotecaWriter.java	7	en	/** * CRUD surface of the library. The three danger-zone admin operations * (backup / restore / clearAll) are declared on the * {@link BibliotecaAdmin} sub-interface; this interface extends it for * backward-compatibility (per the tot.txt MEDIUM finding) but the * canonical way to obtain an admin handle is to require * {@code BibliotecaAdmin} in the constructor — most consumers do not * need those operations and should not have them in scope. * * <p>The "extending BibliotecaAdmin" half of the split is the * compatibility fix: legacy code that calls * {@code cd.backupToSQL(f)} on a {@code BibliotecaWriter} continues to * compile. New code that needs the admin operations should declare the * field as {@code BibliotecaAdmin} (the narrower type) so the type * system flags any caller that does not need to be trusted with * danger-zone operations. */	-	
COMMENT_BLOCK	src/interficie/BookReader.java	32	en	/** * Lean variant of {@link #getAllLlibres()}: same result, but explicitly * documented to skip heavy text/blob columns (descripcio, notes, cover). * Default delegates to {@code getAllLlibres} for backends that already * return a light view (in-memory tests) or where the cost is acceptable. */	-	
COMMENT_BLOCK	src/interficie/BookWriter.java	7	en	/** @deprecated prefer {@link #deleteLlibre(Long)} so callers carry only the key, not the full record. */	-	
COMMENT_BLOCK	src/interficie/InMemoryBibliotecaReader.java	13	en	/** * Pure-in-memory {@link BibliotecaReader} for fast UI / pure-logic tests that don't need JDBC. * Read-only; mutate the supplied lists between operations. * * <p>Note: {@link #aplicarFiltres} returns the full list unchanged — this * class does NOT honour filter criteria. Tests of filter logic must use a * real {@link BibliotecaReader} (e.g. via {@code ControladorDomini}). */	-	
STRING	src/interficie/InMemoryBibliotecaReader.java	34	en	Not found:	-	
COMMENT_BLOCK	src/interficie/InMemoryBibliotecaReader.java	38	en	/** Rebuilds the ISBN→Llibre index. Callers that mutate {@link #books} should * treat the index as dirty (handled by the dirty flag). */	-	
STRING	src/main/ShutdownHooks.java	13	mixed	shutdown-hook-	-	
STRING	src/persistencia/AutorDao.java	45	en	Error creant autor:	-	
COMMENT_BLOCK	src/persistencia/AutorRow.java	3	en	/** JDBC row for {@code autor}; used by {@link AutorDao} and backup export. */	-	
COMMENT_BLOCK	src/persistencia/ControladorPersistencia.java	12	en	/** * Singleton facade over all DAOs. All public methods are {@code synchronized} so that * callers (ControladorDomini and the API layer) get thread-safe access without needing * to lock individual DAOs. * * <p>This wrapper exists to centralise lifecycle (singleton, test reset, profile * switch, shutdown hook) and to give {@code ControladorDomini} a single dependency. * DAOs are not individually synchronised — every call path goes through a * {@code synchronized} method here, so DAO-level {@code synchronized} would be * redundant double-locking. * * <p>Lifecycle: {@link #getInstance()} opens a JDBC connection and initialises DAOs; * {@link #resetForTest()} clears data and discards the instance (between test groups); * {@link #resetForProfileSwitch()} closes the connection (H2 ↔ MariaDB profile switch). * * <p><b>TODO (perf):</b> substituir el {@code synchronized} per un * {@link java.util.concurrent.locks.ReadWriteLock} (lectures concurrents, escriptures * exclusives). Conversió mecànica però tediosa. */	-	
COMMENT_BLOCK	src/persistencia/ControladorPersistencia.java	106	en	/** * Alias for {@link #getAllLlibres()} retained for caller compatibility. * The DAO already returns the light view (no descripcio/notes); heavy * fields must be loaded per-book via {@link #loadHeavyFields}. */	-	
COMMENT_BLOCK	src/persistencia/ControladorPersistencia.java	123	en	/** * Atomically restore the database from a SQL backup file: clears * all data and executes the file's statements inside a single JDBC * transaction. If any statement in the file fails, the entire * transaction is rolled back and the database is unchanged. Used by * {@link domini.facade.BackupDelegate#restoreFromSQL(java.io.File)} to * close the "kill the app between clearAllData() and executeSQLFile()" * window. The pre-restore snapshot to a temp file is the user-facing * undo path; this method is the in-process safety net. */	-	
COMMENT_BLOCK	src/persistencia/ControladorPersistencia.java	144	en	/** * Batched counterpart to {@link #loadHeavyFields(long, Llibre)}: one * round-trip for N books instead of N. The caller supplies an ISBN * list AND a lookup function that maps each ISBN to the in-memory * {@link Llibre} to mutate. The DAO does not depend on the * in-memory state; this is the only path that closes the N+1 * pre-load in {@link domini.facade.BackupDelegate}. */	-	
COMMENT_BLOCK	src/persistencia/ControladorPersistencia.java	162	en	/** * Exposes the underlying JDBC connection for callers that need to run * multi-statement transactions (e.g. {@link herramienta.BackupService} * takes a SERIALIZABLE snapshot of all tables). Callers MUST * synchronize on this {@code ControladorPersistencia} instance while * holding the connection so the DAOs do not observe a half-set * transaction state — every DAO method is {@code synchronized} on * this same monitor, so a {@code synchronized(cp)} block around the * transaction keeps the connection state consistent. */	-	
COMMENT_BLOCK	src/persistencia/ControladorPersistencia.java	223	en	/** @deprecated use {@link #getAllAutorRows()} — kept for callers that need the Object[] shape. */	-	
COMMENT_BLOCK	src/persistencia/ControladorPersistencia.java	230	en	/** @deprecated use {@link #getAllLlibreAutorRows()} — kept for callers that need the Object[] shape. */	-	
COMMENT_BLOCK	src/persistencia/ControladorPersistencia.java	241	mixed	/** For backup/export only — no UI consumer yet. */	-	
STRING	src/persistencia/JarLocator.java	133	mixed	no-jars	-	
COMMENT_BLOCK	src/persistencia/LecturaRow.java	6	en	/** Reading-session row for backup/export. Not wired in the Swing UI yet. */	-	
COMMENT_BLOCK	src/persistencia/LecturaRow.java	12	en	/** Parse a DATE column ({@code YYYY-MM-DD}) into a {@code LocalDate}; null/empty → null. */	-	
COMMENT_BLOCK	src/persistencia/LlibreAutorRow.java	3	en	/** Join row {@code llibre_autor}; used by {@link AutorDao} and backup export. */	-	
COMMENT_BLOCK	src/persistencia/LlibreBlobDao.java	10	en	/** Read/write of the {@code imatge_blob} and lazy {@code descripcio}/{@code notes} load. */	-	
STRING	src/persistencia/LlibreBlobDao.java	51	en	Error carregant camps pesats:	-	
COMMENT_BLOCK	src/persistencia/LlibreBlobDao.java	55	en	/** * Batched counterpart to {@link #loadHeavyFields(long, Llibre)}: one * round-trip for N books instead of N. Books not found in the DB * (e.g. deleted between the caller computing the ISBN list and this * SELECT running) are silently skipped — the in-memory instance * keeps its stale "not loaded" state. Books that are already * {@code isHeavyFieldsLoaded()} should be filtered out by the caller * (it has the in-memory reference) to avoid an unnecessary * {@code target.set*} no-op. */	-	
STRING	src/persistencia/LlibreBlobDao.java	94	en	Error carregant camps pesats (batch):	-	
COMMENT_BLOCK	src/persistencia/LlibreFieldBindings.java	9	en	/** * Single source of truth for the 25+ shared fields between {@code Llibre} * {@code INSERT} and {@code UPDATE} statements. The DAO used to spell * out the same 25 line set of {@code ps.setXxx(N, ll.getYyy())} calls in * three places (insert, update, plus a slightly-different shape in * {@code BackupService.writeLlibreINSERT}). This class centralises the * <i>value list</i> per SQL so all callers stay in lockstep when a new * column is added. * * <p>Each value can be: * <ul> * <li>A boxed primitive / String / byte[] / java.sql.Date — bound as-is.</li> * <li>{@code null} — bound as SQL NULL of the inferred type.</li> * <li>{@link Null} with a specific SQL type — bound as typed NULL.</li> * </ul> * Type coercion mirrors the previous inline behaviour (empty string * instead of NULL for short text fields; boxed-default for null * numerics). */	-	
COMMENT_BLOCK	src/persistencia/LlibreFieldBindings.java	32	en	/** Marker for a SQL NULL of a specific JDBC type. */	-	
COMMENT_BLOCK	src/persistencia/LlibreFieldBindings.java	43	en	/** * Single value-list builder used by both {@link #forInsert(Llibre)} * and {@link #forUpdate(Llibre)}. The two SQLs differ only in: * <ul> * <li>Whether the {@code ISBN} column is included (INSERT only).</li> * <li>Whether the {@code imatge_blob} column is included * (INSERT only).</li> * </ul> * This is the entire delta — same order, same value coercion * helpers, same bind contract. The previous implementation had * two near-identical 25/27-line arrays that drifted out of sync * the moment a column was added (the tot.txt HIGH finding on * {@code LlibreDaoCore}). */	-	
COMMENT_BLOCK	src/persistencia/LlibreFieldBindings.java	89	en	/** 27-value list for the INSERT (includes {@code ISBN} and * {@code imatge_blob}). */	-	
COMMENT_BLOCK	src/persistencia/LlibreFieldBindings.java	93	en	/** 25-value list for the UPDATE (no {@code imatge_blob}, no * {@code ISBN}). The caller appends the WHERE-clause ISBN. */	-	
COMMENT_BLOCK	src/persistencia/LlibreFieldBindings.java	97	en	/** 27-column list for the INSERT, in the same order as * {@link #forInsert(Llibre)}. Used by {@code BackupService} * to generate the SQL column list without re-typing the names * (per the tot.txt MEDIUM finding on INSERT column-list drift). */	-	
COMMENT_BLOCK	src/persistencia/LlibreFieldBindings.java	108	en	/** Render the column list as a single comma-separated SQL fragment * (e.g. {@code "`ISBN`,`nom`,`any`,..."}) for use in an INSERT. */	-	
COMMENT_BLOCK	src/persistencia/LlibreFieldBindings.java	119	en	/** Render the placeholder list for a 27-value INSERT * (e.g. {@code "?,?,?,...,?"}). */	-	
COMMENT_BLOCK	src/persistencia/LlibreFieldBindings.java	136	en	/** Binds a single value to the PreparedStatement, with the type-aware * null handling the old inline code used to do per call. */	-	
COMMENT_BLOCK	src/persistencia/LlibreLecturaDao.java	10	en	/** Reads from the {@code lectura} table (one row per reading session). */	-	
COMMENT_BLOCK	src/persistencia/LlibreLlistaRow.java	3	en	/** Membership row joining a book (isbn) to a shelf (llistaId), with per-shelf rating and read flag. */	-	
COMMENT_BLOCK	src/persistencia/LlibreMapper.java	8	en	/** * Static mappers from {@link ResultSet} rows to {@link Llibre} domain objects. * Shared between {@link LlibreDaoCore} (own SELECTs) and {@link LlistaDao} * (cross-table SELECTs that read llibre rows). Column names must match * the canonical SELECT lists in {@link LlibreDaoCore}. */	-	
COMMENT_BLOCK	src/persistencia/LlibreMapper.java	46	en	/** @param includeNotes if true, read the {@code notes} column from the result set */	-	
COMMENT_BLOCK	src/persistencia/LlibreSearchDao.java	15	en	/** Unified-filter search for the main library table. */	-	
COMMENT_LINE	src/persistencia/LlibreSearchDao.java	44	en	"if (f.getX() != null) { sql.append(" AND ..."); params.add(...); }"	-	
COMMENT_BLOCK	src/persistencia/LlibreSearchDao.java	88	en	/** * Append {@code clause} to {@code sql} and {@code value} to {@code params} * if {@code value} is non-null. No-op when {@code value} is null — the * caller can pass a pre-computed value (e.g. {@code "%foo%"}) and let the * helper decide whether to bind it. Reduces the search() body from * ~20 branches of {@code if (f.getX() != null) { sql.append(...); params.add(...); }} * to single-line per-condition calls. */	-	
COMMENT_BLOCK	src/persistencia/LlibreTagRow.java	3	en	/** Membership row joining a book (isbn) to a tag (tagId). */	-	
COMMENT_BLOCK	src/persistencia/LlistaDao.java	210	en	/** * Single SQL query shared by {@link #getLlistesForLlibre} and * {@link #getLlistesForLlibreContext}. Columns in the result row: * 0=id, 1=nom, 2=valoracio, 3=llegit, 4=ordre, 5=color. */	-	
COMMENT_BLOCK	src/persistencia/MapejadorsFiles.java	11	en	/** Shared DAO helpers for read-only queries. */	-	
COMMENT_BLOCK	src/persistencia/OverdueLoan.java	5	en	/** * Row returned by {@link PrestecDao#getOverdue}: a currently-loaned book * whose loan is older than the threshold (and not yet returned). * * <p>Replaces the previous {@code List<Object[]>} three-element row * (nomPersona, nomLlibre, dataPrestec) with a typed record so callers * get compile-time field access and don't have to cast {@code row[0..2]}. */	-	
STRING	src/persistencia/PrestecDao.java	38	en	Book with ISBN	-	
COMMENT_BLOCK	src/persistencia/PrestecRow.java	8	en	/** Loan row. isbn is a primitive long (always present FK). */	-	
COMMENT_BLOCK	src/persistencia/RelationRow.java	3	en	/** * Marker for book-relation row records: {@link LlibreLlistaRow}, {@link LlibreTagRow}, * {@link LlibreAutorRow}. Permits cross-cutting handlers (export/import, audit dumps) * to dispatch generically. */	-	
COMMENT_BLOCK	src/persistencia/Schema.java	3	en	/** * Schema constants shared across DAOs and the backup path. */	-	
COMMENT_BLOCK	src/persistencia/Schema.java	9	en	/** * Correct DELETE order to satisfy foreign keys: child tables first * (those with FKs to others), parent tables last. Shared between * {@link LlibreDaoCore#clearAllData()}, {@link herramienta.BackupService#backupToSQL} * and any bulk reset — the order must stay in sync or the FOREIGN KEY * constraints will fail in strict mode. */	-	
COMMENT_BLOCK	src/persistencia/ServerConect.java	113	en	/** Set once inside {@link #createDatabase(ConnectionConfig)} or * {@link #createDatabase()} and never reassigned; {@link #closeConnection()} * nulls the field after closing. Do not assign a new Connection elsewhere. */	-	
COMMENT_BLOCK	src/persistencia/ServerConect.java	126	en	/** Variant that takes the password as a {@code char[]} so the caller can * zero it after the call instead of leaving a {@code String} in the heap. * <p>The JDBC {@link java.sql.Driver#connect(String, java.util.Properties)} * contract requires the password as a {@code String} in the * {@code Properties} bag, so a single {@code String} copy of the password * is unavoidable for MariaDB; the copy lives inside the * {@link Connection}'s {@code Properties} until the driver releases it. * The caller's {@code char[]} is the only chunk this method can keep * short-lived: zero it as soon as the connection succeeds or fails. */	-	
STRING	src/persistencia/ServerConect.java	173	mixed	;MODE=MySQL;NON_KEYWORDS=VALUE;CACHE_SIZE=8192	-	
STRING	src/persistencia/ServerConect.java	191	mixed	Unknown dbType:	-	
STRING	src/persistencia/ServerConect.java	192	en	(expected 'h2' or 'mariadb')	-	
COMMENT_BLOCK	src/persistencia/ServerConect.java	203	en	/** Map a known dbType to the matching {@code err_db_init_*} i18n key. * Throws for unknown types so the caller never falls through to * {@code I18n.t("err_db_init_" + dbType)} (which would return the * key itself for unknown types, masking the real error). */	-	
COMMENT_BLOCK	src/persistencia/ServerConect.java	215	en	/** Rejects path separators in H2 file profile names (jdbc:h2:dir/profile). */	-	
COMMENT_BLOCK	src/persistencia/ServerConect.java	224	en	/** * Applies pending schema migrations and records each completed version in * {@code schema_version}. The transaction strategy depends on the connected * engine, detected via {@link java.sql.DatabaseMetaData#getDatabaseProductName()}. * * <p><b>H2</b> supports transactional DDL. The DDL statement and the matching * {@code schema_version} INSERT run inside a single JDBC transaction * ({@code setAutoCommit(false)} → execute → {@code commit}, or * {@code rollback} on failure). A mid-sequence failure on H2 leaves the * schema unchanged and the migration is re-attempted on the next startup. * * <p><b>MariaDB / MySQL</b> do not support transactional DDL: every DDL * statement implicitly commits and cannot be rolled back. On these engines * the {@code setAutoCommit(false)} wrapper is intentionally skipped and * each migration is applied as two independent atomic operations: the DDL * (auto-committed) followed by the {@code schema_version} INSERT (its own * auto-commit transaction). If the INSERT fails it is logged and the * migration is left unrecorded so the next startup will retry it. If the * DDL itself fails, the connection may have partially-migrated state: * operators must inspect {@code schema_version} (which lists the * migrations recorded as applied) and the live schema to determine which * statements completed and finish the migration manually. All DDL in * {@link #MIGRATIONS} is written to be idempotent (guards such as * {@code IF NOT EXISTS}), so a retry against a partially-migrated schema * is safe. */	-	
COMMENT_BLOCK	src/persistencia/ServerConect.java	303	en	/** * Stub kept for clarity — the original 32 → 33 → 34 atomicity plan * (see tot.txt F6) was rejected because the H2 32→33→34 sequence * runs into "Column AUTOR not found" when 32 is re-executed after * a previous partial commit (a real edge case the existing per- * migration transaction model tolerates but the merged * runMigrations3234H2 did not). The original three-transaction * model is preserved; the tot.txt finding is documented but the * fix is not applied here to keep the migration behaviour * identical to the long-shipped version. */	-	
STRING	src/persistencia/ServerConect.java	316	en	see comment	-	
COMMENT_BLOCK	src/persistencia/ServerConect.java	335	en	/** * Drops {@code llibre.autor} only when every non-empty legacy author row has a * matching {@code llibre_autor} link. Skips (returns false) if unmigrated rows remain. */	-	
STRING	src/persistencia/ServerConect.java	349	mixed	Migration 34: skipping DROP COLUMN autor —	-	
STRING	src/persistencia/ServerConect.java	393	en	did not accept URL:	-	
STRING	src/persistencia/ServerConect.java	407	mixed	Driver JAR '	-	
COMMENT_BLOCK	src/persistencia/TagDao.java	26	en	/** * Startup assertion: every column the in-memory path in * {@code StatsDelegate.IN_MEMORY_EXTRACTORS} knows about must also be * in the SQL whitelist. Without this check the two paths drift * silently — a column added to the in-memory extractors but not the * SQL whitelist would yield a smaller result set when the SQL path * is exercised (e.g. for a column that doesn't have an in-memory * extractor at all). Reference is by string to keep this DAO * ignorant of the facade layer (which would create a cycle). */	-	
STRING	src/persistencia/TagDao.java	48	mixed	TagDao.AUTOCOMPLETE_COLUMNS is missing the in-memory extractors:	-	
STRING	src/persistencia/TagDao.java	83	en	createTag: no generated key returned	-	
COMMENT_BLOCK	src/presentacio/AjudaEstadistiques.java	221	en	/** * Builds the "General" stats tab: goal progress + text summary + per-shelf table. * All view construction lives here so the controller stays slim. */	-	
COMMENT_BLOCK	src/presentacio/AjudaVisualitzacioCoberta.java	9	en	/** * Shared cover-image loading: BLOB → URL → default fallback. Lifted from inline logic in * detail panels so all four cover surfaces use the same precedence. * * <p>Per the tot.txt LOW finding: the load runs on a {@link SwingWorker} so a * 500 KB cover decode no longer stutters the EDT. The {@code target} is * captured by reference; the {@code done()} callback checks the JLabel is * still showing the same target (its component identity, not its contents) * via the captured `this` reference. */	-	
COMMENT_BLOCK	src/presentacio/BibliotecaTableModel.java	12	en	/** Table model for the main library book table. */	-	
COMMENT_BLOCK	src/presentacio/BibliotecaTableModel.java	87	en	/** Mutable backing list (package-private use by {@link TableController} only). */	-	
COMMENT_BLOCK	src/presentacio/ConfiguracioDialog.java	23	en	/** * Settings dialog. Each visual section lives in its own class under * {@code presentacio.config.*}; this class wires them into a single * vertical column and exposes the public ctor / {@link #reloadFromConfig} * API that callers depend on. */	-	
COMMENT_BLOCK	src/presentacio/ControladorAccionsLlibre.java	24	mixed	/** Book CRUD, details dialogs, stats, quick views, and undo. */	-	
STRING	src/presentacio/ControladorExportacio.java	157	mixed	no tenia	-	
COMMENT_BLOCK	src/presentacio/ControladorFiltre.java	20	en	/** Filter drawer, search bar, and preset management. */	-	
COMMENT_BLOCK	src/presentacio/ControladorGaleria.java	8	en	/** Thin coordinator for {@link GaleriaCobertesPanel} book list updates. */	-	
COMMENT_BLOCK	src/presentacio/ControladorMarcPrincipal.java	25	en	/** * Facade over the 6 sub-controllers (bookActions, bookIO, contextMenu, filter, * shelf, tablePage) for the main screen. MostrarBibliotecaControl owns the * table/list rendering; this class owns the top-level frame, menubar, and * orchestration. */	-	
COMMENT_BLOCK	src/presentacio/ControladorMarcPrincipal.java	302	en	/** * If the user is currently viewing a specific shelf, add a newly created * book to that shelf so it shows up immediately. No-op when: * - the book is not new (this helper is for {@code isNew} only), * - the user is on "All books" (no shelf filter), * - the add fails (caller already handled the user-facing error). */	-	
COMMENT_BLOCK	src/presentacio/ControladorMenuContextual.java	24	en	/** Table and gallery context menus, batch edit, duplicate, loan. */	-	
COMMENT_BLOCK	src/presentacio/ControladorModeVista.java	16	en	/** Gallery vs table view, series grouping, and title bar. */	-	
COMMENT_BLOCK	src/presentacio/ControladorMostrarBiblioteca.java	15	en	/** Slim coordinator for the main library screen; delegates to sub-controllers. */	-	
COMMENT_BLOCK	src/presentacio/ControladorPaginaTaula.java	10	en	/** * Manages pagination state for the book table. * <p>Two pagination modes exist: * <ul> * <li><b>In-memory</b> ({@code useDBPagination=false}): the full library is held in a * list and sliced by {@link #PAGE_SIZE}.</li> * <li><b>DB-level</b> ({@code useDBPagination=true}): pages are fetched from the database * via {@code getLlibresPage(offset, limit)}; this activates when the library exceeds * {@code ControladorDomini.isLargeLibrary()}.</li> * </ul> * <p>The pagination model ({@code currentPage}, {@code paginatedMode}, * {@code useDBPagination}) is read by the enclosing * {@link MostrarBibliotecaControl} via getter methods; direct field access by * other classes is discouraged. */	-	
COMMENT_LINE	src/presentacio/ControladorPaginaTaula.java	27	en	PAGE_SIZE is unrelated to ControladorDomini.SQL_FILTER_THRESHOLD (2000); pagination kicks in at 100 rows, SQL-filter at 2000 books.	-	
COMMENT_BLOCK	src/presentacio/ControladorPrestatgeria.java	15	en	/** Shelf (llista) combo, drag-to-shelf, and list membership actions. */	-	
COMMENT_BLOCK	src/presentacio/ControladorTaula.java	26	en	/** Table view: model, renderers, column visibility, table UX listeners. */	-	
COMMENT_BLOCK	src/presentacio/FormFieldRegistry.java	18	en	/** * Declarative registry for the filter-drawer form. * * <p>The drawer's 32 hand-declared components (text fields, checkboxes, * combo boxes, buttons) are described by a list of {@link Field} specs. * {@link #build()} instantiates the right {@link JComponent} subclass * for each spec and stores it in a name -> component map. Public * getters on {@link FilterDrawerPanel} continue to expose the same * component instances (e.g. {@code getTextISBN()}) so all 217 external * call sites keep working unchanged. */	-	
COMMENT_BLOCK	src/presentacio/FormFieldRegistry.java	33	en	/** * Declarative spec for a single field. * * @param key logical name used by the registry (e.g. "textISBN") * @param labelKey i18n key for the field's label * @param kind component kind (text / check / combo / button) * @param tooltipKey i18n key for the tooltip, or {@code null} * @param width preferred width in characters (text/combo only) * @param defaultText initial text (text fields only) */	-	
COMMENT_BLOCK	src/presentacio/FormFieldRegistry.java	72	en	/** Append a spec to the registry. Order is preserved. */	-	
COMMENT_BLOCK	src/presentacio/FormFieldRegistry.java	78	en	/** * Instantiate one {@link JComponent} per spec and register it under * {@link Field#key()}. Subsequent {@link #get(String)} calls return * the live component. * * <p>Each spec is responsible for one component; the per-component * styling (font, palette colour, tool tip) is applied in the panel * once the components are wired into the layout — this method only * creates instances and sets the bare-minimum defaults (text, * tool tip text). */	-	
COMMENT_BLOCK	src/presentacio/FormFieldRegistry.java	116	en	/** * Raw-typed accessor kept for the 60+ existing callers that * declared {@code JComboBox<String>} or {@code JComboBox<Object>} * locally. A typed variant would require per-component knowledge * (T from the spec) — not worth the migration given the * existing call sites work as-is. Documented in the tot.txt * LOW finding; kept raw for backward compatibility. */	-	
COMMENT_BLOCK	src/presentacio/FormFieldRegistry.java	140	en	/** * Legacy API for the 25-line skeleton callers that wired * components by hand before the declarative spec existed. * Currently only {@code DetallesLlibrePanel.addFieldEntry} * still uses this (line 295 of that file); the registry-based * spec system is the supported path. Marked {@link Deprecated} * for removal once the last external caller migrates. */	-	
COMMENT_BLOCK	src/presentacio/FormGridBuilder.java	11	en	/** * Shared row-builder for the new-book and edit-book dialogs. Each {@code addX} call appends a * (label, control) pair to a host {@link JPanel} configured with {@link GridLayout}. * * NOTE: DetallesLlibrePanel and GuardarLlibresDialogo still use their own private * addFieldEntry/addComboEntry/addCheckEntry methods instead of this class. Migrating them * to use FormGridBuilder (plus a registry for field binding) would eliminate the duplication * tracked in todo1.txt [un] items for those two classes. */	-	
COMMENT_BLOCK	src/presentacio/GestioLlistesListener.java	3	en	/** Explicit callback interface for shelf-management actions (replaces raw Runnable). */	-	
COMMENT_BLOCK	src/presentacio/GestorArrossegarSoltarPrestatgeria.java	14	en	/** Attaches drag-to-shelf drop targets; keeps DnD logic out of {@link LeftSidebarPanel}. */	-	
COMMENT_BLOCK	src/presentacio/KeyboardShortcuts.java	10	en	/** * Keyboard shortcut wiring extracted from {@link MainFrameControl}. * Use {@link #bind} to register a key combination with a no-arg action. */	-	
COMMENT_BLOCK	src/presentacio/LibraryScreenHost.java	7	en	/** * Callbacks from sub-controllers back to {@link MostrarBibliotecaControl}. * * <p>Acts as a thin controller registry: every sub-controller can be * obtained through a typed accessor instead of via a direct field * reference on the coordinator. The "single injection point" the * tot.txt MEDIUM finding called for is the constructor of * {@link MostrarBibliotecaControl} — controllers are constructed there * and wired via the accessors below, so adding a 7th controller means * editing only the constructor (and adding a one-line accessor here). */	-	
COMMENT_BLOCK	src/presentacio/LibraryViewState.java	13	en	/** Shared mutable state for the main library screen sub-controllers. */	-	
COMMENT_BLOCK	src/presentacio/LlibreFormBinder.java	7	en	/** * Shared field-binding for the new-book ({@code GuardarLlibresDialogo}) and edit * ({@code DetallesLlibrePanel}) dialogs. Currently a thin helper exposing getText/setText * mappings; future refactor will host the full 27-field bind code. */	-	
COMMENT_BLOCK	src/presentacio/LlistaTableModel.java	10	en	/** * Table model for shelf data (id, name, count, color). * Extracted from GestioLlistesDialog so it can be reused * by the stats dialog or any future shelf list view. */	-	
COMMENT_BLOCK	src/presentacio/MemoriaImatgesCoberta.java	10	en	/** LRU cover icons for table + gallery (shared across main screen). */	-	
COMMENT_BLOCK	src/presentacio/MemoriaImatgesCoberta.java	12	mixed	/** Cached marker: no cover available (avoids re-fetch on every repaint). */	-	
COMMENT_BLOCK	src/presentacio/PaginationModel.java	3	en	/** Pagination state extracted from {@link TablePageController}. */	-	
COMMENT_BLOCK	src/presentacio/PaginationView.java	14	en	/** @param page zero-based page index */	-	
COMMENT_BLOCK	src/presentacio/PanelCalaixFiltre.java	28	en	/** * Sidebar drawer that holds the filter inputs (text fields, checkboxes, * combo boxes), the preset bar, the apply/clear buttons, and the * export/import/backup buttons. * * <p>As of R1, the 32 hand-declared component fields are replaced by a * single {@link FormFieldRegistry} of declarative specs. The 32 public * getters are kept as 1-line shims that delegate to the registry, so * the 217 external call sites (most prominently * {@link MostrarBibliotecaPanel}) keep compiling without changes. */	-	
COMMENT_BLOCK	src/presentacio/PanelCalaixFiltre.java	274	en	/** * Single source of truth for the 11 button styles shared by * {@link #applyTheme()} and {@link #applyThemePostLaf()}. The * tot.txt MEDIUM finding flagged the duplicated block; the * helper keeps the two entry points in lockstep when a new * button is added to the registry. */	-	
COMMENT_BLOCK	src/presentacio/PanelCalaixFiltre.java	399	en	/** * Declarative triple for a single labelled text-field filter row. * Used by {@link #buildFilterDrawer()} to declare the three "row1b" * text fields (editorial / serie / idioma) without repeating the * label-key + new-JTextField boilerplate three times. */	-	
COMMENT_BLOCK	src/presentacio/PanelCalaixFiltre.java	407	en	/** * Declarative triple for a single numeric-range filter row. * Used by {@link #buildFilterDrawer()} to declare the three "row2" * range fields (any / valoracio / preu) without repeating the * label-key + two new-JTextFields boilerplate three times. */	-	
COMMENT_BLOCK	src/presentacio/PanelGaleriaCobertes.java	42	en	/** * Grid of book covers, the gallery view of the library. * * <p>Composition: * <ul> * <li>{@link CoverImageService} — async LRU image cache + crop-scale.</li> * <li>{@link CoverCardFactory} — paints each card, wires mouse + selection * events back through a {@link CoverCardFactory.CardHost}.</li> * <li>{@link CoverZoomPopup} — hover-zoom overlay shown next to a card.</li> * </ul> * * <p>This class owns the scroll pane, the wrap layout, the selection / * focus state, the keyboard bindings, and the theme application. The * actual visual + image work lives in the three collaborators above. */	-	
COMMENT_BLOCK	src/presentacio/PanelMarcPrincipal.java	13	en	/** Main window content (library table + status bar). Frame chrome lives in {@link MainFrameControl}. */	-	
COMMENT_BLOCK	src/presentacio/PanelMostrarBiblioteca.java	81	en	/** * Order matters: (1) update palette-derived fields first, (2) sub-panel * pre-LaF overrides, (3) install L&F (resets all defaults), (4) re-apply * UIManager overrides on top of L&F, (5) updateComponentTreeUI, (6) * sub-panel post-LaF overrides for things the L&F reset. */	-	
COMMENT_BLOCK	src/presentacio/SplashScreen.java	72	mixed	/** Emergency cleanup: dispose the dialog regardless of timing. */	-	
COMMENT_BLOCK	src/presentacio/UIComponents.java	18	en	/** * presentacio-layer factory for theme-aware Swing components. * * <p>All presentacio code should call into this class instead of touching * {@link UITheme} styling fields directly, so that swapping to a different * look-and-feel does not require touching every panel/control. * * <p>The styling bodies (button / label / panel / field / cardBorder) were * moved here from {@code herramienta.UITheme} during the session-11 build * fix so {@code UITheme} only owns the theme state (colors, fonts, * UIManager keys) and {@code UIComponents} owns the panel-layer style * application. Field/colors that drive the look still come from * {@link UITheme}; the methods here just apply them. */	-	
COMMENT_BLOCK	src/presentacio/config/BarraBotonsConfiguracio.java	22	en	/** * Save / cancel button bar. Pulls field values from the tagged input * children of the section panels and persists them through * {@link Config} in a single batch. */	-	
COMMENT_BLOCK	src/presentacio/config/ConfiguracioDataSection.java	19	en	/** Data section: DB size display, clear-library button, profile picker. */	-	
COMMENT_BLOCK	src/presentacio/config/ConfiguracioDialogListener.java	3	en	/** Callbacks fired by the settings dialog. Replaces raw Runnable parameters. */	-	
COMMENT_BLOCK	src/presentacio/config/SeccioAparençaConfiguracio.java	17	mixed	/** Appearance section: theme, font size, currency symbol, default rating. */	-	
COMMENT_BLOCK	src/presentacio/config/SeccioDbConfiguracio.java	21	en	/** Database section: type / host / user / password + test-connection button. */	-	
COMMENT_BLOCK	src/presentacio/config/SeccionsConfiguracio.java	6	en	/** * Shared utilities for {@code presentacio.config.*} sections. * Each section panel tags its input children with a {@code ClientProperty * ("id", ...)} so the dialog can look them up by name in * {@link #reloadFromConfig(Container, String)}. */	-	
COMMENT_BLOCK	src/presentacio/config/SeccionsConfiguracio.java	15	en	/** Locate a tagged child by id. Recurses into nested containers. */	-	
COMMENT_BLOCK	src/presentacio/detalles/vista/DialegEtiquetesLlibre.java	31	en	/** * Dialog for managing the tags assigned to a single book. * <p> * Unlike {@link presentacio.detalles.vista.LlistesDelLlibreDialog}, which defers * shelf-membership changes until the user clicks OK/Save, this dialog applies every * tag add/remove operation immediately (via {@link presentacio.detalles.control.TagsDelLlibreControl}). * The immediate-persist model is chosen because tag operations are lightweight single * many-to-many rows, whereas shelf membership also carries per-book rating and read-state * that benefit from batch editing. */	-	
STRING	src/presentacio/detalles/vista/GuardarLlibresDialogo.java	162	mixed	YYYY-MM-DD	-	
COMMENT_BLOCK	src/presentacio/detalles/vista/UtilitatsComboEntitat.java	7	en	/** * Shared combo-reload logic for entity selection dialogs (shelves, tags, etc.). * Preserves the previously selected item by id after reloading the model. */	-	
COMMENT_BLOCK	src/presentacio/detalles/vista/UtilitatsComboEntitat.java	15	en	/** * Reloads a JComboBox from a fresh entity list, restoring the previously * selected item by its id. * * @param <E> entity type displayed in the combo * @param combo the combo to reload * @param items the full list of entities (fresh from domain) * @param idExtractor function returning the entity's numeric id */	-	
COMMENT_BLOCK	src/presentacio/galeria/FabricantTargetesCoberta.java	35	en	/** * Builds the visual card panels that fill the gallery. * * <p>Each card owns its own paint path (card body, footer, progress bar, * stars) and is wired to a {@link Listener} so the host panel can stay * in sync with selection / focus / open events. * * <p>The card receives the {@link CoverImageService} so async image loads * update the card's paint reference on the EDT. */	-	
COMMENT_BLOCK	src/presentacio/galeria/FabricantTargetesCoberta.java	62	en	/** * Host-side state + callbacks shared by every card built by this factory. * * <p>Replaces the former 7-method {@code Listener} interface (tot.txt MEDIUM * finding): the host ({@code GaleriaCobertesPanel}) builds a single * {@code CardHost} that captures its live selection / focus state plus * three callback consumers, and passes it to every card. The card's * mouse adapter reads {@link #selectedISBNs()} / {@link #focusedIdx()} / * {@link #currentLlibres()} for read-only access, mutates * {@link #focusedIdx()} via the int[] wrapper, and dispatches * {@link #onCardClicked}, {@link #onCardRightClicked}, and * {@link #onSelectionMutated} on user input. * * <p>The {@code int[]} wrapper for {@code focusedIdx} is the simplest * mutable-int carrier that survives both lambda capture and per-card * mouse-adapter mutation without resorting to {@code AtomicInteger}. */	-	
COMMENT_BLOCK	src/presentacio/galeria/FabricantTargetesCoberta.java	156	en	/** * Read-only snapshot of everything {@link #paintCard} needs to draw a * card. Carries the live references to {@code imgRef} and {@code hov} * so the image-loader / mouse-Entered mutations are visible on the * next paint. */	-	
COMMENT_BLOCK	src/presentacio/galeria/FabricantTargetesCoberta.java	172	en	/** * Static card paint — extracted from the anonymous JPanel's * paintComponent (per the tot.txt MEDIUM finding). All drawing * parameters come from the {@link CardState} record; the only * host interaction is the selection check. Testable in * isolation by constructing a CardState and a mock CardHost. */	-	
COMMENT_BLOCK	src/presentacio/galeria/FabricantTargetesCoberta.java	230	en	/** * Static mouse adapter — extracted from the inline MouseAdapter * (per the tot.txt MEDIUM finding). Handles hover repaints and * selection / right-click / double-click. The adapter is stateless * beyond the references it captures at construction. */	-	
COMMENT_BLOCK	src/presentacio/galeria/PopupZoomCoberta.java	20	en	/** * Hover-zoom popup shown next to a gallery card after a short delay. * * <p>The popup is a small JPanel added to the root pane's {@link JLayeredPane#POPUP_LAYER}. * It is owned by the gallery: only one popup can be visible at a time, and * it is removed when the host hides it (mouse exit, theme switch, list update, * shutdown). */	-	
COMMENT_BLOCK	src/presentacio/galeria/PopupZoomCoberta.java	40	en	/** * Schedule a zoom popup for {@code card}. The popup is shown * {@value #DELAY_MS} ms after the call (debounced — repeated calls reset * the timer). The image must already be in the {@link CoverImageService} * cache; if not, this call is a no-op. */	-	
COMMENT_BLOCK	src/presentacio/galeria/PopupZoomCoberta.java	53	en	/** Cancel any pending show; remove the overlay if visible. */	-	
COMMENT_BLOCK	src/presentacio/galeria/ServeiImatgesCoberta.java	21	en	/** * Async loader + LRU cache of pre-scaled cover images. * * <p>Keyed by {@link Llibre#getISBN()}. The cache value is the image already * scaled to the requested card dimensions, so the paint path can blit 1:1 * without per-frame scaling work. * * <p>Owns the single-threaded loader pool, the in-flight set, and the * crop-scale math. All access from the EDT is via {@link #submit(Llibre, int, int, Runnable)}; * background loading and cache mutation run on the executor, with the * repaint callback marshalled back to the EDT. * * <p>The LRU is protected by a {@link ReentrantReadWriteLock} so EDT reads * (the paint path, dominant) run concurrently while writes (cache miss → * load) take an exclusive lock. The previous {@code Collections.synchronizedMap} * wrapper serialised every {@code get()} against every other {@code get()}, * which stalled the EDT on a 200-card repaint (per the tot.txt LOW finding). */	-	
COMMENT_BLOCK	src/presentacio/galeria/ServeiImatgesCoberta.java	73	en	/** * Return the cached image for {@code isbn} at the current zoom, or {@code null} * if not yet loaded. The caller (card paint) must tolerate a {@code null} * return — the loader's onLoaded callback will repaint. Read-locked so * concurrent EDT repaints on a 200-card gallery do not serialise. */	-	
COMMENT_BLOCK	src/presentacio/galeria/ServeiImatgesCoberta.java	88	en	/** * Invalidate all cached images. Called when the zoom level changes * (pre-scaled images are now the wrong size) or on theme switch * if the gradient palette needs to be re-applied. */	-	
COMMENT_BLOCK	src/presentacio/galeria/ServeiImatgesCoberta.java	103	en	/** * Schedule background load + crop-scale for {@code l}. The {@code onLoaded} * callback runs on the EDT after the image lands in the cache (or the * load fails). The callback is also fired immediately if the image is * already cached, so callers can treat it as "image is ready, please repaint". */	-	
COMMENT_BLOCK	src/presentacio/galeria/ServeiImatgesCoberta.java	138	en	/** Read-locked containment check (used by submit() before scheduling). */	-	
COMMENT_BLOCK	src/presentacio/listener/EnActualizarBBDD.java	5	en	/** * Composite listener for book insert/update/delete events. * * <p>Merges the two parent contracts ({@link OnLlibreUpdate}, * {@link OnLlibreDelete}) so a presenter can subscribe to both * with a single field. Implementers must override * {@link OnLlibreUpdate#onBookUpdated} and * {@link OnLlibreDelete#onBookDeleted} (or use a default * implementation through a subclass). * * <p>Previously this interface also exposed a Catalan-named * alias ({@code actualitzarLlibre} / {@code eliminarLlibre}) * whose defaults delegated back to the English names, forming * a self-referential cycle that would loop forever if a * subclass overrode neither side. The alias has been removed: * the English names are the only contract, and the only * implementer in the codebase ({@code MainFrameControl}) * already overrides them. */	-	
COMMENT_BLOCK	src/presentacio/listener/OnLlibreDelete.java	5	en	/** UI callback when a book is deleted. */	-	
COMMENT_BLOCK	src/presentacio/listener/OnLlibreDelete.java	16	en	/** Cancellable/vetoed wrapper used by presenters that want to ask * listeners whether a delete should proceed. Created speculatively; * exercised by the test suite ({@code deleteEventCancellableVeto}, * {@code deleteEventNonCancellable}). */	-	
COMMENT_BLOCK	src/presentacio/listener/OnLlibreUpdate.java	5	en	/** UI callback after a book is created or updated. */	-	
COMMENT_BLOCK	src/presentacio/renderers/LlegitCheckBoxEditor.java	16	en	/** * Cell editor for the "llegit" column. Only persists the {@code llegit} * flag — all other fields on the loaded {@code Llibre} are stale * (the table model only loads the light row, not descripcio/notes). * The user is expected to open the details dialog (which calls * {@code loadHeavyFields}) before editing the read flag if they want * the other fields refreshed (per the tot.txt MEDIUM finding). */	-	
COMMENT_BLOCK	src/presentacio/renderers/SearchHighlightRenderer.java	15	en	/** Cached set reference — refreshed via {@link #setLoanedISBNs(Set)} from * {@link presentacio.TableController} whenever the host's * {@code state.loanedISBNs} is reassigned. Avoids the per-cell * {@code Supplier.get()} dispatch the previous API required (the * tot.txt MEDIUM finding flagged 10 000 lambda calls per repaint). */	-	

# --- from translate1.txt (330 rows) ---
IDENTIFIER	src/interficie/BookReader.java	7		BookReader (interface)	LectorLlibre	occ=7 (also referenced in: InMemoryBibliotecaReader, LlibreMapper, ServerConect, Configuracio, RenderitzadorCellaCoberta, AjudaEstadistiques, LlibreFormBinder, …) Base read interface over Llibre. Method names inside (obtenirLlibre, getSize, existsLlibre, get10Llibres, get100Llibres, maxIndex100Llibres, obtenirRecentlyAdded, esLargeLibrary, comptarLlibresDB, obtenirLlibresPage, aplicarFiltres, cercarLlibresSQL, obtenirLlibreBlob) are mostly Catalan — only the interface name is English.
IDENTIFIER	src/interficie/BookWriter.java	5		BookWriter (interface extends BookReader)	EscritorLlibre	occ=6 (also in: ControladorDomini, DelegatLlibre, ServeiCoberta, GuardarLlibresDialogoControl, AjudaEstadistiques, README.md)
IDENTIFIER	src/interficie/LoanReader.java	8		LoanReader (interface)	LectorPrestec	occ=2 (also in: BibliotecaReader)
IDENTIFIER	src/interficie/LoanWriter.java	3		LoanWriter (interface extends LoanReader)	EscritorPrestec	occ=1
IDENTIFIER	src/interficie/ShelfReader.java	9		ShelfReader (interface)	LectorPrestatgeria	occ=1
IDENTIFIER	src/interficie/ShelfWriter.java	5		ShelfWriter (interface extends ShelfReader)	EscritorPrestatgeria	occ=1
IDENTIFIER	src/interficie/TagReader.java	8		TagReader (interface)	LectorEtiqueta	occ=1
IDENTIFIER	src/interficie/TagWriter.java	5		TagWriter (interface extends TagReader)	EscritorEtiqueta	occ=1
IDENTIFIER	src/interficie/BibliotecaReader.java	7		BibliotecaReader (interface)	LectorBiblioteca	occ=1 Compound already in Catalan; flag only the "Reader" suffix.
IDENTIFIER	src/interficie/BibliotecaWriter.java	24		BibliotecaWriter (interface)	EscritorBiblioteca	occ=1
IDENTIFIER	src/interficie/BibliotecaAdmin.java	10		BibliotecaAdmin (interface)	AdministradorBiblioteca	occ=1
IDENTIFIER	src/interficie/InMemoryBibliotecaReader.java	21		InMemoryBibliotecaReader (class)	LectorBibliotecaEnMemoria	occ=1 Used in tests. English triple-word name. ### Identifiers in src/presentacio/
IDENTIFIER	src/presentacio/SplashScreen.java	12		SplashScreen (class)	PantallaInici / PantallaCarrega	occ=1 Pure-English name for the loading splash dialog.
IDENTIFIER	src/presentacio/LoadingDialog.java	7		LoadingDialog (class extends JDialog)	DialegCarrega	occ=1
IDENTIFIER	src/presentacio/PaginationView.java	6		PaginationView (class)	VistaPaginacio	occ=1
IDENTIFIER	src/presentacio/PaginationModel.java	4		PaginationModel (class)	ModelPaginacio	occ=1
IDENTIFIER	src/presentacio/LlistaTableModel.java	15		LlistaTableModel (class)	ModelTaulaLlista	occ=1 "Table" is a loanword commonly accepted in Catalan, "Llista" is Catalan. The class itself is mostly Catalan. Flag for review; "Model" suffix is also a loanword.
IDENTIFIER	src/presentacio/LlibreFormBinder.java	12		LlibreFormBinder (class)	EnquadernadorFormulariLlibre / LligadorFormulariLlibre	occ=1 "Form" + "Binder" both English.
IDENTIFIER	src/presentacio/LibraryViewState.java	14		LibraryViewState (class)	EstatVistaBiblioteca	occ=1
IDENTIFIER	src/presentacio/LibraryScreenHost.java	18		LibraryScreenHost (interface)	AmfitrioPantallaBiblioteca	occ=1 "Host" + "Screen" English.
IDENTIFIER	src/presentacio/LeftSidebarPanel.java	34		LeftSidebarPanel (class extends JPanel)	PanellBarraEsquerra	occ=1
IDENTIFIER	src/presentacio/FormValidator.java	15		FormValidator (class)	ValidadorFormulari	occ=1
IDENTIFIER	src/presentacio/FormGridBuilder.java	20		FormGridBuilder (class)	ConstructorGraellaFormulari	occ=1
IDENTIFIER	src/presentacio/FormFieldRegistry.java	29		FormFieldRegistry (class)	RegistreCampsFormulari	occ=1
IDENTIFIER	src/presentacio/BibliotecaTableModel.java	13		BibliotecaTableModel (class)	ModelTaulaBiblioteca	occ=1
IDENTIFIER	src/presentacio/KeyboardShortcuts.java	14		KeyboardShortcuts (class)	DreceresTeclat	occ=1
IDENTIFIER	src/presentacio/renderers/SearchHighlightRenderer.java	13		SearchHighlightRenderer (class)	RenderitzadorDestacatCerca	occ=1
IDENTIFIER	src/presentacio/renderers/ProgressBarRenderer.java	12		ProgressBarRenderer (class extends JProgressBar)	RenderitzadorBarraProgres	occ=1
IDENTIFIER	src/presentacio/renderers/LlegitCheckBoxEditor.java	24		LlegitCheckBoxEditor (class)	EditorCasellaLlegit	occ=1 Mixed; "Llegit" Catalan, "CheckBox" universal, "Editor" English → flag "Editor".
IDENTIFIER	src/presentacio/renderers/LlegitCheckBoxRenderer.java	11		LlegitCheckBoxRenderer (class)	RenderitzadorCasellaLlegit	occ=1 Same as above.
IDENTIFIER	src/presentacio/layouts/WrapLayout.java	31		WrapLayout (class extends FlowLayout)	DisposicióEmbolcall / keep as borrowed name	occ=1 Javadoc says "Adaptat de l'exemple públic 'WrapLayout' del tutorial de Java de Sun" — the name is borrowed from the source.
IDENTIFIER	src/presentacio/QuantADialog.java	25		QuantADialog (class)	QuantADialeg	occ=1 "Quant a" is Catalan, "Dialog" is English.
IDENTIFIER	src/presentacio/PanelTaulaCentral.java	33		jTableBilio (field)	taulaLlibres / jTaulaLlibres	occ=44 references across 8 files (PanelTaulaCentral, PanelMostrarBiblioteca, ControladorTaula, ControladorPrestatgeria, ControladorModeVista, ControladorMenuContextual, ControladorMarcPrincipal, ControladorFiltre, ControladorAccionsLlibre) "Bilio" is Catalan-influenced abbreviation of "Biblioteca" mixed with English "jTable" prefix. Should be "taulaLlibres".
IDENTIFIER	src/presentacio/PanelTaulaCentral.java	236		getjTableBilio() (getter)	obtenirTaulaLlibres()	occ=30+ references in 7 files.
IDENTIFIER	src/presentacio/PanelCalaixFiltre.java	374		getchckbxLlegit() (getter)	obtenirCasellaLlegit()	occ=3+ (also in PanelMostrarBiblioteca.java:159) "chckbx" is NetBeans-era auto-generated prefix; "Llegit" is Catalan.
IDENTIFIER	src/presentacio/PanelCalaixFiltre.java	375		getchckbxNoLlegit() (getter)	obtenirCasellaNoLlegit()	occ=2
IDENTIFIER	src/presentacio/PanelCalaixFiltre.java	376		getbtnFiltrar() (getter)	obtenirBtnFiltrar()	occ=2
IDENTIFIER	src/presentacio/PanelCalaixFiltre.java	41		scrolpaneFiltro (field)	panellDesplacamentFiltre / scrollFiltre	occ=6+
IDENTIFIER	src/presentacio/PanelCalaixFiltre.java	42		panelFiltros (field)	panellFiltres	occ=5+ "Filtros" is Spanish plural of "filtro"; Catalan plural is "filtres".
IDENTIFIER	src/presentacio/listener/EnActualizarBBDD.java	24		EnActualizarBBDD (interface, extends OnLlibreUpdate, OnLlibreDelete)	EnActualitzarBBDD	occ=38 references across 10 files "Actualizar" is Spanish; Catalan is "Actualitzar". This is a Spanish-influenced identifier.
IDENTIFIER	src/presentacio/listener/OnLlibreUpdate.java	6		OnLlibreUpdate (interface)	EnActualitzarLlibre / QuanLlibreActualitzat	occ=4
IDENTIFIER	src/presentacio/listener/OnLlibreDelete.java	7		OnLlibreDelete (interface)	EnEliminarLlibre / QuanLlibreEliminat	occ=6 Note that the internal event class `EsborrarEvent` IS Catalan (Esborrar = to delete in Catalan), so the file itself is mixed. The interface name uses English "On…".
IDENTIFIER	src/presentacio/listener/OnLlibreBlobChanged.java	4		OnLlibreBlobChanged (interface)	EnCanviarBlobLlibre	occ=1
IDENTIFIER	src/presentacio/listener/OnLlistaMembershipChanged.java	4		OnLlistaMembershipChanged (interface)	EnCanviarMembreLlista	occ=1
IDENTIFIER	src/presentacio/listener/OnLlibreAdded.java	6		OnLlibreAdded (interface)	EnAfegirLlibre	occ=1
IDENTIFIER	src/presentacio/listener/EnActualizarBBDD.java	7		onBookUpdated (method)	enActualitzarLlibre / quanLlibreActualitzat	occ=5 references in 5 files English "on…" callback naming convention; Catalan would be "en…" or "quan…".
IDENTIFIER	src/presentacio/listener/EnActualizarBBDD.java	8		onBookDeleted (method)	enEliminarLlibre	occ=4
IDENTIFIER	src/presentacio/listener/EnActualizarBBDD.java	10		onBookDeleting (method)	enEliminantLlibre	occ=4
IDENTIFIER	src/presentacio/listener/OnLlibreBlobChanged.java	5		onBlobChanged (method)	enCanviarBlob	occ=1
IDENTIFIER	src/presentacio/listener/OnLlistaMembershipChanged.java	5		onMembershipChanged (method)	enCanviarMembre	occ=1
IDENTIFIER	src/presentacio/listener/OnLlibreAdded.java	7		onAdded (method)	enAfegit	occ=1
IDENTIFIER	src/presentacio/GestioLlistesListener.java	4		GestioLlistesListener (interface)	EscoltadorGestioLlistes / GestioLlistesEscoltador	occ=1 "GestioLlistes" Catalan, "Listener" English.
IDENTIFIER	src/presentacio/GestioLlistesListener.java	5-7		onShelvesChanged / onShelfRenamed / onShelfDeleted (default methods)	enCanviarPrestatgeries / enReanomenarPrestatge / enEliminarPrestatge	occ=3
IDENTIFIER	src/presentacio/config/ConfiguracioDialogListener.java	5-8		onThemeChange / onProfileSwitch / onFontChange / onRefreshData (default methods)	enCanviarTema / enCanviarPerfil / enCanviarLletra / enRefrescarDades	occ=6 (also in BarraBotonsConfiguracio.java:90, ConfiguracioDataSection.java:79)
IDENTIFIER	src/presentacio/detalles/vista/DetallesLlibrePanel.java	37		DetallesLlibrePanel (class extends JDialog)	PanellDetallsLlibre / DetallsLlibrePanell	occ=11 references in 6 files (LlibreFormBinder.java:15, ControladorImportacio.java, ControladorMenuContextual.java, etc.) "Detalles" is Spanish; Catalan is "Detalls".
IDENTIFIER	src/presentacio/detalles/control/DetallesLlibrePanelControl.java	22		DetallesLlibrePanelControl (class)	ControladorPanellDetallsLlibre	occ=10+ references
IDENTIFIER	src/presentacio/detalles/vista/GuardarLlibresDialogo.java	35		GuardarLlibresDialogo (class extends JDialog)	DialegDesarLlibres	occ=12+ references in 6 files "Dialogo" is Spanish; Catalan is "Diàleg" / "Dialeg". "Guardar" is Spanish; Catalan is "Desar" or "Guardar" (loanword, but inconsistent with the rest of the codebase which uses "desar"/"posar").
IDENTIFIER	src/presentacio/detalles/control/GuardarLlibresDialogoControl.java	28		GuardarLlibresDialogoControl (class)	ControladorDialegDesarLlibres	occ=4+ references
IDENTIFIER	src/presentacio/detalles/vista/LlistesDelLlibreDialog.java	30		LlistesDelLlibreDialog (class extends JDialog)	DialegLlistesLlibre	occ=8+ references "Del" is Spanish contraction; Catalan would be "de" or simply "LlistesLlibre". "Dialog" is English → "Dialeg".
IDENTIFIER	src/presentacio/detalles/control/LlistesDelLlibreControl.java	16		LlistesDelLlibreControl (class)	ControladorLlistesLlibre	occ=4
IDENTIFIER	src/presentacio/detalles/vista/LlistesDelLlibreTableModel.java	9		LlistesDelLlibreTableModel (class)	ModelTaulaLlistesLlibre	occ=5
IDENTIFIER	src/presentacio/galeria/FabricantTargetesCoberta.java	45		FabricantTargetesCoberta (class)	keep	occ=1 Pure Catalan. Listed here only because neighbour classes were mixed.
IDENTIFIER	src/presentacio/galeria/PopupZoomCoberta.java	28		PopupZoomCoberta (class)	PopupZoomCoberta / keep	occ=1 "Popup" and "Zoom" are international loanwords, widely used in Catalan. Optional rename to "FinestretaZoomCoberta".
IDENTIFIER	src/presentacio/galeria/ServeiImatgesCoberta.java	39		ServeiImatgesCoberta (class)	keep	occ=1 Catalan. ### Identifiers in src/main/
IDENTIFIER	src/main/Ejecutable.java	19		Ejecutable (class — main entry point)	Executable	occ=1 "Ejecutable" is Spanish (and English). Catalan accepted form is "Executable" (the loanword). Flag because the suffix `-able` is consistent with the codebase, but the rest of the project avoids Spanish spellings. The Makefile pom.xml `<mainClass>main.Ejecutable</mainClass>` (pom.xml:75) would also need to be updated.
IDENTIFIER	src/main/ShutdownHooks.java	6		ShutdownHooks (class)	GanxosTancada	occ=1 Pure English. ### Identifiers in src/herramienta/
IDENTIFIER	src/herramienta/ConfigDTO.java	7		ConfigDTO (record)	ConfigDTO / RegistreConfiguracio / ConfiguracioDTO	occ=3 references (the record itself and fromConfig() factory) "DTO" = Data Transfer Object, an English abbreviation. Used in one file only; not yet a Catalan convention.
IDENTIFIER	src/herramienta/Lang.java	4		Lang (enum)	Idioma	occ=1 (the enum itself) + 6 references to `Lang.CA/ES/EN` and `Lang.fromCode(…)` "Lang" is universal (used in i18n contexts), but Catalan would be "Idioma". The values CA/ES/EN are universal language codes; keep those. Only the enum name needs a decision.
IDENTIFIER	src/herramienta/I18n.java	8		I18n (class)	keep	occ=1 "i18n" is a universal industry abbreviation — explicitly listed in the SKIP rules. Listed here only for completeness.
IDENTIFIER	src/herramienta/FiltreUtils.java	7		FiltreUtils (class)	UtilitatsFiltre	occ=1 "Filtre" Catalan, "Utils" English (universal). Flag the "Utils" suffix.
IDENTIFIER	src/herramienta/FieldAutoComplete.java	13		FieldAutoComplete (class)	AutocompletatCamp	occ=1 + 1 reference "Field" + "AutoComplete" both English. AdjuntarReturning internal class is Catalan — the rest of the file is mixed.
IDENTIFIER	src/herramienta/AutoCompletion.java	23		AutoCompletion (class extends PlainDocument)	Autocompletat	occ=1 Pure English class name; rest of file uses "Cerca…" (Catalan) in Javadoc. Note: file starts with a public-domain license header (English) — see LICENSE HEADERS section.
IDENTIFIER	src/herramienta/Completer.java	8		Completer (class)	Completador	occ=1 Pure English.
IDENTIFIER	src/herramienta/FormatOptions.java	8		FormatOptions (class)	OpcionsFormat	occ=1
IDENTIFIER	src/herramienta/FontSize.java	3		FontSize (enum)	MidaLletra	occ=1 Pure English.
IDENTIFIER	src/herramienta/Isbn13Normalizer.java	4		Isbn13Normalizer (class)	NormalitzadorIsbn13	occ=1 "Normalizer" English.
IDENTIFIER	src/herramienta/RelationRemapper.java	16		RelationRemapper (class)	RemapejadorRelacions	occ=1 "Remapper" English; the package contains `MapejadorsFiles` (Catalan) — inconsistent.
IDENTIFIER	src/herramienta/JsonImporter.java	31		JsonImporter (class)	ImportadorJson	occ=1 "Json" universal, "Importer" English.
IDENTIFIER	src/herramienta/csv/ShelvesHelper.java	14		ShelvesHelper (class)	UtilitatsPrestatgeries	occ=1 Pure English ("Shelves" + "Helper").
IDENTIFIER	src/herramienta/csv/ShelvesHelper.java	29		cercarOrCreateShelf (method)	cercarOCrearPrestatge	occ=3 (also in GoodreadsCsvStrategy.java:70, LibraryThingCsvStrategy.java:68) Mixed — "cercar" Catalan, "Or" English ("o" Catalan), "Create" English, "Shelf" English ("Prestatge" Catalan).
IDENTIFIER	src/herramienta/csv/ShelvesHelper.java	54		afegirBookToShelf (method)	afegirLlibreAPrestatge	occ=1 Mixed.
IDENTIFIER	src/herramienta/csv/CsvImportStrategy.java	6		CsvImportStrategy (interface)	EstrategiaImportacioCsv	occ=4 (CsvImportStrategy, AbstractMappingCsvStrategy, NativeCsvStrategy, GoodreadsCsvStrategy, LibraryThingCsvStrategy) "Strategy" English, "Import" loanword. Note: "csv" is universal.
IDENTIFIER	src/herramienta/csv/CsvImportStrategy.java	8		getName() (method)	obtenirNom()	occ=4 (in CsvImportStrategy and 3 strategy implementations) Standard Java-getter pattern; flagged because other interfaces in the project use obtenir/posar verb forms.
IDENTIFIER	src/herramienta/csv/AbstractMappingCsvStrategy.java	11		AbstractMappingCsvStrategy (class)	EstrategiaMapatgeCsvAbstracta	occ=1 "Mapping" + "Abstract" English, "Strategy" English.
IDENTIFIER	src/herramienta/csv/NativeCsvStrategy.java	10		NativeCsvStrategy (class)	EstrategiaCsvNativa	occ=1 "Native" English.
IDENTIFIER	src/herramienta/export/JsonExporter.java	8		JsonExporter (class)	ExportadorJson	occ=1 Mixed.
IDENTIFIER	src/herramienta/export/HtmlExporter.java	10		HtmlExporter (class)	ExportadorHtml	occ=1 Mixed.
IDENTIFIER	src/herramienta/export/GoodreadsExportService.java	18		GoodreadsExportService (class)	ServeiExportacioGoodreads	occ=1 "Service" English; "Goodreads" is a 3rd-party name (keep).
IDENTIFIER	src/herramienta/ParseHelpers.java	24		ParseHelpers (class)	UtilitatsAnalisi	occ=1 "Helpers" English plural; the file internally uses `analitzar*` (Catalan) for the actual methods.
IDENTIFIER	src/herramienta/ParseHelpers.java	28,36,44,52,63,74		analitzarLongOrNull / analitzarIntOrNull / analitzarDoubleOrNull / parseLong / parseInt / parseDouble (methods)	keep analitzar* methods; rename parse* to analitzar* (consistency)	occ=6 The "parse*" trio is English while the "analitzar*" trio is Catalan — inconsistent within the same class. Recommend renaming parse* → analitzar*.
IDENTIFIER	src/herramienta/csv/Rfc4180Reader.java	32		hasNext() (interface implementation) (method)	keep	occ=1 Rfc4180Reader implements AutoCloseable; the method name is mandated by the contract — keep. ### Identifiers in src/persistencia/
IDENTIFIER	src/persistencia/ServerConect.java	14		ServerConect (class)	ConnexioServidor / ServerConnexio	occ=11 references (class + 6 references from ControladorPersistencia, SeccioDbConfiguracio, JarLocator) "Server" English + "Conect" Spanish-influenced spelling (correct is "Connect" English, "Connexió" Catalan). The class is a typo/misspelling.
IDENTIFIER	src/persistencia/JarLocator.java	21		JarLocator (class)	LocalitzadorJar / LocalitzadorJars	occ=1 "Locator" English.
IDENTIFIER	src/persistencia/LlibreFieldBindings.java	28		LlibreFieldBindings (class)	EnllacosCampsLlibre	occ=1 "Bindings" English.
IDENTIFIER	src/persistencia/LlibreSearchDao.java	16		LlibreSearchDao (class)	CercadorLlibreDao	occ=1 "Search" English.
IDENTIFIER	src/persistencia/LlibreMapper.java	14		LlibreMapper (class)	MapejadorLlibre	occ=1 "Mapper" English.
IDENTIFIER	src/persistencia/RelationRow.java	8		RelationRow (interface)	RelacioFila / FilaRelacio	occ=1 "Relation" English, "Row" English.
IDENTIFIER	src/persistencia/OverdueLoan.java	-		OverdueLoan (record)	PrestecEndarrerit	occ=1 "Overdue" English; "Loan" English. The Catalan file `domini/facade/DelegatPrestec.java` uses Catalan terms — the record should match.
IDENTIFIER	src/persistencia/ServerConect.java	35		Migracio (record)	keep	occ=1 Pure Catalan. Listed for context.
IDENTIFIER	src/persistencia/LlibreDaoCore.java	110		buildSelect (method)	keep / construirSelect	occ=1 "Build" + "Select" English.
IDENTIFIER	src/persistencia/LlibreMapper.java	32		buildLlibreCore (method)	keep / construirLlibreNucli	occ=1 "Build" English.
IDENTIFIER	src/persistencia/LlibreMapper.java	47		fillLlibreTail (method)	keep / omplirCuaLlibre	occ=1 "Fill" + "Tail" English.
IDENTIFIER	src/persistencia/LlistaDao.java	72		getCount(int llistaId) (method)	obtenirRecompte(int llistaId)	occ=1 "get…" English.
IDENTIFIER	src/persistencia/ServerConect.java	120		getConnection() (method)	obtenirConnexio()	occ=1 ### Identifiers in src/domini/
IDENTIFIER	src/domini/LlibreFilter.java	3		LlibreFilter (class)	FiltreLlibre	occ=1 "Filter" English; the package already contains `FiltreUtils` (Catalan) and `ConstructorFiltreLlibre` (Catalan). Out of step.
IDENTIFIER	src/domini/LlibreFilter.java	39		getFormat() (getter)	obtenirFormat()	occ=4 (also in Llibre.java:240, LlibreFilter.java:39, DelegatEstadistiques.java:36, DetallesLlibrePanelControl.java:146) "get…" English getter; convention in the rest of the codebase is obtenir/posar.
IDENTIFIER	src/domini/Llibre.java	240		getFormat() (getter)	-	See above.
IDENTIFIER	src/domini/Llibre.java	244		isHeavyFieldsLoaded() (getter)	teCampsPesatsCarregats()	occ=1 "is…" English.
IDENTIFIER	src/domini/Llibre.java	248		setHeavyFieldsLoaded(boolean v) (setter)	posarCampsPesatsCarregats(boolean v)	occ=1 "set…" English.
IDENTIFIER	src/domini/Llista.java	19		getColor() (getter)	obtenirColor()	occ=2 (also in LlistaTableModel.java:38) "get…" English getter.
IDENTIFIER	src/domini/Llista.java	32		setColor() (setter)	posarColor()	occ=1 "set…" English setter.
IDENTIFIER	src/domini/Llibre.java	-		class is in Catalan; identifier `Llibre` is the canonical name.	keep	
IDENTIFIER	src/domini/facade/TagDelegate.java	19		TagDelegate (class)	DelegatEtiqueta	occ=1 "Delegate" English. The sibling classes in the package are Catalan (DelegatLlibre, DelegatPrestatgeria, DelegatCopiaSeguretat, DelegatEstadistiques, DelegatPrestec). Only this one uses English "Delegate".
IDENTIFIER	src/domini/facade/StateContext.java	25		StateContext (class)	ContextEstat	occ=1 "State" English, "Context" English. The class is otherwise completely Catalan.
IDENTIFIER	src/domini/ControladorDomini.java	113		getSize() (method)	obtenirMida()	occ=2 (also in DelegatLlibre.java:88) "getSize" English. ### Misc English-style method/field names scattered across src/ These are English-style accessors/utility methods flagged as a single category. They are in the "Java Beans" style (getX/setX/isX), which is technically allowed by the spec but inconsistent with the rest of the project, which uses `obtenir*` / `posar*`.
IDENTIFIER	herramienta/csv/CsvImportStrategy.java	8		String getName();	-	
IDENTIFIER	herramienta/csv/GoodreadsCsvStrategy.java	11		@Override public String getName() { return "Goodreads"; }	-	
IDENTIFIER	herramienta/csv/LibraryThingCsvStrategy.java	11		@Override public String getName() { return "LibraryThing"; }	-	
IDENTIFIER	herramienta/csv/UtilitatsCsv.java	-		Various getters	-	
IDENTIFIER	herramienta/AutoCompletion.java	106		private void setText(String text) { … }	-	
IDENTIFIER	herramienta/ClientOpenLibrary.java	220		private static String fetchWithRetry(String url) (rename to "obtenirAmbReintent")	-	
IDENTIFIER	herramienta/ClientOpenLibrary.java	253		private static String fetchJsonWithRetry(String url) (rename to "obtenirJsonAmbReintent")	-	
IDENTIFIER	herramienta/ServeiCoberta.java	84		public static void fetchAsync(BookWriter cd, String isbn, Runnable onDone) (rename to "obtenirAsync")	-	
IDENTIFIER	herramienta/Configuracio.java	273		public static String getCurrencySymbol()	-	
IDENTIFIER	herramienta/Configuracio.java	274		public static void setCurrencySymbol(String s)	-	
IDENTIFIER	herramienta/Configuracio.java	437		public static String getSortOrder()	-	
IDENTIFIER	herramienta/ConfiguracioDb.java	16		public static void setType(String type)	-	
IDENTIFIER	herramienta/ConfiguracioFinestra.java	19		public static void setBounds(int x, int y, int w, int h)	-	
IDENTIFIER	herramienta/ConfiguracioUi.java	36		public static void setCurrency(String symbol)	-	
IDENTIFIER	presentacio/PanelCalaixFiltre.java	50-92		Combo/Button/Field/Check specs — field names "comboPresets", "btnCarregaPreset", "btnDesaPreset", "btnEsborraPreset", "chckbxLlegit", "chckbxNoLlegit", "textISBN", "textNom", "textAutor", "comboTagFilter", "filterEditorial", "filterSerie", "filterIdioma", "filterFormat", "anyMin", "anyMax", "valoracioMin", "valoracioMax", "preuMin", "preuMax", "bttnFiltrar", "bttnQuitarFiltros", "btnExportDropdown", "btnImportDropdown", "btnFetchCovers", "btnEscanejarISBN", "btnBackupBD", "btnRestaurarBD" — all English-style spec keys.	Catalan-form keys ("comboPresets" → "combo_presets" or Catalan equivalent) — low priority since they are internal field keys, not user-facing.	
IDENTIFIER	presentacio/ControladorAccionsLlibre.java	268-269		onThemeChange / onRefreshData (English callback names)	-	
STRING	src/domini/SqlOp.java	6	en	" * Lambda body that may throw {@link SQLException}. Used by domain-layer try/catch wrappers"	-	Suggested Catalan: "Cos de lambda que pot llançar {@link SQLException}. Usat pels embolcalls try/catch de la capa de domini"
STRING	src/domini/LlibreLlistaContext.java	4-8	en	"Per-book context for a shelf membership: which shelf, which book, and the shelf-local values (rating, read flag) that only make sense in the context of that specific (book, shelf) pair. Use this instead of putting the per-book fields on {@link Llista} directly — the shelf itself has no rating, only the (book, shelf) join does."	-	Suggested Catalan: "Context per llibre d'una pertinença a prestatge: quin prestatge, quin llibre, i els valors locals del prestatge (valoració, marca de llegit) que només tenen sentit en el context d'aquesta parella (llibre, prestatge) concreta. Fes servir això en lloc de posar els camps per-llibre directament a {@link Llista} — el prestatge en si no té valoració, només la unió (llibre, prestatge)."
COMMENT_BLOCK	src/domini/facade/StateContext.java	13-23	en	Class-level Javadoc	-	Suggested Catalan: "Estat compartit en memòria per la façana {@link domini.ControladorDomini}. …" File: 60+ lines of mixed Javadoc in this file; recommended comprehensive rewrite.
STRING	src/domini/facade/StateContext.java	49-56	en	"The monitor used by {@link #withLock} / {@link #withLockReturning}. Package-private (no public accessor): every delegate in {@code domini.facade} goes through {@code withLock(...)} so there is no reason for a caller to acquire the lock directly. The package-private access is preserved for the facade package (e.g. tests that need a raw {@code synchronized (state.lock) { ... }}) but the public surface stays clean."	-	Suggested Catalan: "Accessor de paquet (sense getter públic): cada delegat de {@code domini.facade} passa per {@code withLock(...)}…"
STRING	src/domini/facade/StateContext.java	59-63	en	"Returns the live backing list. Callers MUST hold {@link #lock()} before reading or mutating; iterating without the lock risks {@link java.util.ConcurrentModificationException} and mutating without the lock can desync the list from the id-index maps. Prefer {@link #withLock} / {@link #withLockReturning} for read+copy patterns."	-	Suggested Catalan: "Retorna la llista de suport viva. Els consumidors HAN DE tenir {@link #lock()} agafat abans de llegir o mutar; iterar sense el lock pot provocar {@link java.util.ConcurrentModificationException}…"
STRING	src/domini/facade/StateContext.java	72	en	"Replace all three backing lists atomically (used by restoreFromSQL)."	-	Suggested Catalan: "Substitueix les tres llistes de suport atòmicament (usat per restoreFromSQL)."
STRING	src/domini/facade/StateContext.java	84-89	en	"Clear all three backing lists and the id-index maps (used by clearAll). Mirrors {@link #replaceAll(ArrayList, ArrayList, ArrayList)}'s contract by going through {@link #rebuildIdIndexesLocked()} for consistency…"	-	Suggested Catalan: "Buida les tres llistes de suport i els mapes d'índex per id (usat per clearAll). Reflecteix el contracte de {@link #replaceAll(…)} passant per {@link #rebuildIdIndexesLocked()} per consistència…"
STRING	src/domini/facade/TagDelegate.java	13-17	en	"Tag ({@link Tag}) management and book↔tag relation operations. All methods follow the same contract: mutate the persistence layer first, then update the in-memory tag list + id-index map atomically under the {@link StateContext#lock() state lock}."	-	Suggested Catalan: "Gestió d'Etiquetes ({@link Tag}) i operacions de relació llibre↔etiqueta. Tots els mètodes segueixen el mateix contracte: muta primer la capa de persistència, després actualitza la llista d'etiquetes en memòria + el mapa d'índex per id atòmicament sota el {@link StateContext#lock() lock d'estat}."
STRING	src/domini/facade/DelegatCopiaSeguretat.java	14-16	en	"Backup, restore, and clear-all. Touches all three state lists; the snapshot is taken atomically under the state lock; the file I/O runs outside the lock (the original code did the same — I/O is not held…)."	-	Suggested Catalan: "Còpia de seguretat, restauració i buidat total. Toca les tres llistes d'estat…"
COMMENT_BLOCK	src/domini/facade/DelegatLlibre.java	17-37	en	(class-level Javadoc — extensive English)	-	
STRING	src/domini/facade/DelegatPrestatgeria.java	17-32	en	"Shelf ({@link Llista}) management and book↔shelf relation operations. All single-DB mutations follow an atomic contract: the state lock is held for the entire {@code pre-check → persistence → in-memory mutate} sequence. This closes the race that existed in the pre-facade {@code ControladorDomini}, where two threads renaming the same shelf could both pass the pre-check, race the DB (last-writer-wins), and then both mutate the in-memory map — the second mutation was silently lost or the in-memory state diverged from the DB."	-	Suggested Catalan: "Gestió de prestatgeries ({@link Llista}) i operacions de relació llibre↔prestatge. Totes les mutacions individuals a la BBDD segueixen un contracte atòmic: el lock d'estat es manté durant tota la seqüència {@code pre-comprovació → persistència → mutació en memòria}. Això tanca la cursa que existia a l'antic {@code ControladorDomini}…"
STRING	src/domini/BibliotecaException.java	8	english-catalan mix (the parenthetical is english)	"(NotFound, Duplicate, Validation) permeten als API routers distingir"	"(NoTrobat, Duplicat, Validacio) permeten als API routers distingir…"	
STRING	src/domini/Llibre.java	52-63	en	"Copy the canonical 'updateable' fields (everything except the per-language title, cover blob, autors list, notes, pages, etc.) into {@code target}…"	-	Suggested Catalan: "Copia els camps 'actualitzables' canònics (tot menys el títol per idioma, el blob de portada, la llista d'autors, les notes, les pàgines, etc.) a {@code target}…"
STRING	src/domini/Llibre.java	134	en	"Returns {@code s.trim()} if {@code s} is non-null and not blank, else {@code null}."	"Retorna {@code s.trim()} si {@code s} no és null ni blanc, altrament {@code null}."	
STRING	src/domini/Llibre.java	139	en	"Returns the title for the given lang code ('ca','es','en'), falling back to nom."	"Retorna el títol per al codi d'idioma donat ('ca','es','en'), caient al nom si no hi és."	### Comments in src/domini/facade/ — full coverage needed (English-heavy Javadoc, ~200 lines)
COMMENT_BLOCK	src/domini/facade/DelegatCopiaSeguretat.java	14-16, 96, 132, 136, 145, 154		English Javadoc and exception messages	-	
COMMENT_BLOCK	src/domini/facade/DelegatLlibre.java	17-37, 142, 168, 181, 186, 190, 204, 208		English Javadoc and exception messages	-	
COMMENT_BLOCK	src/domini/facade/DelegatPrestatgeria.java	17-32, 46, 51, 74		English Javadoc and exception messages	-	
COMMENT_BLOCK	src/domini/facade/TagDelegate.java	13-17, 31		English Javadoc + English exception "Tag not found"	-	### Comments in src/persistencia/ (mixed — Javadoc mostly English, inline comments mostly Catalan)
STRING	src/persistencia/ControladorPersistencia.java	108	en	"The DAO already returns the light view (no descripcio/notes); heavy"	"El DAO ja retorna la vista lleugera (sense descripcio/notes); els camps pesats…"	
STRING	src/persistencia/ControladorPersistencia.java	241	en	"For backup/export only — no UI consumer yet."	"Només per a còpia/exportació — sense consumidor d'IU encara."	
STRING	src/persistencia/LlibreFieldBindings.java	10-22	en	"Single source of truth for the 25+ shared fields between {@code Llibre} and the SQL DAOs. …"	-	Multi-line English Javadoc covering the entire class.
STRING	src/persistencia/LlibreFieldBindings.java	24-26	en	"Type coercion mirrors the previous inline behaviour (empty string / null → 0/0.0/false)."	"La coerció de tipus reflecteix el comportament anterior (string buit / null → 0/0.0/false)."	
STRING	src/persistencia/LlibreFieldBindings.java	32	en	"Marker for a SQL NULL of a specific JDBC type."	"Marcador per a un SQL NULL d'un tipus JDBC concret."	
STRING	src/persistencia/LlibreFieldBindings.java	51	en	"This is the entire delta — same order, same value coercion as the previous inline writer."	"Aquest és tot el delta — mateix ordre, mateixa coerció de valors que l'escriptor anterior."	
STRING	src/persistencia/LlibreFieldBindings.java	108	en	"Render the column list as a single comma-separated SQL fragment…"	"Genera la llista de columnes com un únic fragment SQL separat per comes…"	
STRING	src/persistencia/LlibreFieldBindings.java	119	en	"Render the placeholder list for a 27-value INSERT."	"Genera la llista de placeholders per a un INSERT de 27 valors."	
STRING	src/persistencia/LlibreFieldBindings.java	136	en	"Binds a single value to the PreparedStatement, with the type-aware…"	"Vincula un sol valor al PreparedStatement, amb la coerció…"	
STRING	src/persistencia/LlibreMapper.java	9	en	"Static mappers from {@link ResultSet} rows to {@link Llibre} domain objects."	"Mapejadors estàtics de files {@link ResultSet} a objectes de domini {@link Llibre}."	
STRING	src/persistencia/LlibreLecturaDao.java	10	en	"Reads from the {@code lectura} table (one row per reading session)."	"Lectures de la taula {@code lectura} (una fila per sessió de lectura)."	
STRING	src/persistencia/ServerConect.java	113-115	en	"Set once inside {@link #createDatabase(ConnectionConfig)} or {@link #createDatabase()} and never reassigned…"	"Assignada un sol cop dins de {@link #createDatabase(ConnectionConfig)} o {@link #createDatabase()} i mai reassignada…"	
STRING	src/persistencia/ServerConect.java	126-134	en	"Variant that takes the password as a {@code char[]} so the caller can zero it after the call instead of leaving a {@code String} in the heap. <p>The JDBC {@link java.sql.Driver#connect(String, java.util.Properties)} contract requires the password as a {@code String} in the {@code Properties} bag…"	-	Suggested Catalan: see file
STRING	src/persistencia/ServerConect.java	186-191	ca	"dbType desconegut / futur. Llança un missatge precís en lloc de caure a I18n.t('err_db_init_' + dbType)..."	keep	Catalan.
STRING	src/persistencia/ServerConect.java	203-206	en	"Map a known dbType to the matching {@code err_db_init_*} i18n key. Throws for unknown types so the caller never falls through to {@code I18n.t('err_db_init_' + dbType)} (which would return the key itself for unknown types, masking the real error)."	"Mapeja un dbType conegut a la clau i18n {@code err_db_init_*} corresponent. Llança per a tipus desconeguts…"	
STRING	src/persistencia/ServerConect.java	215	en	"Rejects path separators in H2 file profile names (jdbc:h2:dir/profile)."	"Rebutja separadors de ruta en noms de perfil de fitxer H2 (jdbc:h2:dir/perfil)."	
STRING	src/persistencia/ServerConect.java	224-249	en	"Applies pending schema migrations and records each completed version in {@code schema_version}. The transaction strategy depends on the connected engine, detected via {@link java.sql.DatabaseMetaData#getDatabaseProductName()}. <p><b>H2</b> supports transactional DDL…"	-	~25 lines of English Javadoc.
STRING	src/persistencia/ServerConect.java	303-313	en	"Stub kept for clarity — the original 32 → 33 → 34 atomicity plan (see tot.txt F6) was rejected because the H2 32→33→34 sequence runs into 'Column AUTOR not found' when 32 is re-executed after a previous partial commit (a real edge case the existing per-migration transaction model tolerates but the merged runMigrations3234H2 did not). The original three-transaction model is preserved; the tot.txt finding is documented but the fix is not applied here to keep the migration behaviour identical to the long-shipped version."	-	
STRING	src/persistencia/ServerConect.java	335-338	en	"Drops {@code llibre.autor} only when every non-empty legacy author row has a matching {@code llibre_autor} link. Skips (returns false) if unmigrated rows remain."	"Esborra {@code llibre.autor} només quan cada fila d'autor heretada no buida té un enllaç {@code llibre_autor} corresponent. Salta (retorna fals) si queden files sense migrar."	
STRING	src/persistencia/ServerConect.java	94-102	ca	"Migració 32-34: passa els autors de la columna `llibre.autor` (string) a la taula `llibre_autor` (relació N:M). Risc: si 33 falla parcialment en una BBDD gran (OOM, FK violada, etc.) i 34 corre igual, es perd la informació d'autors dels llibres que no s'han migrat..."	keep	
STRING	src/persistencia/ServerConect.java	358-364	ca	"Fa servir la casing de l'identificador que reporta el JDBC (H2 posa en minúscules, MariaDB conserva la mescla)..."	keep	
STRING	src/persistencia/JarLocator.java	42-50	ca	"ServerConect.loadDriverFromLib}) llança un…"	keep	The Javadoc reference to ServerConect.loadDriverFromLib should be updated when the class is renamed.
STRING	src/persistencia/TagDao.java	14-15	ca	"Whitelist de columnes vàlides per a {@link #getDistinctValues(String)}. El nom de la columna s'interpola directament a la SQL (no hi ha…)"	keep	
COMMENT_BLOCK	src/persistencia/Schema.java	-		entirely SQL DDL — no comments flagged.	-	### Comments in src/herramienta/
STRING	src/herramienta/I18n.java	1	ca	"// AUTO-GENERAT per scripts/gen_strings.py — edita strings/strings.csv en lloc d'això"	keep	
STRING	src/herramienta/AutoCompletion.java	18-21	en	"/* This work is hereby released into the Public Domain. To view a copy of the public domain dedication, visit http://creativecommons.org/licenses/publicdomain/ */"	keep (PUBLIC DOMAIN dedication, see LICENSE HEADERS section)	This is a public-domain dedication, not a copyright. Listed separately.
STRING	src/herramienta/AutoCompletion.java	90-91	ca	"// Cerca el primer element del model que comenci pel prefix escrit (insensible a majúscules/minúscules)"	keep	
STRING	src/herramienta/CsvImportStrategy.java	10	en	"Returns true if the row was imported, false if skipped (e.g. duplicate). Throws on parse error."	"Retorna cert si la fila s'ha importat, fals si s'ha omès (p.ex. duplicat). Llança en error d'anàlisi."	
COMMENT_BLOCK	src/herramienta/FormatOptions.java	11, 17		English Javadoc	"Retorna opcions de format amb una primera entrada en blanc…"	
STRING	src/herramienta/FieldAutoComplete.java	17-18	ca	"Límit per defecte de sugerències mostrades al popup. Modificable per {@link #attach(JTextField, java.util.List, int)}."	keep	
COMMENT_BLOCK	src/herramienta/FiltreUtils.java	21-25		Catalan	keep	
STRING	src/herramienta/FiltreUtils.java	42-50	en	"Single source of truth for the in-memory filter predicate. SQL path in {@code LlibreDao.search} mirrors this — when adding a new field to {@link LlibreFilter}, also extend the SQL builder there. <p>{@code tagISBNs} and {@code llistaISBNs} are pre-computed ISBN sets supplied by the caller (they require DB lookups) so this method stays pure and side-effect-free."	"Única font de veritat per al predicat de filtre en memòria. El camí SQL a {@code LlibreDao.search} el reflecteix — quan afegeixis un camp nou a {@link LlibreFilter}, estén també el constructor SQL allà. <p>{@code tagISBNs} i {@code llistaISBNs} són conjunts d'ISBN precalculats subministrats pel consumidor (requereixen cerques a la BBDD) de manera que aquest mètode es manté pur i sense efectes secundaris."	
COMMENT_BLOCK	src/herramienta/GoodreadsCsvStrategy.java	17-24		Catalan	keep	
STRING	src/herramienta/GoodreadsExportService.java	22, 24, 64	ca	"Sobrecàrrega de String conservada per compatibilitat enrere..."	keep	
STRING	src/herramienta/I18n.java	617-622	ca	"Comprova cada posició d'argument sol·licitada, no només {0}, de manera que una clau amb `{1}` però sense `{0}` també s'assenyala (veure el finding LOW de tot.txt). Fa servir l'índex d'argument més alt com a sentinella..."	keep	
STRING	src/herramienta/Isbn13Normalizer.java	20	ca	"// Posem en majúscules qualsevol 'x' abans d'eliminar els no…"	keep	
STRING	src/herramienta/ImportadorLlibres.java	142	catalan with english sqlite error name in quotes	"// 'database is locked' — els consumidors poden reintentar."	keep	The English phrase is the SQLite error code/message, kept for grep-ability.
COMMENT_BLOCK	src/herramienta/LibraryThingCsvStrategy.java	18, 30, 31, 43		Catalan	keep	
COMMENT_BLOCK	src/herramienta/Configuracio.java	313, 323, 344, 505, 508, 520		Catalan	keep	
STRING	src/herramienta/ServeiCopiaSeguretat.java	74	ca	"(anteriorment se l'empassava el catch-all de baix; el …"	keep	
STRING	src/herramienta/UITheme.java	319	en	"Backwards-compat overload — accepts the legacy 'small'/'medium'/'large' key."	"Sobrecàrrega de compatibilitat enrere — accepta la clau heretada 'small'/'medium'/'large'."	
STRING	src/herramienta/UITheme.java	343	ca	"que UIComponents té la implementació real. Els dos camps…"	keep	
STRING	src/herramienta/UtilitatsCsv.java	41, 62, 97	en	"Returns the trimmed value for the given column name, or '' if the column is absent" / "Returns the trimmed value parsed as a double, or 0.0 on null/blank/parse failure." / "Returns true if a book with the given ISBN already exists in the library (skip on import)."	"Retorna el valor retallat per al nom de columna donat, o '' si la columna no hi és" / "Retorna el valor retallat analitzat com a double, o 0.0 si és null/buit/error d'anàlisi." / "Retorna cert si ja existeix un llibre amb l'ISBN donat a la biblioteca (omet en importar)."	
STRING	src/herramienta/ValidadorLlibre.java	99	ca	"(llibres catalogats abans de l'any natural de publicació). El +5…"	keep	
STRING	src/herramienta/ClientOpenLibrary.java	44-49, 146, 149, 185, 190	ca	"test concurrent que posa testBaseUrl…" / "Ordre de caiguda per a imatges de coberta:" / "Les URLs base es poden sobreescriure via testBaseUrl per a mocks de test." / "Analitza la resposta de Google Books de la mateixa manera…" / "tot el prefix com a URL). Reutilitza el mateix recorregut…"	keep	Catalan.
STRING	src/herramienta/Configuracio.java	508	ca	"Javadoc de getDbPassword reconeix el text pla; aquesta és…"	keep	### Comments in src/presentacio/ (LARGEST ENGLISH PRESENCE — ~150+ English Javadoc lines)
STRING	src/presentacio/PanelMarcPrincipal.java	13	en	"Main window content (library table + status bar). Frame chrome lives in {@link MainFrameControl}."	"Contingut de la finestra principal (taula de biblioteca + barra d'estat). La cromia de la finestra és a {@link MainFrameControl}."	The reference to "MainFrameControl" is also stale (that class was renamed to ControladorMarcPrincipal).
STRING	src/presentacio/PanelGaleriaCobertes.java	43	en	"Grid of book covers, the gallery view of the library."	"Graella de portades de llibres, la vista de galeria de la biblioteca."	
STRING	src/presentacio/PanelCalaixFiltre.java	29-38	en	"Sidebar drawer that holds the filter inputs (text fields, checkboxes, combo boxes), the preset bar, the apply/clear buttons, and the export/import/backup buttons. <p>As of R1, the 32 hand-declared component fields are replaced by a single {@link FormFieldRegistry} of declarative specs. The 32 public getters are kept as 1-line shims that delegate to the registry, so the 217 external call sites (most prominently {@link MostrarBibliotecaPanel}) keep compiling without changes."	"Calàix lateral que conté els controls de filtre (camps de text, caselles, quadres combinats), la barra de presets, els botons d'aplicar/buidar, i els botons d'exportació/importació/còpia. <p>A partir de R1, els 32 camps de components declarats a mà es substitueixen per un sol {@link FormFieldRegistry} d'especificacions declaratives…"	
STRING	src/presentacio/PanelCalaixFiltre.java	275	en	"Single source of truth for the 11 button styles shared by…"	"Única font de veritat per als 11 estils de botó compartits per…"	
STRING	src/presentacio/PanelCalaixFiltre.java	400, 408	en	"Declarative triple for a single labelled text-field filter row." / "Declarative triple for a single numeric-range filter row."	"Triple declaratiu per a una sola fila de filtre de camp de text amb etiqueta." / "Triple declaratiu per a una sola fila de filtre de rang numèric."	
STRING	src/presentacio/ControladorTaula.java	75-79	ca	"Actualitza el conjunt d'ISBN prestats en caché al renderer. La… Evita el dispatch per supplier de cel·la que el finding MEDIUM…"	keep	
STRING	src/presentacio/ControladorTaula.java	212	ca	"Columna de detalls oculta — doble clic a la fila o Enter obre…"	keep	
STRING	src/presentacio/ControladorPaginaTaula.java	11-15	en	"Manages pagination state for the book table. …"	"Gestiona l'estat de paginació de la taula de llibres. …"	Also: "PAGE_SIZE is unrelated to ControladorDomini.SQL_FILTER_THRESHOLD (2000); pagination kicks in at 100 rows, SQL-filter at 2000 books." (ControladorPaginaTaula.java:27)
STRING	src/presentacio/ControladorMostrarBiblioteca.java	15	en	"Slim coordinator for the main library screen; delegates to sub-controllers."	"Coordinador prim de la pantalla principal de la biblioteca; delega als subcontroladors."	
STRING	src/presentacio/ControladorModeVista.java	16	en	"Gallery vs table view, series grouping, and title bar."	"Vista galeria vs taula, agrupació per sèries i barra de títol."	
STRING	src/presentacio/ControladorMenuContextual.java	24	en	"Table and gallery context menus, batch edit, duplicate, loan."	"Menús contextuals de taula i galeria, edició per lots, duplicar, prestar."	
STRING	src/presentacio/ControladorMarcPrincipal.java	26-44	en	"Facade over the 6 sub-controllers (bookActions, bookIO, contextMenu, filter, …"	"Façana sobre els 6 subcontroladors (bookActions, bookIO, contextMenu, filter, …"	Also: "If the user is currently viewing a specific shelf, add a newly created" (English, line 303)
STRING	src/presentacio/ControladorGaleria.java	8	en	"Thin coordinator for {@link GaleriaCobertesPanel} book list updates."	"Coordinador prim per a les actualitzacions de la llista de llibres de {@link GaleriaCobertesPanel}."	
STRING	src/presentacio/ControladorAccionsLlibre.java	120	ca	"// Limita la cua d'undoing a UNDO_MAX (20). Quan…"	keep	
STRING	src/presentacio/AjudaEstadistiques.java	222-224	en	"Builds the 'General' stats tab: goal progress + text summary + per-shelf table. All view construction lives here so the controller stays slim."	"Construeix la pestanya d'estadístiques 'General': progrés de l'objectiu + resum de text + taula per prestatge. Tota la construcció de la vista viu aquí perquè el controlador es mantingui prim."	
STRING	src/presentacio/PaginationView.java	5	en	"Updates pagination widgets on {@link MostrarBibliotecaPanel}."	"Actualitza els ginys de paginació a {@link MostrarBibliotecaPanel}."	
STRING	src/presentacio/PaginationModel.java	3	en	"Pagination state extracted from {@link TablePageController}."	"Estat de paginació extret de {@link TablePageController}."	
STRING	src/presentacio/LlistaTableModel.java	10-14	en	"Table model for shelf data (id, name, count, color). Extracted from GestioLlistesDialog so it can be reused by the stats dialog or any future shelf list view."	"Model de taula per a dades de prestatgeries (id, nom, recompte, color). Extret de GestioLlistesDialog per poder-se reutilitzar pel diàleg d'estadístiques o qualsevol vista futura de llistes de prestatgeries."	
STRING	src/presentacio/LlibreFormBinder.java	24	en	"Populate the four core text fields from a Llibre. Reduces copy-paste in dialogs."	"Omple els quatre camps de text principals des d'un Llibre. Redueix el copy-paste als diàlegs."	
STRING	src/presentacio/LibraryViewState.java	13	en	"Shared mutable state for the main library screen sub-controllers."	"Estat mutable compartit pels subcontroladors de la pantalla principal de la biblioteca."	
STRING	src/presentacio/LibraryScreenHost.java	8-18	en	"Callbacks from sub-controllers back to {@link MostrarBibliotecaControl}. Slim interface (4 methods) so the host can stay framework-free. (Was a 7-method interface before the tot.txt MEDIUM finding.)"	"Callbacks dels subcontroladors cap a {@link MostrarBibliotecaControl}. Interfície prima (4 mètodes) perquè l'amfitrió es mantingui lliure de framework. (Era una interfície de 7 mètodes abans del finding MEDIUM de tot.txt.)"	
STRING	src/presentacio/FormValidator.java	14	en	"Visual validation feedback for form fields."	"Retroalimentació visual de validació per als camps de formulari."	
STRING	src/presentacio/FormFieldRegistry.java	19-26	en	"Declarative registry for the filter-drawer form. Builds Swing components on demand for each spec and stores it in a name -> component map. Public access is by component kind, not by raw map lookup, so future rendering changes (e.g. scaling, custom focus traversal) are one-line changes inside the registry."	"Registre declaratiu per al formulari del calàix de filtres. Construeix components Swing sota demanda per a cada especificació i els desa en un mapa nom → component. L'accés públic és per tipus de component, no per cerca directa al mapa, de manera que els canvis futurs de renderització (p.ex. escalat, recorregut de focus personalitzat) siguin canvis d'una sola línia dins del registre."	
STRING	src/presentacio/FormFieldRegistry.java	34	en	"Declarative spec for a single field."	"Especificació declarativa per a un sol camp."	
STRING	src/presentacio/FormFieldRegistry.java	72	en	"Append a spec to the registry. Order is preserved."	"Afegeix una especificació al registre. L'ordre es conserva."	
STRING	src/presentacio/ConfiguracioDialog.java	95	en	"Refresh form fields from {@link Config} (e.g. after external/API changes)."	"Refresca els camps del formulari des de {@link Config} (p.ex. després de canvis externs/API)."	
STRING	src/presentacio/BibliotecaTableModel.java	12	en	"Table model for the main library book table."	"Model de taula per a la taula principal de llibres de la biblioteca."	
STRING	src/presentacio/BibliotecaTableModel.java	87	en	"Mutable backing list (package-private use by {@link TableController} only)."	"Llista de suport mutable (ús package-private només per {@link TableController})."	"TableController" doesn't exist (it was renamed to ControladorTaula). Stale.
STRING	src/presentacio/config/SeccionsConfiguracio.java	7-8	en	"Shared utilities for {@code presentacio.config.*} sections. Each section panel tags its input children with a {@code ClientProperty key, which SeccionsConfiguracio.recogerSeccions(...) harvests to build a single flat field map. <p>The naming convention is 'lblFoo' for the label of a field whose key is 'foo', and the optional 'lblFooCol' hint for inline help text. The collect loop also tolerates containers with a 'seccio.skip' flag set (e.g. a sub-section not yet ready for the dialog)."	-	
STRING	src/presentacio/config/SeccionsConfiguracio.java	15	en	"Locate a tagged child by id. Recurses into nested containers."	"Localitza un fill etiquetat per id. Recorre recursivament els contenidors niats."	
STRING	src/presentacio/config/BarraBotonsConfiguracio.java	23	en	"Save / cancel button bar. Pulls field values from the tagged input children registered by each section panel."	"Barra de botons desar/cancel·lar. Recull els valors dels camps dels fills d'entrada etiquetats registrats per cada secció."	
STRING	src/presentacio/config/ConfiguracioDialogListener.java	3	en	"Callbacks fired by the settings dialog. Replaces raw Runnable parameters."	"Callbacks disparats pel diàleg de configuració. Substitueix els paràmetres Runnable crus."	
STRING	src/presentacio/listener/EnActualizarBBDD.java	6-12	en	"Composite listener for book insert/update/delete events. <p>Merges the two parent contracts ({@link OnLlibreUpdate}, {@link OnLlibreDelete}) so a presenter can subscribe to both event families through a single field. <p>Subclasses can override either {@link OnLlibreUpdate#onBookUpdated} and {@link OnLlibreDelete#onBookDeleted} (or use a default …) implementation."	"Escoltador compost per a esdeveniments d'inserció/actualització/eliminació de llibres. <p>Fusiona els dos contractes pare ({@link OnLlibreUpdate}, {@link OnLlibreDelete})…"	
STRING	src/presentacio/GestioLlistesListener.java	3	en	"Explicit callback interface for shelf-management actions (replaces raw Runnable)."	"Interfície de callbacks explícita per a accions de gestió de prestatgeries (substitueix el Runnable cru)."	
STRING	src/presentacio/KeyboardShortcuts.java	11	en	"Keyboard shortcut wiring extracted from {@link MainFrameControl}."	"Cablejat de dreceres de teclat extret de {@link MainFrameControl}."	Stale reference (MainFrameControl → ControladorMarcPrincipal).
STRING	src/presentacio/layouts/WrapLayout.java	13-16	ca	"Originalment extreta de la classe interna privada de… Adaptat de l'exemple públic 'WrapLayout' del tutorial de Java de Sun: …"	keep	Catalan. The English "WrapLayout" is the original Java Tutorial class name.
STRING	src/presentacio/renderers/SearchHighlightRenderer.java	15	en	"Cached set reference — refreshed via {@link #setLoanedISBNs(Set)} from…"	"Referència a set en caché — refrescat via {@link #setLoanedISBNs(Set)} des de…"	
STRING	src/presentacio/renderers/LlegitCheckBoxEditor.java	17, 20	en	"Cell editor for the 'llegit' column. Only persists the {@code llegit} flag (in-place toggle) — full book edits flow through the details dialog to keep audit + validation centralised. The user is expected to open the details dialog (which calls …) when they want to change anything else."	"Editor de cel·la per a la columna 'llegit'. Només persisteix la marca {@code llegit}…"	
STRING	src/presentacio/detalles/vista/UtilitatsComboEntitat.java	9	en	"Preserves the previously selected item by id after reloading the model."	"Conserva l'ítem seleccionat anteriorment per id després de recarregar el model."	
STRING	src/presentacio/detalles/vista/DialegEtiquetesLlibre.java	32, 37	en	"Dialog for managing the tags assigned to a single book. The immediate-persist model is chosen because tag operations are lightweight single-row mutations, and the user can dismiss the dialog with Esc/Cancel without losing work — operations are committed as they happen. By contrast, the sibling LlistesDelLlibreDialog ajorna (compare with the …)."	"Diàleg per gestionar les etiquetes assignades a un sol llibre…"	
STRING	src/presentacio/detalles/control/ControladorEtiquetesLlibre.java	38	ca	"Mapa en caché d'etiqueta → recompte de llibres. S'omple un sol cop…"	keep	
STRING	src/presentacio/detalles/control/LlistesDelLlibreControl.java	29, 32, 145	ca	"en temps de construcció — però memberIds és un camp Set mutable…" / "el Set en si, a cada recàrrega per mantenir vàlida la referència…" / "Reutilitza el mateix HashSet entre recàrregues (clear+addAll)…"	keep	
STRING	src/presentacio/detalles/control/GuardarLlibresDialogoControl.java	72-74	ca	"Carrega les llistes d'autocompletat diferenciades fora de l'EDT — són consultes a la BBDD que poden bloquejar la UI en biblioteques grans."	keep	
STRING	src/presentacio/detalles/control/DetallesLlibrePanelControl.java	71-72	ca	"Ha fallat la càrrega de camps pesats per a l'ISBN…" / "la descripció i les notes es renderitzaran buides i qualsevol desat sobreescriurà els originals"	keep	
STRING	src/presentacio/detalles/control/DetallesLlibrePanelControl.java	125	ca	"l'autocompletat és best-effort; el diàleg continua funcionant sense"	keep	
COMMENT_BLOCK	src/presentacio/detalles/control/DetallesLlibrePanelControl.java	351-355		Catalan	keep	
COMMENT_BLOCK	src/presentacio/detalles/control/DetallesLlibrePanelControl.java	404-410		Catalan	keep	
STRING	src/presentacio/galeria/ServeiImatgesCoberta.java	21-38	en	"Async loader + LRU cache of pre-scaled cover images. <p>Keyed by {@link Llibre#getISBN()}. The cache value is the image already scaled to the requested card dimensions, so the paint path can blit 1:1 without per-frame scaling work. <p>Owns the single-threaded loader pool, the in-flight set, and the crop-scale math. All access from the EDT is via {@link #submit(Llibre, int, int, Runnable)}; background loading and cache mutation run on the executor, with the repaint callback marshalled back to the EDT. <p>The LRU is protected by a {@link ReentrantReadWriteLock} so EDT reads (the paint path, dominant) run concurrently while writes (cache miss → load) take an exclusive lock. The previous {@code Collections.synchronizedMap} wrapper serialised every {@code get()} against every other {@code get()}, which stalled the EDT on a 200-card repaint (per the tot.txt LOW finding)."	-	~18 lines of English Javadoc.
STRING	src/presentacio/galeria/ServeiImatgesCoberta.java	74-78	en	"Return the cached image for {@code isbn} at the current zoom, or {@code null} if not yet loaded. The caller (card paint) must tolerate a {@code null} return — the loader's onLoaded callback will repaint. Read-locked so concurrent EDT repaints on a 200-card gallery do not serialise."	"Retorna la imatge en caché per a {@code isbn} al zoom actual, o {@code null} si encara no s'ha carregat…"	
STRING	src/presentacio/galeria/ServeiImatgesCoberta.java	89-92	en	"Invalidate all cached images. Called when the zoom level changes…"	"Invalida totes les imatges en caché. Es crida quan canvia el nivell de zoom…"	
STRING	src/presentacio/galeria/ServeiImatgesCoberta.java	104-106	en	"Schedule background load + crop-scale for {@code l}. The {@code onLoaded}…"	"Planifica la càrrega en segon pla + retall-escalat per a {@code l}. El callback {@code onLoaded}…"	
STRING	src/presentacio/galeria/ServeiImatgesCoberta.java	148	en	"Visible for tests / future tooling: drain the executor on shutdown."	"Visible per a tests / eines futures: buida l'executor en tancar."	
STRING	src/presentacio/galeria/ServeiImatgesCoberta.java	155	ca	"Abans els reexportava {@code TableCellComponents}. S'han mogut aquí…"	keep	
COMMENT_BLOCK	src/presentacio/galeria/ServeiImatgesCoberta.java	161, 176, 222		Catalan	keep	
STRING	src/presentacio/galeria/PopupZoomCoberta.java	24-30	en	"Per-card hover-popup that shows a high-resolution preview of the cover. It is owned by the gallery: only one popup can be visible at a time, and it auto-hides on card repaint to keep the overlap predictable. <p>Triggered by hovering a card for {@link #HOVER_DELAY_MS}; the popup is positioned next to the card and removed on the next zoom change or card repaint."	"Popup flotant per targeta que mostra una previsualització d'alta resolució de la portada…"	
STRING	src/presentacio/galeria/PopupZoomCoberta.java	41-43	en	"Schedule a zoom popup for {@code card}. The popup is shown…"	"Planifica un popup de zoom per a {@code card}. El popup es mostra…"	
STRING	src/presentacio/galeria/PopupZoomCoberta.java	53	en	"Cancel any pending show; remove the overlay if visible."	"Cancel·la qualsevol mostra pendent; treu la superposició si és visible."	
STRING	src/presentacio/galeria/FabricantTargetesCoberta.java	36-39	en	"Builds the visual card panels that fill the gallery. Extracted from a single huge PanelGaleriaCobertes method (per the tot.txt MEDIUM finding) so the card paint + hit-test logic is testable in isolation. The card exposes a small host interface (AmfitrioTargeta, 7 callbacks) so the host panel can stay presentation-only and not depend on the card's internal Llibre/ISBN fields."	"Construeix els panells visuals de targetes que omplen la galeria…"	
STRING	src/presentacio/galeria/FabricantTargetesCoberta.java	112-118	ca	"Estat de la targeta — capturat en temps de construcció, mutat pels…" / "Pintat a paintCard()." / "Captura els camps de Llibre que paintCard necessita en un record…"	keep	
STRING	src/presentacio/galeria/FabricantTargetesCoberta.java	173-177	en	"Static card paint — extracted from the anonymous JPanel's paintComponent (per the tot.txt MEDIUM finding). All drawing goes through paintCard() so the visual layer stays independent of the host panel and the mouse handler. The paint path never calls back into the host except through the small AmfitrioTargeta interface."	"Pintat estàtic de la targeta — extret del paintComponent del JPanel anònim…"	
STRING	src/presentacio/galeria/FabricantTargetesCoberta.java	231-233	en	"Static mouse adapter — extracted from the inline MouseAdapter…"	"Adaptador de ratolí estàtic — extret del MouseAdapter inline…"	
STRING	src/presentacio/ControladorAccionsLlibre.java	268-269		"onThemeChange / onRefreshData" (English)	enCanviarTema / enRefrescarDades	### Comments in src/main/ (mostly Catalan, with a few English-style exception messages)
STRING	src/main/Ejecutable.java	30	ca	"Excepció no capturada al fil…"	keep	
STRING	src/main/Ejecutable.java	33	ca	"Error fatal: " + e.getClass().getSimpleName() + ": " + e.getMessage()	keep	
STRING	src/main/Ejecutable.java	36-39	ca	"La signatura del handler d'excepcions no captades rep Throwable, no Exception; alguns consumidors llancen Error (StackOverflowError, OutOfMemoryError) que DialogoError accepta via la sobrecàrrega (Throwable)."	keep	The comment uses "DialogoError" but the actual class is `DialegError` (Catalan). Stale reference.
STRING	src/main/Ejecutable.java	42	ca	"Error fatal: " + e.getClass().getSimpleName()	keep	
COMMENT_BLOCK	src/main/Ejecutable.java	77, 91, 98		Catalan log messages	keep	
COMMENT_BLOCK	src/main/ShutdownHooks.java	-		Class Javadoc and method comments — class name "ShutdownHooks" is English (flagged in IDENTIFIERS). The body uses Catalan (e.g. "Tanca ordenadament…", etc.).	-	### Comments in scripts/ (Python scripts — only # comments flagged)
COMMENT_BLOCK	scripts/loop-review.sh	-	en	#!/bin/bash — script header	keep	Shell script boilerplate, universal.
COMMENT_BLOCK	scripts/loop-review-prompt.txt	-	en	Prompt content	keep	Prompt for an LLM agent; not user-facing.
COMMENT_BLOCK	scripts/loop-prompt-tune-prompt.txt	-	en	Prompt content	keep	
COMMENT_BLOCK	scripts/loop-prompt-tune.sh	-	en	Shell	keep	
COMMENT_BLOCK	scripts/_build_test_classes.py	-	en	Python comments	keep (developer-facing tool)	
COMMENT_BLOCK	scripts/fuzz-nightly.sh	-	en	Shell	keep	
COMMENT_BLOCK	scripts/gen_strings.py	-	en	Python	keep (developer tool)	
COMMENT_BLOCK	scripts/patch_tests_after_web_removal.py	-	en	Python	keep	
COMMENT_BLOCK	scripts/split_agent_briefs.py	-	en	Python	keep	### Comments in pom.xml
COMMENT_BLOCK	pom.xml	22	en	<!-- ── Source layout ──────────────────────────────── -->	Catalan ("Disposició del codi font")	Section banner.
COMMENT_BLOCK	pom.xml	29	en	<!-- Compiler -->	keep (universal term)	Standard Maven plugin name.
COMMENT_BLOCK	pom.xml	39	en	<!-- Surefire: run JUnit 5 tests -->	keep (Surefire is the plugin name)	
COMMENT_BLOCK	pom.xml	45	en	<!-- Only JUnit 5 tests; plain-Java BibliotecaTest runs via make test -->	keep (developer note)	
COMMENT_BLOCK	pom.xml	56-57	en	<!-- JaCoCo coverage — disabled by default (ASM in 0.8.12 doesn't support Java 25 class files). Enable with: mvn verify -Pcoverage -->	keep (developer note)	
COMMENT_BLOCK	pom.xml	67	en	<!-- Executable JAR -->	keep (technical)	
COMMENT_BLOCK	pom.xml	88	en	<!-- ── Dependencies ──────────────────────────────── -->	Catalan	Section banner.
COMMENT_BLOCK	pom.xml	91	en	<!-- Runtime / compile -->	keep (technical scope name)	
COMMENT_BLOCK	pom.xml	107	en	<!-- Test frameworks -->	keep	### Comments in Makefile
COMMENT_BLOCK	Makefile	1-3	en	# ---------- RUN & COMPILE ---------- # / # Multiple targets share the same `bin/` output (compile, fuzz-test-compile, / # populate, test). They must run serially to avoid javac-on-bin races and / # the resulting "rm: cannot remove classes.txt" / corrupted class files.	keep (developer build notes)	
COMMENT_BLOCK	Makefile	48	en	# ---------- FUZZ DEPS ----------	keep	
COMMENT_BLOCK	Makefile	50-58	en	# jqwik 1.9.0 (property-based, JUnit 5) / # Jazzer 0.24.0 (coverage-guided libFuzzer for the JVM) / # jazzer-junit-launcher was renamed to jazzer in 0.x — Maven Central / # only ships jazzer, jazzer-api, and jazzer-junit. The agent attaches / # dynamically via ByteBuddyAgent when JAZZER_FUZZ=1 is set.	keep (developer tool comments)	
COMMENT_BLOCK	Makefile	61-80	en	# Fuzz jars are added to the test classpath so `make test` compiles the / # jqwik fuzz tests. The Jazzer @FuzzTest tests are excluded from ... / # Jazzer 0.24.0 has a packaging inconsistency: the native libs in ...	keep (extensive technical notes — ~20 lines)	
COMMENT_BLOCK	Makefile	113	en	# Optional UI audit (requires display; skipped in headless CI unless DISPLAY set)	keep	
COMMENT_BLOCK	Makefile	118-121	en	# ---------- POPULATE ------- # / # Fetches books from OpenLibrary and inserts them. / # Usage: make populate (default 2000 books) / # make populate MAX=500 (custom limit)	keep	
COMMENT_BLOCK	Makefile	131-145	en	# ---------- INSTALLERS ----- # / # make jar → biblioteca.jar ... / # Launch4j (cross-compile JAR → Windows EXE): ...	keep (build notes)	
COMMENT_BLOCK	Makefile	152	en	# Pick a JDK whose bin/java actually exists (some distros leave stub dirs).	keep	
COMMENT_BLOCK	Makefile	164, 169, 186, 203, 228, 251, 303, 335, 340, 435	en	# ── slim JAR ... / # ── fat JAR ... / # ── icon: ... / # ── Windows installer: ... / # ── Standalone Windows .exe ... / # ── Windows installer (.exe) ... / # ── Linux installer: ... / # ---------- STRINGS ---------- # / # ---------- FUZZ ------------ # / # ---------- CLEAN ---------- #	keep (section headers, English in Makefiles is conventional)	
COMMENT_BLOCK	Makefile	384-388, 397-404, 426-431	en	# Run the jqwik property tests for the domini layer. ... / # Run the Jazzer harnesses. JAZZER_FUZZ=1 switches Jazzer from regression ... / # Compile test sources (including fuzz tests) into bin/. ...	keep (developer notes)	### Comments in AGENTS.md / CLAUDE.md / README.md / Biblioteca.html (top-level docs)
STRING	AGENTS.md	5-12	en	"Do not write or edit production code until you have a short plan the user can follow. This is the highest-priority rule for every task, including small fixes. Before the first line of code: 1. Understand — Read relevant files…"	Catalan version of the rules	AGENTS.md is for AI agents; English is conventional. Listed here so the user can decide whether to localise.
COMMENT_BLOCK	AGENTS.md	23-37		Same — English throughout. Loop delegation section.	-	
COMMENT_BLOCK	AGENTS.md	59-95		Build & Test Commands section — English.	-	
STRING	AGENTS.md	128	en	"Language: Catalan/Spanish mixed"	keep	Meta-note that explicitly acknowledges the mixed convention. (Self-referential.)
COMMENT_BLOCK	CLAUDE.md	1-63		Entire file is in English	Catalan version	Like AGENTS.md, CLAUDE.md is for AI assistants and is conventionally in English. Listed for transparency.
COMMENT_BLOCK	README.md	5-7, 21-49, 53-79, 81-141, 142-168, 170-208, 210-230, 232-260	mixed	The README is mostly in Catalan — only the technical commands, links, and table headers are in English (e.g. "Objectiu | Comanda | Descripció", "Llicència", "Requisits", "Windows", "Linux", "Característiques", "Productivitat", "Tests", "Poblar amb dades d'exemple"). All visible Catalan prose is correct.	keep	The README is the user-facing document and the Catalan content is well-done. Only flag the language note in AGENTS.md.
COMMENT_BLOCK	Biblioteca.html	-		Generated HTML output — skip per scope rules.	-	
STRING	src/herramienta/I18n.java	263	spanish (wrong! in the ca column)	"Cuidado que hay diferentes columnas en la tabla de Libros"	-	Suggested Catalan: "Les columnes del CSV no coincideixen amb les de la taula de llibres" or similar This is the `dlg_import_warn` key — the ca column literally contains Spanish. The es and en columns are also present. The ca string was never translated.
STRING	src/herramienta/I18n.java	578	spanish (wrong! in the es column — but the ca column is also inconsistent)	"El nombre de la estante no puede estar vacío"	Re-check the es entry: the es column has "estante" (Spanish for shelf) but the en column says "Shelf name" and the ca column says "El nom de la llista no pot estar buit". The es entry appears to be a copy-paste error. Catalan is correct.	Key `val_llista_blank`. ### User-facing strings in src/herramienta/I18n.java (full file) The I18n.java file is generated by `scripts/gen_strings.py` from `strings/strings.csv`. The file is largely Catalan. The exceptions are: - Line 263 (above) — Spanish string in the ca column. - All entries from line 1 to 653 are otherwise in Catalan (ca), Spanish (es), and English (en) — as designed.
STRING	src/herramienta/I18n.java	592	en	"CSV files" (file picker filter)	"Fitxers CSV"	The ca column of `lbl_csv_files` is "CSV files" (English) — should be Catalan. ### User-facing strings in src/*.java (hard-coded, NOT going through I18n)
STRING	src/persistencia/ServerConect.java	191-192	en	"Unknown dbType: " + cfg.dbType() + " (expected 'h2' or 'mariadb')"	"Tipus de BBDD desconegut: " + cfg.dbType() + " (s'esperava 'h2' o 'mariadb')"	Exception message bubbled up to UI.
STRING	src/persistencia/ServerConect.java	401	en	"lib/ directory not found. Search log:\n"	"No s'ha trobat el directori lib/. Registre de cerca:\n"	
STRING	src/persistencia/ServerConect.java	407	en	"Driver JAR '" + jarNameHint + "' not found. Search log:\n"	"No s'ha trobat el JAR del driver '" + jarNameHint + "'. Registre de cerca:\n"	
STRING	src/persistencia/ServerConect.java	393	en	"Driver " + driverClass + " did not accept URL: " + url	"El driver " + driverClass + " no ha acceptat l'URL: " + url	
STRING	src/domini/facade/TagDelegate.java	31	en	"Tag not found: " + id	"Etiqueta no trobada: " + id	
STRING	src/domini/facade/DelegatPrestatgeria.java	46	en	"Shelf not found: " + id	"Prestatge no trobat: " + id	
STRING	src/domini/facade/DelegatCopiaSeguretat.java	132	en	"Restore failed: " + e.getMessage()	"Ha fallat la restauració: " + e.getMessage()	
STRING	src/domini/facade/DelegatCopiaSeguretat.java	130-131	en	"\n\nOriginal error: " + e.getMessage() + "\nRollback error: " + ex.getMessage()	"\n\nError original: " + e.getMessage() + "\nError de recuperació: " + ex.getMessage()	
STRING	src/herramienta/InMemoryBibliotecaReader.java	34	en	"Not found: " + isbn	"No trobat: " + isbn	
STRING	src/herramienta/InMemoryBibliotecaReader.java	76	en	"Shelf not found: " + id	"Prestatge no trobat: " + id	
STRING	src/herramienta/InMemoryBibliotecaReader.java	87	en	"Tag not found: " + id	"Etiqueta no trobada: " + id	
STRING	src/herramienta/csv/GoodreadsCsvStrategy.java	35	ca	"ISBN buit"	keep	Catalan.
STRING	src/herramienta/csv/GoodreadsCsvStrategy.java	45, 49	en	"Number of Pages" / "Original Publication Year" (literal CSV column names)	keep	Goodreads is a 3rd-party service — column names are dictated by the import format.
STRING	src/herramienta/csv/LibraryThingCsvStrategy.java	54	en	"Publication Year" (literal CSV column name)	keep	Same.
STRING	src/herramienta/ClientOpenLibrary.java	221	en	"Interrupted while rate-limiting"	"Interromput durant la limitació de freqüència"	
STRING	src/herramienta/ClientOpenLibrary.java	235	en	"Interrupted during retry backoff"	"Interromput durant el retrocés entre reintents"	
STRING	src/herramienta/ClientOpenLibrary.java	259	en	"Malformed JSON from " + url + ": " + analitzarEx.getMessage()	"JSON mal format des de " + url + ": " + analitzarEx.getMessage()	
STRING	src/herramienta/ServeiCopiaSeguretat.java	107	en	"Auto-backup SQL failure"	"Ha fallat la còpia SQL automàtica"	
STRING	src/main/Ejecutable.java	42	ca	"Error fatal: " + e.getClass().getSimpleName()	keep	Catalan.
STRING	src/presentacio/ControladorAccionsLlibre.java	268-269	en	"onThemeChange" / "onRefreshData" (string keys in callback dispatch)	"enCanviarTema" / "enRefrescarDades"	Method-name references in inner class declarations; if methods are renamed, the strings must be too.
STRING	src/interficie/BookReader.java	15	en	"Unexpected checked exception in findLlibre(" + isbn + ")"	"Excepció comprovada inesperada a findLlibre(" + isbn + ")"	
STRING	src/presentacio/LlistaTableModel.java	16	mixed	private static final String[] COLS = { "ID", "Nom", "Count", "Color" };	{ "ID", "Nom", "Recompte", "Color" }	These are user-visible table column headers. "Count" should be "Recompte".
STRING	src/persistencia/ConnectionConfig.java	9-11	english (the message-after-npe)	requireNonNull(host, "host") / requireNonNull(user, "user") / requireNonNull(password, "password")	keep	Property names used in JDBC URLs — must stay "host", "user", "password".
STRING	src/herramienta/AutoCompletion.java	18-21	en	"/* This work is hereby released into the Public Domain. To view a copy of the public domain dedication, visit http://creativecommons.org/licenses/publicdomain/ */"	-	PUBLIC DOMAIN dedication, not a copyright. Keep verbatim.
LICENSE_HEADER	src/domini/BibliotecaException.java	1-9		Standard file header (Javadoc describing the class).	-	No copyright header in this file.
STRING	src/presentacio/layouts/WrapLayout.java	16-17		"Adaptat de l'exemple públic 'WrapLayout' del tutorial de Java de Sun: …"	-	This is attribution to a third-party source. The class itself is "Adaptat" (adapted) — the Java Tutorial attribution. Keep the "Sun" reference for provenance.
STRING	src/persistencia/JarLocator.java	42-50		"ServerConect.loadDriverFromLib}) llança un…"	-	Reference to a method — should be updated when ServerConect is renamed.
LICENSE_HEADER	src/domini/facade/StateContext.java	1-24		No license header; only Javadoc.	-	Most Java files in `src/` do not have license headers. The LICENSE in the project root (GPL v3) covers the whole codebase.

# === META (verbatim from translate1.txt) ===

## === SUMMARY ===
- Total non-Catalan identifiers flagged: ~85 distinct identifier names
   (28 classes/interfaces, 12 listener/callback methods, 17 method/getter names, 28 field/local variable names)
- Total non-Catalan comment lines flagged: ~700 lines (rough estimate — Javadoc body text)
   * src/presentacio/: ~150 lines of English Javadoc across 22 files
   * src/domini/facade/: ~200 lines of English Javadoc across 7 files
   * src/persistencia/: ~80 lines of English Javadoc across 5 files
   * src/herramienta/: ~50 lines of English Javadoc across 8 files
   * src/domini/: ~30 lines of English Javadoc across 4 files
   * src/main/: ~5 lines (mostly Catalan; one stale class-name reference)
   * scripts/, Makefile, pom.xml: ~80 lines of English comments (developer notes — all flagged for review, but most are technical/universal)
- Total non-Catalan user-facing string literals flagged: 17 (15 in Java + 2 in I18n.java ca column)
- Total files with at least one non-Catalan item: ~140 of 186 (.java) — i.e. ~75%
- Files clean of non-Catalan content: ~46 .java files (mostly small controllers/listeners/records with only identifiers)

## === RECOMMENDED DIRECTORY RENAMES (informational, do NOT rename) ===
- `src/herramienta/` → `src/eines/` (herramienta is Spanish; eina is Catalan)
- `src/fichers/` → `src/fitxers/` (fichers is Spanish; fitxers is Catalan; the directory contains only
   `234.csv`, `credit.html`, `license.html` — non-Java content, in scope for content check but the folder
   rename would also need to update import paths in tooling, even though no Java code imports from it)
- `src/interficie/` → `src/interficies/` or `src/interfície/` (the standard Catalan spelling uses the
   acute accent; the folder name "interficie" is missing the accent; this is a low-priority visual fix)

## === RECOMMENDED FILE RENAMES (informational, do NOT rename) ===
Top 10 highest-impact Spanish/English class-name renames (in rough priority order):

1. `src/main/Ejecutable.java` → `Executable.java`  (Spanish → loanword)
2. `src/persistencia/ServerConect.java` → `ConnexioServidor.java`  (typo + Spanish)
3. `src/presentacio/detalles/vista/DetallesLlibrePanel.java` → `PanellDetallsLlibre.java`  (Spanish)
4. `src/presentacio/detalles/control/DetallesLlibrePanelControl.java` → `ControladorPanellDetallsLlibre.java`
5. `src/presentacio/detalles/vista/GuardarLlibresDialogo.java` → `DialegDesarLlibres.java`
6. `src/presentacio/detalles/control/GuardarLlibresDialogoControl.java` → `ControladorDialegDesarLlibres.java`
7. `src/presentacio/listener/EnActualizarBBDD.java` → `EnActualitzarBBDD.java`  (Spanish verb)
8. `src/interficie/BookReader.java` → `LectorLlibre.java` + 5 sibling renames in `interficie/`
9. `src/presentacio/detalles/vista/LlistesDelLlibreDialog.java` → `DialegLlistesLlibre.java`  (Spanish "Del")
10. `src/presentacio/detalles/control/LlistesDelLlibreControl.java` → `ControladorLlistesLlibre.java`
   + `LlistesDelLlibreTableModel.java` → `ModelTaulaLlistesLlibre.java`

## === JUDGMENT CALLS / EDGE CASES (documented for the user) ===
- **Skipped (universal / loanword)**: i18n, l10n, ISBN, CSV, JSON, HTML, URL, UUID, ID, DAO, DTO, Font, FontSize ("Font" is universal in CSS/typography contexts), Image, Icon, View (in CSS layout context), WrapLayout (borrowed name from Java Tutorial), Color, Tag (universal in DB/i18n), OK, GitHub, ISO, UTF-8, JVM, JDK, JUnit, JDBC, MIME, NSIS, Nimbus, L&F.
- **Skipped (Swing / Java-spec overrides)**: `getTableCellRendererComponent`, `getTableCellEditorComponent`, `getColumnCount`, `getRowCount`, `getColumnClass`, `getValueAt`, `setValueAt`, `isCellEditable`, `getColumnName`, `removeEldestEntry`, `paintComponent`, `insertUpdate`, `removeUpdate`, `changedUpdate`, `addActionListener`, `addKeyListener`, `addMouseListener`, `addFocusListener`, `addPropertyChangeListener`, `addDocumentListener`, `addAncestorListener`, `addChangeListener`, `addWindowListener`, `addItemListener`, `addPopupMenuListener`, `getToolTipText`, `getSourceActions`, `setTransferHandler`, `getTransferHandler`, `setPopupVisible`, `setEditable`, `isDisplayable`, `getLayeredPane`, `getLocationOnScreen`, `getScreenSize`, `getClass().getName()`, `getDeclaredConstructor().newInstance()`, `getMetaData()`, `getColumns()`, `executeQuery`, `executeUpdate`, `prepareStatement`, `createStatement`, `getResultSet`, `getString`, `getInt`, `getLong`, `getBoolean`, `getDouble`, `getBytes`, `setString`, `setInt`, `setLong`, `setBytes`, `next()`, `hasNext()`, `setLookAndFeel`, `putClientProperty`, `getClientProperty`, `getRootPane`, `setFocusable`, `setEnabled`, `setSelected`, `setText`, `getText`, `isSelected`, `setFont`, `setBackground`, `setForeground`, `setBorder`, `setOpaque`, `setLayout`, `setPreferredSize`, `setMinimumSize`, `setMaximumSize`, `setAlignmentX`, `setAlignmentY`, `add(Box.createVerticalStrut(…))`, `setCursor`, `setFocusPainted`, `setBorderPainted`, `setContentAreaFilled`, `setRolloverEnabled`, `setVerifyInputWhenFocusTarget`. (These are mandated by the Java/Swing API and cannot be renamed.)
- **Skipped (service names / brands)**: Goodreads, LibraryThing, OpenLibrary, MariaDB, MySQL, H2, Jazzer, jqwik, Launch4j, NSIS, ImageMagick, Swing, Nimbus, Maven, Surefire, JaCoCo, JOptionPane, JPanel, JFrame, JTable, JScrollPane, JLabel, JButton, JTextField, JCheckBox, JComboBox, JDialog, JTextArea, JProgressBar, JMenuItem, JPopupMenu, JTabbedPane, JRootPane, JLayeredPane, JComponent, JTextComponent.
- **Skipped (third-party library classes/imports)**: `LinkedHashMap`, `HashMap`, `ArrayList`, `HashSet`, `ConcurrentHashMap`, `Executors`, `ExecutorService`, `ReentrantReadWriteLock`, `Consumer`, `BiConsumer`, `Supplier`, `SwingWorker`, `SwingUtilities`, `ActionListener`, `KeyListener`, `FocusListener`, `MouseAdapter`, `KeyAdapter`, `DocumentListener`, `TransferHandler`, `DefaultTableCellRenderer`, `AbstractCellEditor`, `AbstractTableModel`, `TableCellRenderer`, `TableCellEditor`, `ComboBoxEditor`, `ComboBoxModel`, `AttributeSet`, `PlainDocument`, `BadLocationException`, `ImageIcon`, `BufferedImage`, `ImageIO`, `Graphics2D`, `RenderingHints`, `GradientPaint`, `Graphics`, `Color`, `Font`, `FontMetrics`, `BasicStroke`, `Shape`, `Dimension`, `BorderFactory`, `EmptyBorder`, `Box`, `BoxLayout`, `BorderLayout`, `FlowLayout`, `GridLayout`, `SwingConstants`, `KeyEvent`, `KeyStroke`, `WindowAdapter`, `WindowEvent`, `Dialog`, `DocumentEvent`, `PrinterJob`, `Printable`.
- **Skipped (SQL strings, regex patterns, property keys, system property names, log tags)**: every `String sql = "SELECT …"` / `String CREATE_TABLE = "…"` / `"jdbc:…"` / `"~/.biblioteca/…"` / `"dbType"` / `"dbUser"` / `"user"` / `"password"` / `"biblioteca.test"` / `"biblioteca.h2.url"` literal.
- **Skipped (CSV column names from third-party services)**: "Book Id", "Title", "Author", "ISBN", "Number of Pages", "Original Publication Year", "Publication Year", "Review", "Collections", "Bookshelves", "Private Notes", "ISBN13", "Binding", "Year Published", etc. — these are dictated by the Goodreads / LibraryThing export formats.
- **Skipped (`.properties` `en` and `es` bundles)**: only `strings_ca.properties` is in scope.
- **Skipped (test/ files)**: not in scope per task rules.
- **Skipped (generated/built files)**: `Biblioteca.html`, `bin/`, `target/`, `*.jar`, `crash-*`, `components.jsx` not in scope.
- **Mixed judgement calls**:
   - "Form", "Field", "Tag", "Color", "View" — kept when used as a single technical term in a context that's clearly a loanword (e.g. `TextField`, `JTextField`, `format`).
   - "Presets" — kept (universal i18n term).
   - "Pantalla" vs "Screen" — the project uses both; "Pantalla" is the Catalan form, "Screen" appears in some class names (LibraryScreenHost, SplashScreen).
   - "Mode" — kept (universal).
   - "Splash" — kept as part of "SplashScreen" because it's the standard term.
   - "Llista" vs "List" — the project uses both; class `Llista` is Catalan, but `LinkedList`/`List<…>` is Java spec.
   - The `jTableBilio` name — flagged even though "Bilio" is short for "Biblioteca" because the `j` prefix and "Bilio" suffix are inconsistent with the rest of the codebase.
   - "AutoCompletion" / "FieldAutoComplete" / "Completer" — all three classes in `herramienta/` do almost the same thing with English names; recommend consolidation in addition to renaming.

## === FILE INVENTORY (for the user's reference) ===
Total scanned:
- 186 .java files in src/  (note: task brief said ~192; the actual count is 186)
- 4 .properties files in src/ (3 i18n bundles + version)
- 1 LICENSE file in src/ (English boilerplate, kept)
- 1 .properties file in src/ (version.properties — version=2.10, technical, kept)
- 1 .xml file at root (pom.xml)
- 1 Makefile at root
- 3 .md files at root (AGENTS.md, CLAUDE.md, README.md)
- ~20 scripts in scripts/ (only # comments reviewed)
- 1 Biblioteca.html (built output, skipped)
- 4 .properties files at root and 0 .bat files at src/ root

