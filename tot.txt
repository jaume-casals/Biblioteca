================================================================================
  BACKLOG (tot.txt) — Biblioteca
  Generated: 2026-06-09
  Status legend: [OPEN] not fixed, [DONE] closed in the most recent session.
  Priority scale: [1] critical / [2] important / [3] nice-to-have.
  Audit gates:  `scripts\test.bat`  (Windows)   |   `make test`  (Linux/macOS).
================================================================================

================================================================================
  0.  SESSION 9  (2026-06-09 — GaleriaCobertesPanel split + tot.txt sweep)
================================================================================

Session 9:
   - B3 [OPEN → DONE].  GaleriaCobertesPanel split 720 → 253 lines.
     New package presentacio/galeria/ with three collaborators:
       * CoverImageService (146 lines): ExecutorService, LRU cache
         (capacity 150, access-order), in-flight set, load-from-blob-or-
         file fallback, center-crop-scale math.  Public surface:
         setCd, getCached, clear, submit, shutdown.
       * CoverCardFactory (375 lines): build(Llibre, int, Listener)
         returning a JPanel; the card's paintComponent (shadow, clip,
         cover, dot, selection overlay, border) is the same as before.
         Carries a static Listener interface (selectedISBNs, focusedIdx,
         setFocusedIdx, currentLlibres, onCardClicked, onCardRightClicked,
         onSelectionMutated) so the host panel can mutate state from
         inside the card's mouse handlers.
       * CoverZoomPopup (83 lines): schedule(Llibre, Component,
         CoverImageService) + hide(); owns the overlay JPanel, the
         debounce Timer, and the JLayeredPane.POPUP_LAYER placement.
     Duplicated inner WrapLayout in GaleriaCobertesPanel (was 41 lines,
     exact copy of presentacio.layouts.WrapLayout) deleted; the panel
     now imports and uses the public one.
     Unused 12-line record presentacio/GaleriaCardModel.java deleted
     (no callers anywhere in src/, test/, or checkBiblio/).
     Public API of GaleriaCobertesPanel unchanged (setCd, setOnCardClick,
     setOnRightClick, setOnDeleteSelected, getSelectedLlibres, selectAll,
     clearSelection, adjustZoom, updateLlibres, applyTheme).
     Verification: scripts\test.bat green — 184 plain + 168 JUnit 5 +
     6 DominiPersistenciaJUnit5 + 3 RestoreSqlJUnit5 = 361 tests,
     0 failures.

   - B11 [OPEN → DONE].  domini/ControladorDomini.java normalized:
     two space-indented methods (swapLlistesOrdre, setLlibreBlob)
     re-indented to match the file's tab convention.  Whole file
     scan: 510 tab-indented lines before, 512 after (and zero
     4-space-indented lines).

   - Tot.txt swept:
       * B3, B11 removed from [OPEN] section 6 (now done above).
       * Four "Decision: keep as-is" [OPEN] entries removed
         (mixed language, ISBN dual type, BibliotecaWriter, DateUtils).
       * Eight "Accepted debt" anti-pattern [HIGH]/[MED]/[LOW] entries
         removed from section 5; their rationales are in earlier
         session notes where they were decided.
       * [PARTIAL] BookReader (section 4) and [PARTIAL] BackupRouter
         (section 3) removed — both are stale historical markers
         with no live code to fix.
       * [DONE] UiTestSupport entry in section 7 removed; it's
         already documented in session 8.
       * Empty sections (3, 4, 5, 7, 8) removed.
     Remaining [OPEN] work: B7 only (ControladorDomini 555-line
     facade — split into shelf/tag/loan/stats delegates).  Still
     deferred; 2-3 hour refactor that touches 20+ callers.

================================================================================
  1.  REMOVED / NOT IN SCOPE
================================================================================

[REMOVED] Web / API layer removed (commits 877f163, fa32d64).
          - src/api/, src/web/ deleted
          - ModeSelectorDialog, ModeButton, WebLauncher deleted
          - Ejecutable rejects --web, always starts Swing
          - ConfigDTO.java kept, Javadoc trimmed
          - If a new HTTP layer is ever added, the api/* router hardening
            (404/409/400/500, PNG MIME sniff, sanitized errors, CSRF) must
            be re-applied — see Session 2 / Session 4 notes for the specifics.

[REMOVED] Mode picker removed with the web layer. Ejecutable no longer
          offers mode choice; --web flag is rejected.

================================================================================
  2.  SESSIONS  (historical record; details in git log)
================================================================================

Session 2 (bugs + dead code + test harness):
   - LlibreRouter update/delete/image: 404 not 500, PNG MIME sniff
   - ApiServer.classify: 404/409/400/500 mapping          (obsolete — api/ removed)
   - OpenLibraryClient: empty catch → LOG, interrupt re-thrown, pages null guard
   - TagDao AUTOCOMPLETE_COLUMNS whitelist verified
   - Dead code deleted: 6 interficie, 6 persistencia, 4 api files
   - Makefile test target: classpath + -jar launcher
   - Llista.setColor: isValidColor() + BibliotecaException.Validation
   - Llibre.setAny: negative-year guard
   - LlibreDao.executeSQLFile: quote-aware split + FORBIDDEN_LEAD blocklist
   - FiltreUtils.matches(): single predicate source
   - Tests: 7 plain + 10 JUnit 5

Session 3 (refactors + I18n + backups):
   - OpenLibraryClient: COVER_BASE/COVER_SIZE + javadoc
   - DialogoError: IS_HEADLESS cached, UIManager warning icon
   - install.sh: Java check + -Xmx512m
   - ModeSelectorDialog: window-X SWING default             (then file deleted)
   - BookExporter.exportHTML: "La meva biblioteca" → I18n key
   - ShelfParser extracted to domini; BookExporter.exportCSV delegates
   - BookExporter.exportPDF dead wrappers deleted
   - UITheme.rebuildFonts: EDT assertion
   - HttpRouter: duplicate errCtx.commit() removed          (obsolete — api/ removed)
   - BackupService.sqlEsc: \u001A stripped
   - GoodreadsCsvStrategy: Private Notes mapped
   - AutoCompletion: noted dead (no presentacio callers)
   - I18n: export_html_heading + dlg_export_html_title keys
   - Tests: 197 plain + 184 JUnit 5

Session 4 (post-SESSION4_PLAN — phased items):
   - LlibreLlistaContext.isbn → long; LlibreLlistaContext.of(...) long
   - LlibreDao.java empty-nom !isBlank() guard
   - CoverService L1 synchronized via L1_LOCK
   - CoverCellRenderer NO_COVER sentinel + convertColumnIndexToView
   - ProgressBarRenderer passes Llibre via model, no DB in paint
   - BibliotecaTableModel.isCellEditable returns column == COL_LLEGIT
   - ImportController / ExportController / BackupController / FilterController
     all use SwingWorker for I/O
   - DetallesLlibrePanelControl: get() inside done() (non-blocking)
   - TagDao.invalidateLlibreTagCache called from delete / deleteFromLlibre /
     deleteAllFromLlibre
   - ControladorDomini: getAllLlistes / getAllTags return new ArrayList copy;
     setLlibreBlob uses Objects.equals; backupToSQL takes snapshot under lock
   - ControladorPersistencia.resetForTest() now synchronized
   - Isbn13Normalizer.toIsbn13 converts any valid 10-digit ISBN to 13
   - CsvUtils.parseIsbn converts any valid 10-digit ISBN to 13 (not just X)
   - BookImporter.importCSV uses Rfc4180Reader + StandardCharsets.UTF_8
   - BookImporter.importCalibre uses StandardCharsets.UTF_8
   - OpenLibraryClient: errorStream drained + disconnect() in finally
   - run.sh: I18nAudit in javac, javalin/kotlin-stdlib removed
   - run2.sh: same fixes
   - StressTest: GraphicsEnvironment.isHeadless() guard, ISBN collision
     detection, log() synchronized, screenshot only on fail/warn
   - UIAudit: biblioteca.h2.url always set, headless guard, copy-paste run
   - ServerConect.MIGRATIONS uses record Migration(int version, String sql)
   - BookExporter: any>0 ? any : "" (HTML)

Session 5 (post-tot.txt sweep — phased items):
   - ShelfParser.exportToCsv: pw.printf now uses Locale.ROOT (es-ES locale
     was emitting "7,5" / "19,99" which broke re-import to 0.0)
   - Config.save: atomic write via Files.move (REPLACE_EXISTING + ATOMIC_MOVE);
     new flushNow() public method; shutdown hook registered for SAVE_SCHEDULER
   - ConfiguracioDialog: db-size query moved off EDT to SwingWorker
   - ShelfController.refreshComboLlistes: DB calls now in SwingWorker
   - ControladorDomini.deleteLlibre(Long): null ISBN now throws Validation
     (not NotFound) per I18n audit
   - ControladorDomini.backupToSQL: loadHeavyFields() moved OUTSIDE the
     synchronized block; snapshot taken atomically inside the lock
   - ControladorDomini.swapLlistesOrdre: in-memory swap moved BEFORE the DB
     write, with symmetric undo in the catch block
   - LlibreValidator.countDig: replaced String.valueOf(n).length() with a
     branchless threshold chain (correct for Long.MAX_VALUE, negative, etc.)
   - LlibreDao.search: instanceof chain replaced with bindParam() pattern-
     matching switch (null → SQL NULL; unknown types throw with class name)
   - LlibreDao.executeSQLFile: splitStatements() scans the whole file in
     one pass with inQuote state, so ';' inside string literals across
     lines no longer mis-splits
   - ServerConect.runMigrations: per-engine dispatch (H2 keeps the
     setAutoCommit(false)/commit/rollback wrapper; MariaDB/MySQL takes a
     non-transactional path because DDL auto-commits there); Javadoc
     explains the manual-cleanup requirement on a mid-sequence MariaDB
     failure and the idempotency guarantee
   - BackupService.autoBackup: removed redundant Thread.sleep(30_000) —
     scheduleWithFixedDelay already has the 30s initial delay
   - Llibre: new Builder and bindUpdateableFields() helper; all internal
     callers (LlibreValidator, LlibreDao) use the builder or the helper.
     9-positional-arg ctor kept for backward compat.
   - UITheme.FONT_BASE / FONT_BOLD: now private static volatile with
     fontBase() / fontBold() accessors; 16 callers updated
   - strings/ui.csv: added app_title, tab_advanced, val_any_negatiu,
     err_db_connect, err_db_close, dlg_shortcuts_content,
     validation_invalid_number, dlg_restore_title, dlg_backup_title,
     csv_bad_isbn, export_html_heading keys
   - I18nAudit.loadCsvKeys + auditDuplicateKeys: quote-aware splitCsvLine() +
     stripQuotes() helper. Naive s.split(",", 4) kept the surrounding
     quotes on the key column, so 26 real keys were reported as "missing
     from CSV" even though they were present.
   - I18nAudit.auditJavaKeyUsage: now ignores captured keys ending in "_"
     (concatenation sites, e.g. I18n.t("err_db_init_" + cfg.dbType())).
   - MainFrameControl JFrame title and AboutDialog header now use
     I18n.t("app_title")
   - LlistesDelLlibreDialog: shelfCheckList now MULTIPLE_INTERVAL_SELECTION
     and pre-selects indices for current shelf memberships
   - ControladorPersistencia: 17-line audit-history comment trimmed to a
     14-line class JavaDoc
   - I18nAudit: FAIL=0, WARN=1012 (down from FAIL=35, WARN=1036)

Session 6 (tot.txt consolidation — 2026-06-09):
   - MainFramePanel now extends JPanel (was JFrame); chrome lives in
     MainFrameControl. statusBar gets accessibleName from I18n.t.
   - GuardarLlibresDialogo: addFieldEntry / addComboEntry / addCheckEntry
     inlined; now uses presentacio.FormGridBuilder. Still keeps
     setSize(600, 720) override — tracked below.
   - Tidy: dead RunI18nAudit.java and i18n_audit_test.txt deleted from
     repo root (I18nAudit is now driven from checkBiblio/run*.ps1).

Session 7 (tot.txt sweep — 20 of ~27 [OPEN] items closed):
   - parseDoubleOrZero dedup: removed from domini/ShelfParser.java,
     consolidated to herramienta/csv/CsvUtils.java (now also null/blank
     safe).
   - UITheme.rebuildFonts(String) → rebuildFonts(FontSize); legacy
     String overload kept as a thin compat shim.  Two call sites
     (Ejecutable, MostrarBibliotecaPanel) migrated.
   - MainFrameControl.getInstance(): removed the unused single-arg
     (panel) overload; init path is (panel, cd), read path is ().
   - ControladorDomini.getLlistaById / getTagById: O(1) via new
     llistesById / tagsById maps; rebuilt atomically in
     rebuildIdIndexes() inside BIB_LOCK (load, restore, clear paths).
   - GuardarLlibresDialogo: removed setSize(600, 720) override that
     was undoing the constructor's setPreferredSize.
   - LlibreValidator.countDig: 13-branch magic ladder replaced with
     Long.toString(Math.abs(n)).length().
   - BookReader / InMemoryBibliotecaReader / ControladorDomini:
     get10Llibres / get100Llibres / maxIndex100Llibres KEPT (test
     suite BibliotecaTest:265,1707 and BibliotecaJUnit5Test:481,1639
     exercise them; tot.txt's "Likely-dead" claim was incorrect).
   - MainFrameControl: extracted assignToCurrentShelfIfNeeded(Llibre)
     helper from the dense onBookUpdated body.
   - I18n.langIndex(): magic 0/1/2 literals replaced with
     Lang.fromCode(Config.getLang()).ordinal().
   - GoodreadsCsvStrategy / LibraryThingCsvStrategy: canHandle now
     requires a minimum column count and a canonical
     strategy-specific column (Title / Authors / Exclusive Shelf /
     BCID) so a stray substring can't trigger the wrong strategy.
   - GuardarLlibresDialogo: added paisOrigen / estat / exemplars /
     nomCa / nomEs / nomEn fields to match DetallesLlibrePanel
     (create-book flow parity with edit-book flow).  Controller wired
     through l.setPaisOrigen / setEstat / setExemplars /
     setNomCa / setNomEs / setNomEn.
   - getAllLlibresSummary() (lean variant) added to BookReader +
     ControladorPersistencia; MostrarBibliotecaControl.refresh()
     opts in to skip heavy text/notes/cover when no shelf is active.
   - UITheme theme selector: all 4 themes (LIGHT/DARK/SEPIA/OCEAN)
     already reachable from ConfiguracioDialog and the sidebar
     toggle; added i18n keys theme_light / theme_dark / theme_sepia /
     theme_ocean to ui.csv so the user-facing labels render.
   - UIComponents (presentacio): extended with styleSidebarButton and
     cardBorder; 160 call sites migrated from UITheme.styleXxx to
     UIComponents.styleXxx across 17 presentacio files (UIComponents
     itself still delegates to UITheme — the wrapper factory
     pattern is now in place; UITheme.styleXxx can be deleted in a
     follow-up without touching any caller).
   - BookExporter.exportHTML: split into writeHtmlHeader /
     writeHtmlBody / writeHtmlGroupedByShelf / writeHtmlShelfHeader
     helpers.  HTML_CSS constant unchanged (was already a constant).
   - ControladorPersistencia: getAllLlibresSummary() = getAll() (the
     underlying DAO already uses buildLlibreLight, so the lean view
     was implicit).  Documented in Javadoc.
   - I18nAudit.auditDuplicateKeys: was already implemented (lines
     170-191 of checkBiblio/I18nAudit.java); tot.txt entry was stale.
   - Config god-class split: 4 sub-stores (UI_STORE, DB_STORE,
     WINDOW_STORE, FILTER_STORE) with a composite Map view that the
     existing file-load/save code uses unchanged.  UiConfig /
     DbConfig / WindowConfig got a few more typed accessors
     (UiConfig.lang / currency / readingGoal / sortColumn /
     sortOrder; DbConfig.profile / hasPassword).  Static setters on
     Config remain — the public API surface is unchanged.
   - B2 plaintext DB password: Javadoc on getDbPassword() now
     documents the exposure surface (config.properties readability,
     backup-tool leakage, shared-host risk) and the three migration
     paths (OS keystore, jasypt, integrated auth).  No code change
     to the storage path.
   - B4 mostrarEstadistiques: buildGeneralTab (goal + summary +
     shelf table) extracted into EstadistiquesHelper; controller
     goes from a 115-line method to 7 lines for the general tab.
   - B5 BookActionsController:108: replaced
     MainFrameControl.getInstance().getLlibreIsbn(isbn) with
     state.cd.findLlibre(isbn).orElse(null) — same source of truth,
     no singleton hop from a child controller.
   - B6 LlibreDao INSERT/UPDATE binding: extracted
     persistencia/LlibreFieldBindings with forInsert / forUpdate
     returning Object[] value lists and a single bind(PreparedStatement,
     int, Object) helper.  Both insert and update in LlibreDao are
     now ~6 lines; adding a new column is a single place.  The
     BackupService.writeLlibreINSERT path was intentionally left
     untouched — its per-field SQL formatting (%.4f for doubles, ''
     for empty strings) diverges from the JDBC path and the tot.txt
     didn't ask to align them.

   - B3 (GaleriaCobertesPanel 644-line split) and B7
     (ControladorDomini 532-line facade split into Shelf/Tag/Loan/
     Stats delegates) and checkBiblio.UiTestSupport extraction
     DEFERRED — each is a 2-3 hour refactor that touches 20+ callers
     and risks breaking the build.  The other 24 [OPEN] items from
     the original tot.txt were completed; those three remain [OPEN]
     below.  (B3 + UiTestSupport landed in sessions 8 + 9; B7 is
     still open.)

Session 8 (checkBiblio harness dedup — item 7 closed):
   - checkBiblio.UiTestSupport (new, 376 lines): public-final static-utility
     class holding the helpers that were duplicated between UIAudit and
     StressTest — findBtnIn (Container + JFrame overloads), findBtnByTooltip,
     findComponent<T>, findTextFieldNear, findCheckBox, findCheckBoxGlobal,
     flattenVisible, collectComponents, waitForMainFrame, waitForDialog,
     getTopDialog, getTopDialogExcept, getTopWindow, findWindowByTitle,
     windowTitle, clickComponent(Robot, Component), setField, setFieldNear,
     doClick, clickSave, isBookDetailsDialog, isBookFormDialog,
     looksLikeError, isTestBookDeleteConfirm, norm, sleep, pressKey,
     pressCtrlN, pressCtrlA, pressEscape, pressEnter, pressDelete.  Each
     helper is a thin pass-through to the body that was previously copy-
     pasted in both UIAudit and StressTest; naming was unified in the
     process (e.g. findButtonContaining → findBtnIn, the JFrame / Robot /
     PrintWriter state stays local to each harness main).
   - UIAudit.java: 993 → 552 lines (-441).  All helpers delegate to
     UiTestSupport.  No public API change.
   - StressTest.java: 1619 → 1377 lines (-242).  Same delegation; the
     screenshot-on-regression branch in phase() is replaced with a log
     line ("PHASE n produced new issues: fails +N warns +N").
   - Screenshot feature removed entirely (per session-8 user request).
     The cmdScreenshot interactive command, the screenshotSeq counter,
     Files.createDirectories("checkBiblio/screenshots"), the cmdScreenshot
     and screenshot() bodies, all screenshot calls in runAutomated and
     phase() runners, the screenshot-cleanup blocks in run.sh / run.ps1
     / run2.sh / run2.ps1, the SCREENSHOT FAILED regex in the issue-
     pattern match, and the 42 screenshot PNGs + the now-empty
     checkBiblio/screenshots/ directory are all gone.
   - run.sh / run.ps1 / run2.sh / run2.ps1: javac class list updated to
     include checkBiblio/UiTestSupport.java alongside UIAudit.java /
     I18nAudit.java / StressTest.java.
   - Verification: `scripts\test.bat` → 184 plain + 168 JUnit 5 +
     6 DominiPersistenciaJUnit5 + 3 RestoreSqlJUnit5 = 361 tests, 0
     failures.  checkBiblio files compile cleanly with
     `javac -Xlint:deprecation`.  Live boot of UIAudit reaches the
     interactive console; live boot of StressTest passes phases 01-05
     with the new helpers (setFieldNear, clickSave, waitForDialog,
     isBookFormDialog).

Session 9 (see top of file): GaleriaCobertesPanel split
(presentacio/galeria/* trio), B11 indentation normalization, and
tot.txt sweep.

================================================================================
  3.  CROSS-CUTTING / NICE-TO-HAVE
================================================================================

[OPEN]  B7  LARGE  ControladorDomini 555-line facade — group shelf/tag/
                   loan/stats into delegate objects to slim the public
                   surface.  Deferred from session 7 — 2-3 hour refactor,
                   deferred to a follow-up.

================================================================================
  4.  TESTS
================================================================================

Current state (last `scripts\test.bat` run, 2026-06-09, post-session-9):
  - 184 plain tests passing  (test/BibliotecaTest.java)
  - 168 JUnit 5 tests passing (test/BibliotecaJUnit5Test.java +
                                  test/DominiPersistenciaJUnit5Test.java)
  - 3 RestoreSqlJUnit5 tests passing
  - 0 failures, 0 skipped.

Required gate:  `scripts\test.bat`  (Windows)  |  `make test`  (Linux/macOS)
must be green before merging any code change.
